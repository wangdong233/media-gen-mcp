# pares5 · M4 收尾发版:功能分析

> **迭代**:pares5 · **阶段**:M4(收尾)· **日期**:2026-07-20
> **前置**:M0-M3 完成(7 commit,117/117 测试,16 工具,3 vision provider + fallback 链)
> **M4 目标**:README(vision 章节+三档部署手册+rejected)+ 0.11.0 发版 + 发版前最终验证。

---

## 一、M4 范围(文档 + 发版,无新代码)

1. **README**:
   - 加「🔍 图像识别(0.11.0+)」章节:4 工具(extract_text/table/chart/describe_image)+ 3 provider(tesseract/paddle/vlm)+ 三档部署手册 + config 示例 + fallback 链 + License + rejected-by-design
   - Providers 段补 paddle/vlm(识别 provider)
   - 架构段补 vision 第三模态
2. **package.json**:0.10.0 → 0.11.0
3. **check-schema**:已 16 工具(M2 改),M4 确认
4. **发版**:build + 全量 117 测试 + git commit/push(HTTPS) + npm publish

---

## 二、发版前验证清单

- [ ] build 零错
- [ ] 117/117 测试零回归(check-schema 16 工具 + fixes + pares3 + pares5_m1/m2/m3)
- [ ] README vision 章节完整(4 工具 + 3 provider + 三档部署 + config + rejected)
- [ ] serverInfo.version 自读 package.json(M0 确认,无需手改)
- [ ] git commit + push HTTPS(SSH 被 fake-ip 拦)
- [ ] npm publish(prepublishOnly 自动 build+chmod+test)

---

## 三、0.11.0 发版说明(release notes 核心)

**新增:图像识别(4 工具 + 3 provider)**
- extract_text / extract_table / analyze_chart / describe_image
- tesseract(进程内 WASM 零配置兜底)+ paddle(PaddleX serving REST 中文 SOTA)+ vlm(vLLM OpenAI 兼容增强)
- 能力袋 MediaProvider 组合类型 + vision 第三模态 + 完整 fallback 链(复用 0.10.0 机制)
- 纯免费 Apache 全栈,避开 AGPL/GPL/阈值/署名陷阱

**向后兼容**:非破坏性拓宽,现有 12 工具 + agnes/zhipu 零感知;未配识别 provider 则 tesseract 零配置兜底 extract_text。
