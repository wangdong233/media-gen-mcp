# P0-2 测试报告 · LLM 友好错误契约(handler 层引擎 stderr 归一化)

> **阶段**: P0-2(继 01-功能分析 / 02-实施记录 / 03-代码审查 之后第 4 阶段:测试)
> **角色**: 测试工程师(P0-2)
> **测试日期**: 2026-07-21
> **依据**: PRD `doc_v11/P0-2-LLM友好错误契约实施规划.md` + `pares2/01-功能分析.md`(test_strategy)+ `pares2/02-实施记录.md` + `pares2/03-代码审查.md`
> **目标代码**: `/Users/wangdong/Documents/Project/Agnes AI接入/media-gen-mcp`(MIT,v0.11.0,main)
> **测试结论**: **test_ok = true · 全部 7 道闸门 green · 78 case 全绿 · F1 medium 残留已闭环**

---

## 0. 结论速览(all_passed = true)

### 0.1 7 道闸门实地复跑结果

| 闸门 | 命令 | 本测试复跑结果 | 备注 |
|---|---|---|---|
| **TypeScript build** | `npm run build` | ✅ 0 error(`tsc` `noUnusedLocals:true` 严) | F1 修复后零回归 |
| **Schema 防御** | `npm test`(check-schema.mjs) | ✅ 5 passed(G1/G2/G3 三断言;G2 仍守 19 工具) | 向后兼容验证 |
| **冒烟测试(既有)** | `node --test test/error-format.smoke.mjs` | ✅ **27 passed**(实施时 R2 新增 3 条 case) | 14 pattern 正样本 + 反崩溃 + 元契约 + Graphviz bug 锁死 e2e |
| **跨引擎回退防御** | `node scripts/check-error-text.mjs` | ✅ 11 passed | d2/graphviz/chart/index 4 文件不含跨引擎回退触发子串 |
| **【新】单元测试** | `node --test test/unit.error-format.mjs` | ✅ **30 passed** | pattern 边界 + helper 白盒 + **F1 修复锁死 3 条** + engineHint 路由 |
| **【新】集成测试** | `node --test test/integration.error-format.mjs` | ✅ **14 passed** | handler 包装模块间接通 + 4 处 PNG 复用对称性 + 顶层 err 协议 |
| **【新】用户旅程测试** | `node --test test/user-journey.error-format.mjs` | ✅ **7 passed**(总耗时 ~18s) | MCP stdio e2e:4 错输入 + 2 happy path + tools/list |

**合计 78 case 全绿**;**build/test 双闸门 F1 修复后零回归**。

### 0.2 关键交付物

| 新增/修改 | 文件(绝对路径) | 内容 |
|---|---|---|
| 新建 | `/Users/wangdong/Documents/Project/Agnes AI接入/media-gen-mcp/test/unit.error-format.mjs` | 30 case:14 pattern 边界 + helper 白盒 + F1 修复锁死 + engineHint 路由 + 元契约 |
| 新建 | `/Users/wangdong/Documents/Project/Agnes AI接入/media-gen-mcp/test/integration.error-format.mjs` | 14 case:handler 包装仿真 + 模块级 GraphvizEngine happy/error + 4 处 PNG 复用对称性 |
| 新建 | `/Users/wangdong/Documents/Project/Agnes AI接入/media-gen-mcp/test/user-journey.error-format.mjs` | 7 case:MCP stdio JSON-RPC 完整旅程(spawn `node dist/index.js`) |
| 修改 | `/Users/wangdong/Documents/Project/Agnes AI接入/media-gen-mcp/src/handlers/error-format.ts` | **F1 medium 修复**:兜底分支 line 410 加 `typeof ctx.input === "string"` check(1 行改动 + 6 行注释) |

### 0.3 F1 medium 残留闭环

R3 代码审查(03-代码审查.md §0.1)登记的唯一 medium 残留 **F1(兜底分支对 object ctx.input 输出 `[object Object]`)** 已在本测试阶段闭环:

- **修复**:`src/handlers/error-format.ts:410` 加 `typeof ctx.input === "string" ? ctx.input : JSON.stringify(ctx.input)`(与 R2 已修的 make 函数 + 主函数 make 调用层完全对称)
- **锁死测试**:`test/unit.error-format.mjs` 新增 3 条 case(F1 fix 三视角):
  - object ctx.input 不输出 `[object Object]`,改输出 JSON 片段(`"mark":"bar"`)
  - object ctx.input 截断到 80 字符(防超长 spec 撑爆 token)
  - string ctx.input 行为不变(向后兼容)
- **触发路径已锁死**:`generate_chart handler`(`src/index.ts:1008`)传 Vega-Lite spec 对象作为 ctx.input,Vega-Lite 错误形态多样(3 条 pattern 之外的未识别错误)落到兜底是常态

---

## 1. 测试策略(PRD §5 + pares2/01 §6 落地)

### 1.1 四层测试矩阵

| 层 | 形态 | 接入位置 | 文件 | case 数 |
|---|---|---|---|---|
| **冒烟测试** | `node --test` 单文件 | 实施者本地手动跑(保 P0-3 立场) | `test/error-format.smoke.mjs` | 27 |
| **【新】单元测试** | `node --test` 单文件 | 同上 | `test/unit.error-format.mjs` | 30 |
| **【新】集成测试** | `node --test` 单文件 | 同上 | `test/integration.error-format.mjs` | 14 |
| **【新】用户旅程测试** | `node --test` 单文件 + spawn MCP server | 同上 | `test/user-journey.error-format.mjs` | 7 |

**说明**:三层测试文件互不重叠,**与既有 smoke 测试分工明确**:
- smoke:14 pattern 各 1 真实样本的正样本广度覆盖(实施者写)
- unit:针对 pattern 的**边界 / 同源变体 / 数据流细节**做白盒断言(测试工程师写)
- integration:**handler 层模块间接通**(handler catch 块实际接入 normalizeEngineError)
- user-journey:**MCP stdio 完整端到端**(以用户视角验证 isError + content[0].text)

**不接入 `npm test`**:保 P0-3 立场(P0-3 才引入 test/ 目录结构、tsconfig.test.json、CI stale gate、`npm test` 接入)。`package.json` 的 `"test": "node scripts/check-schema.mjs"` 零变更。

### 1.2 测试覆盖哲学

按 PRD §1.2 五条成功标准 + 反崩溃硬契约,每条都有可自动验证的 case 锁死:

| PRD 条款 | 自动验证 case | 锁死文件 |
|---|---|---|
| §1.2.1 契约三件套 | 元契约 case `every known case carries [engine] tag + 修复 verb` | smoke + unit |
| §1.2.2 offending 片段回显 | 每条 pattern 的 make 函数都断言 offending(包括 F1 修复后兜底分支也回显) | unit + user-journey |
| §1.2.3 Graphviz bug 修复 | e2e 锁死 `digraph G { A -> }` → 不再 `produced no SVG` | smoke + integration + user-journey |
| §1.2.4 向后兼容零感知 | 19 工具 inputSchema 字节比对 + happy path 字节返回 + check-schema.mjs G2 | integration + user-journey |
| §1.2.5 覆盖基线 ≥14 条 | 14 pattern 各 1 真实样本 + 17 个 D2 style 关键字全量覆盖 | smoke + unit |
| 反崩溃硬契约 | `/TypeError\|Cannot read\|is not a function/` 永不穿透(元 case + 反崩溃 case) | smoke + unit + integration |

---

## 2. 测试矩阵(完整用例清单)

### 2.1 单元测试(`test/unit.error-format.mjs`,30 case)

#### D2 pattern 1(数值属性范围)— 4 case

| 用例 ID | 描述 | 期望 | 结果 |
|---|---|---|---|
| U-D2-p1-1 | 含连字符 stroke-width(0–15)+ errmsg 1-indexed 行号 | `[d2] line 1:` + `0–15` + offending 回显 `style.stroke-width: 1.5` | ✅ |
| U-D2-p1-2 | 含连字符 border-radius(0–20) | `border-radius` + `0–20` + `__didMatchLastKnownPattern() === true` | ✅ |
| U-D2-p1-3 | 行号解析优先取 errmsg 1-indexed(不取 range 0-indexed) | range=5 errmsg=2 → `line 2` + offending 取 input 第 2 行 | ✅ |
| U-D2-p1-4 | 非 JSON 字符串形态(extractD2Errmsgs 回退 [rawMsg]) | `expected "opacity" 0–1` + `__didMatchLastKnownPattern() === true` | ✅ |

#### D2 pattern 3(style 关键字)— 2 case

| 用例 ID | 描述 | 期望 | 结果 |
|---|---|---|---|
| U-D2-p3-1 | 全量 17 个 style 关键字逐一匹配(锁死 PRD §9 #13 回归) | 17/17 全命中 pattern 3,无落入兜底 | ✅ |
| U-D2-p3-2 | 反向引用 `\1` 守护:形似但非 `<KW> must be style.<X>` 不误匹配 | `fill must be style.stroke` 不命中 pattern 3 | ✅ |

#### D2 pattern 4(pickUnbalancedBrace 简化版启发式)— 2 case

| 用例 ID | 描述 | 期望 | 结果 |
|---|---|---|---|
| U-D2-p4-1 | map 块未闭合 → offending 回显首个含 `{` 的行 | `offending:.*x: \{` | ✅ |
| U-D2-p4-2 | map 块已闭合(depth=0)→ 无 offending 片段(format 省略) | 不含 `offending:` | ✅ |

#### Graphviz / Vega-Lite / resvg patterns — 9 case

| 用例 ID | 描述 | 期望 | 结果 |
|---|---|---|---|
| U-G-p1 | syntax error in line N near 'X' → offending 回显该行原文 | `[graphviz] line 2:` + offending | ✅ |
| U-G-p2 | syntax error in line N(无 near)→ 兜底 offending | `line 3:` + offending | ✅ |
| U-G-p3 | syntax error near 'X' 无行号 → 不抛 offending | 不含 `offending:` | ✅ |
| U-V-p1 | Invalid field type 'undefined' → offending 指向 channel.type 路径 | `encoding.<channel>.type="<缩写或空>"` | ✅ |
| U-V-p2 | Unrecognized signal name 'foo' → datum 语法 remediation | `datum\.<field>` | ✅ |
| U-V-p3 | gradient misuse → 走 mark.fill/encoding.color.scale remediation | `mark\.fill\|encoding\.color\.scale` | ✅ |
| U-R-p1 | 缺 xmlns 的 SVG → offending 回显前 80 字符 | `offending: <svg>.*width="50"` | ✅ |
| U-R-p2-a | default font-family '' not found(含单引号)→ 字体 remediation | `字体加载失败` + `PingFang\|Noto\|Microsoft YaHei` | ✅ |
| U-R-p2-b/c/d | 'No match for ... font-family.' / Failed to load / malformed / family name 三变体 | 全部命中 pattern 2 | ✅ |

#### helper 白盒 — 3 case

| 用例 ID | 描述 | 期望 | 结果 |
|---|---|---|---|
| U-H-1 | extractD2Errmsgs: 非 JSON → 回退 [rawMsg](反崩溃) | `[d2]` + 含原 raw | ✅ |
| U-H-2 | extractD2Errmsgs: JSON 数组但 entries 无 errmsg 字段 → 回退 [rawMsg] | `[d2]` | ✅ |
| U-H-3 | pickD2Line: 多条 errmsg → 取首条匹配 pattern 的(非 entries[0]) | 命中第 2 条 pattern 3 | ✅ |

#### F1 修复锁死(R3 medium 残留闭环)— 3 case 🔒

| 用例 ID | 描述 | 期望 | 结果 |
|---|---|---|---|
| **U-F1-1** | 兜底分支对 object ctx.input 不输出 `[object Object]`,改输出 JSON 片段 | 不含 `[object Object]`;含 `"mark":"bar"` + `"quantitativ"`(用户拼错的值) | ✅ |
| **U-F1-2** | 兜底分支对 object ctx.input 截断到 80 字符(防超长 spec) | preview ≤ 100 字符 | ✅ |
| **U-F1-3** | 兜底分支对 string ctx.input 行为不变(向后兼容) | 含 `x: { bad syntax }` + 不含 `[object Object]` | ✅ |

#### engineHint 路由边界 — 2 case

| 用例 ID | 描述 | 期望 | 结果 |
|---|---|---|---|
| U-EH-1 | engine='d2' + engineHint='resvg' → 走 resvg patterns 表 | `[resvg]` + `xmlns` | ✅ |
| U-EH-2 | engine='d2' 不给 hint → 按 engine 走 d2 patterns 表(不命中) | `[d2]` + 兜底 | ✅ |

#### 反崩溃契约 — 2 case

| 用例 ID | 描述 | 期望 | 结果 |
|---|---|---|---|
| U-AC-1 | 空字符串 rawMsg → 返回 `[d2]` + 兜底标记,不抛 | `string` + `[d2]` | ✅ |
| U-AC-2 | 反崩溃 regex `/TypeError\|Cannot read\|is not a function/` 永不出现(4 引擎 × 未知错误) | 4 个 case 全 doesNotMatch | ✅ |

#### 元契约(三件套)— 2 case

| 用例 ID | 描述 | 期望 | 结果 |
|---|---|---|---|
| U-MC-1 | 归一化文本单行拼装,定界符 `' | '` 清晰分隔三件套 | 单行 + 顺序 engine → msg → offending → 修复 | ✅ |
| U-MC-2 | 兜底文本带换行标记(区分归一化成功 vs 兜底) | 含 `\n` + `未识别的错误形态` | ✅ |

### 2.2 集成测试(`test/integration.error-format.mjs`,14 case)

#### handler 层仿真(`simulateHandlerCatch`)— 7 case

| 用例 ID | 描述 | 期望 | 结果 |
|---|---|---|---|
| I-H-1 | generate_diagram + D2 float stroke-width → 归一化文本 | `[d2]` + `stroke-width` + `0–15` + `整数` + offending | ✅ |
| I-H-2 | generate_diagram + Graphviz 非法 DOT → 归一化(锁 §4.1) | `[graphviz]` + `line 1` + `意外 token "}"` + offending | ✅ |
| I-H-3 | generate_diagram + D2→PNG 复用 [resvg] 前缀路由 | `[resvg]` + `xmlns`(不落入 d2 兜底) | ✅ |
| I-H-4 | generate_chart + Vega-Lite Invalid field type | `[vega-lite]` + 4 合法 type + `拼错\|缩写` | ✅ |
| I-H-5 | generate_chart + object ctx.input 不崩(R2-1 在 handler 路径仍生效) | `[resvg]` + offending 含 `"mark":"bar"` + 不含 TypeError | ✅ |
| I-H-6 | render_svg + resvg 字体加载失败 → 路由到 resvg patterns | `[resvg]` + `字体加载失败` + `PingFang` | ✅ |
| I-H-7 | render_svg + 非 resvg 错(Chrome 不可用)→ 不归一化(原样或兜底) | `Chrome\/Edge not available\|未识别的错误形态` | ✅ |

#### 模块级 e2e — 3 case

| 用例 ID | 描述 | 期望 | 结果 |
|---|---|---|---|
| I-M-1 | GraphvizEngine 合法 DOT happy path → 返回 svg | `<svg` + len > 100 | ✅ |
| I-M-2 | GraphvizEngine 非法 DOT → 抛 syntax error(锁 §4.1) | `syntax error in line 1 near '}'` + 不含 `produced no SVG` | ✅ |
| I-M-3 | GraphvizEngine PNG 复用 [resvg] 前缀(代码静态断言) | `throw new Error("[resvg] " +` 在源码里 | ✅ |

#### 契约对称性 — 4 case

| 用例 ID | 描述 | 期望 | 结果 |
|---|---|---|---|
| I-S-1 | 4 处 PNG 复用路径全部加 [resvg] 前缀(d2/graphviz/chart/render-svg) | 4/4 文件全有前缀模式 | ✅ |
| I-S-2 | handler 三处包装都 import normalizeEngineError | 3/3 case body 都有调用 | ✅ |
| I-S-3 | 不存在第二个错误归一化函数定义(enhanceD2Error / enhanceGraphvizError 已删) | 无 `function enhanceD2Error(` 也无 `function enhanceGraphvizError(` | ✅ |
| I-S-4 | 顶层 err() 协议不变(单字段 {content:[{type:"text"}], isError}) | `function err(message: string)` 签名零变更 | ✅ |

### 2.3 用户旅程测试(`test/user-journey.error-format.mjs`,7 case)

经真实 MCP stdio JSON-RPC 协议 spawn `node dist/index.js`,验证用户实际看到的 isError + content[0].text。

| 用例 ID | 描述 | 用户请求 | 期望 | 结果 | 耗时 |
|---|---|---|---|---|---|
| **UJ-1** | Graphviz 非法 DOT | `generate_diagram { code:"digraph G { A -> }" }` | isError=true + `[graphviz]` + 中文消息 + 不含 `produced no SVG` | ✅ | 1063ms |
| **UJ-2** | Vega-Lite 拼写错 type | `generate_chart { spec:{...type:"quantitativ"} }` | isError=true + 4 个合法 type + `拼错\|缩写` | ✅ | 1053ms |
| **UJ-3** | render_svg 缺 xmlns | `render_svg { svg:'<svg><rect/>', format:"png" }` | isError=true + `[resvg]` + `xmlns` + offending | ✅ | 8362ms |
| **UJ-4** | D2 float stroke-width | `generate_diagram { code:"x: { style.stroke-width: 1.5 }" }` | isError=true + `[d2]` + `stroke-width` + `0–15` + `整数` + offending | ✅ | 2463ms |
| **UJ-5** | D2 happy path | `generate_diagram { code:"a -> b", format:"svg" }` | isError ≠ true + `{engine, format, local_path}` | ✅ | 2662ms |
| **UJ-6** | Graphviz happy path | `generate_diagram { code:"digraph G { A -> B -> C }", engine:"graphviz" }` | isError ≠ true + `{engine:"graphviz", format:"svg"}` | ✅ | 1057ms |
| **UJ-7** | 19 工具枚举不变 | `tools/list` | `tools.length === 19` + 关键工具名存在 | ✅ | 1056ms |

**总耗时**:~18s(7 case 串行;D2 WASM 加载首次 ~2.5s 是主要瓶颈,其余 ~1s)。

### 2.4 既有冒烟测试复跑(`test/error-format.smoke.mjs`,27 case)

实施者已交付,本测试阶段实地复跑全绿(对齐 03-代码审查 §0.2 的复跑结果):
- 14 条 pattern 各 1 真实样本(D2 ×6 / Graphviz ×3 / Vega-Lite ×3 / resvg ×2)
- 2 条 D2 含连字符 style 关键字(font-size / border-radius)
- D2 Go 错误模板负样本(锁死 §9 #11 不误路由到 resvg)
- 反崩溃 ×2(未知错误 + 空字符串)
- engineHint 路由 ×2
- 元契约(12 case 都带 `[engine]` + `修复:`)
- **Graphviz bug 锁死 e2e**(`digraph G { A -> }` 不再 produced no SVG)
- **Graphviz happy path**(零回归)
- **R2-T1**:resvg pattern 1 对 object ctx.input 不崩(锁死 R2-1)
- **R2-T2**:p.make 抛 circular ref 被主函数 try/catch 兜底(锁死 R2-2)
- **R2-T3**:chart.ts PNG 复用 [resvg] 前缀透传(node:module register 装 loader mock @resvg/resvg-js)

---

## 3. PRD 对齐检查表(PRD §1.2 五条 + 反崩溃硬契约)

| PRD 条款 | 要求 | 本测试验证 case | 状态 | 证据 |
|---|---|---|---|---|
| **§1.2.1 契约三件套** | 4 引擎每种已知错误经归一化必含 ① 定位、② 问题陈述、③ 修复动词 | smoke 元契约 case + unit 元契约 + user-journey 4 错输入 | ✅ | smoke 行 282-304 / unit U-MC-1 / UJ 1-4 |
| **§1.2.2 offending 片段回显** | D2/Graphviz 回显出错行原文,Vega-Lite 指出 channel 字段路径,resvg 回显 SVG 前 80 字符 | 每条 pattern 的 make 函数都断言 offending(含 F1 修复后兜底分支也回显 JSON 片段) | ✅ | unit U-D2-p1/p3/p4 / U-G-p1 / U-V-p1 / U-R-p1 / **U-F1-1** |
| **§1.2.3 Graphviz bug 修复** | `digraph G { A -> }` 必含 `syntax error in line 1 near '}'` 而非 `produced no SVG` | smoke e2e + integration I-M-2 + UJ-1 | ✅ | smoke 行 308-321 / I-M-2 / UJ-1 |
| **§1.2.4 向后兼容零感知** | 合法调用字节不变、19 工具 inputSchema 不变、check-schema.mjs 仍 pass、错误仍是单字段协议 | integration I-S-1/2/3/4 + UJ-5/6/7 + check-schema.mjs G2 + git diff 字节比对 | ✅ | 见 §4 向后兼容专项 |
| **§1.2.5 覆盖基线 ≥14 条** | knownErrorPatterns 首版 ≥14 条(D2×6 / Graphviz×3 / Vega-Lite×3 / resvg×2) | smoke 14 pattern 正样本 + unit U-D2-p3-1 全量 17 关键字 | ✅ | smoke 行 30-200 / unit U-D2-p3-1 |
| **反崩溃硬契约** | `/TypeError\|Cannot read\|is not a function/` 禁穿透 | smoke 反崩溃 ×2 + R2-T1/T2 + unit U-AC-1/2 + integration I-H-5 | ✅ | smoke 行 203-236, 246-259 / unit U-AC |

**结论**:PRD §1.2 五条 + 反崩溃硬契约全部达标。F1 修复后 §1.2.2 兜底场景 offending 回显软指标也达标(原先只影响兜底场景,现在连兜底都回显 JSON 片段)。

---

## 4. 向后兼容验证专项(诚实 + 字节级)

### 4.1 19 工具 inputSchema 字节比对(零变更)

| 验证项 | 方法 | 结果 |
|---|---|---|
| 工具数 | `npm test` G2 断言 | ✅ 19 工具齐全 |
| inputSchema: 出现次数 | grep 计数 src/index.ts | ✅ 19 次(与基线一致) |
| 工具名定义数 | grep `name: "..."` 计数 | ✅ 19 次(与基线一致) |
| buildTools 函数体字节比对 | `git diff` hunk 位置分析 | ✅ 4 个 hunk 全在 buildTools 之后(L34 import + L944+/L995+/L1081+ handler body) |
| handler 改动位置 | `git diff src/index.ts \| grep "^@@"` | ✅ L34(+1 import)/ L944(+14 generate_diagram)/ L979(+14 generate_chart)/ L1051(+19 render_svg),全在 handler switch 内 |
| 改动总量 | `git diff --stat src/index.ts` | +65 / -16(全在 import + 3 handler catch 块) |

### 4.2 happy path 字节返回验证(user-journey 实跑)

| 调用 | isError | 返回字段 | 与 P0-2 前一致? |
|---|---|---|---|
| `generate_diagram { code:"a -> b", engine:"d2", format:"svg" }` | undefined(成功) | `{ engine:"d2", format:"svg", local_path:"...diagram_*.svg" }` | ✅ |
| `generate_diagram { code:"digraph G { A -> B -> C }", engine:"graphviz", format:"svg" }` | undefined(成功) | `{ engine:"graphviz", format:"svg", local_path:"..." }` | ✅ |
| `tools/list` | n/a | 19 工具 | ✅ |

**理由**:handler 包装只在 `catch (e)` 块里加 `normalizeEngineError(...) + throw new Error(normalized)`,success 路径(`ok(...)`)字节零变更。顶层 catch 仍按 `err(e.message)` 协议输出(P0-2 前后一致)。

### 4.3 err() helper 签名零变更

`integration I-S-4` 锁死:`function err(message: string) { return { content: [{ type: "text", text: message }], isError: true }; }` 单字段协议不变。

### 4.4 check-schema.mjs G1/G2/G3 三断言仍 pass

```
[G1] create_video schema enum === videoConstraints  ✅ ✅
[G2] 19 工具齐全                                       ✅
[G3] mode/resolution schema enum                       ✅ ✅
=== 5 passed, 0 failed ===
```

### 4.5 向后兼容红线遵守清单

- ✅ `package.json`(版本号 0.11.0 / 依赖 / scripts.test 全不动)
- ✅ `tsconfig.json`(`noUnusedLocals:true` 仍守门)
- ✅ `scripts/check-schema.mjs`(G1/G2/G3 三断言不动,G2 仍守 19 工具)
- ✅ 19 工具的 `name` / `description` / `inputSchema`(P0-1 成果字节级保留)
- ✅ `src/diagram/render.ts`(引擎分发不动)
- ✅ `src/render-svg.ts` Chrome 后端逻辑不动(仅 renderWithResvg 加 [resvg] 前缀)

---

## 5. F1 medium 残留闭环详情(R3 → 测试工程师接手)

### 5.1 F1 残留描述(来自 03-代码审查.md §4.2)

`src/handlers/error-format.ts:410` 兜底分支(未命中任何 pattern 时):

```ts
// 原代码(R3 指出的 medium 残留)
return `[${engine}] ${rawMsg}\n(未识别的错误形态,...原始输入前 80 字符:${String(ctx.input).slice(0, 80)})`;
```

`String(ctx.input)` 对 object 输出字面 `"[object Object]"`,失去诊断价值。触发路径真实可达:`generate_chart handler`(`src/index.ts:1008`)传 `a.spec as Record<string, unknown>`(Vega-Lite spec 对象),Vega-Lite 错误形态多样(3 条 pattern 之外的未识别错误)落到兜底是常态。

### 5.2 修复方案(与 R2 已修的 make 函数 + 主函数 make 调用层完全对称,1 行改动 + 6 行注释)

```ts
// src/handlers/error-format.ts:410(修复后)
const inputPreview = typeof ctx.input === "string"
  ? ctx.input
  : JSON.stringify(ctx.input);
return `[${engine}] ${rawMsg}\n(未识别的错误形态,...原始输入前 80 字符:${inputPreview.slice(0, 80)})`;
```

### 5.3 锁死测试(3 case,在 `test/unit.error-format.mjs`)

```js
test("F1 fix: 兜底分支对 object ctx.input 不输出 '[object Object]',改输出 JSON 片段", () => {
  const spec = { mark: "bar", encoding: { x: { field: "a", type: "quantitativ" } } };
  const out = normalizeEngineError("vega-lite", "some unknown vega error xyz123", { input: spec, raw: "" });
  assert.match(out, /\[vega-lite\]/);
  assert.doesNotMatch(out, /\[object Object\]/);     // 🔒 F1 锁死
  assert.match(out, /"mark"\s*:\s*"bar"/);            // 🔒 改输出 JSON 片段
  assert.match(out, /"quantitativ"/);                  // 🔒 用户拼错的值能从兜底文本里看到
});
```

### 5.4 修复前后行为对比(实地复跑)

```
=== F1 修复前(R3 时点)===
[vega-lite] some unknown vega error xyz123
(未识别的错误形态,...原始输入前 80 字符:[object Object])  ← 失去诊断价值

=== F1 修复后(本测试阶段)===
[vega-lite] some unknown vega error xyz123
(未识别的错误形态,...原始输入前 80 字符:{"mark":"bar","encoding":{"x":{"field":"a","type":"quantitativ"}}})  ← JSON 片段,LLM 可定位
```

### 5.5 修复对 R3 §6 残留矩阵的更新

| Severity | R3 后残留(主控选 B) | 本测试阶段后残留 |
|---|---|---|
| critical | 0 | **0** |
| high | 0 | **0** |
| medium | 1(F1 兜底分支漏修) | **0**(F1 闭环) |
| low | 7(F2-F8 全部分类闭环) | 7(不变,F2-F8 仍是已分类的非阻断项) |

---

## 6. 无法自动验证项(诚实声明)

### 6.1 测试覆盖外的边界(P0-2 范围外,记入 open_points)

| 项 | 原因 | 影响 | 后续 |
|---|---|---|---|
| **viz-js v3 errors 数组格式未来版本稳定性** | 跨版本,本测试只对 v3.28.0 当前格式承诺 | 若未来 viz-js 改 throw 模式,graphviz patterns 表需重测 | 锁 `@viz-js/viz@^3.28.0` caret 不跨 major |
| **D2 JSON 数组 e.message 格式稳定性** | `@terrastruct/d2@0.1.33` 当前 dump 成 `[{range, errmsg}]`;未来 WASM 版本升级可能换格式 | extractD2Errmsgs 已 try/catch 兜底回退 [rawMsg];rx 都基于 errmsg 字段,格式变会失配 | 渐近积累,按未命中率(`__didMatchLastKnownPattern=false`)补表 |
| **resvg 错误形态完整枚举** | 调查只抓了 3 类(SVG parsing / Font / 误报 root node),`@napi-rs/resvg-js` 对 SVG-in-SVG / foreignObject / CSS filter 等场景的错误覆盖未深入 | resvg 首版 2 条 pattern 是最小集 | 渐近积累 |
| **PDF 管线错误风格** | `src/pdf/*` 异步 job 失败错误未探(P0-2 范围声明) | 不影响 P0-2 验收 | 后续独立 P 项统一 |
| **D2 图标解析失败路径** | `src/diagram/d2.ts:20-50 resolveD2Icons` 失败当前是静默移除 `<image>`,不抛错(P0-2 不改此行为) | 用户若想看到碎图告警不可得 | open_points 记录 |
| **offending 片段截断长度 120/80** | 首版简单 slice(0, N) 不加省略号标记,若 D2 单行极长(如内联 style 块)可能截到中半 | 信息不完整但仍有定位价值 | 后续可加 `...` 前后缀 |

### 6.2 用户旅程测试的性能边界

| 引擎 | 单次 spawn 耗时(含 WASM 加载) | 备注 |
|---|---|---|
| Graphviz | ~1s | WASM 一次性加载,viz-js v3 |
| Vega-Lite | ~1s(错) / ~1s(happy) | vega 编译较快 |
| resvg | ~1-8s(取决于 SVG 复杂度) | napi-rs native,启动快 |
| **D2** | **~2.5s(错)+ ~2.5s(happy)** | **22MB WASM + worker 启动是瓶颈** |

7 case 串行总耗时 ~18s。若接入 CI(P0-3 接入 `npm test` 后)需考虑 D2 WASM 加载成本,可改并行(`node --test --test-concurrency=N`)。

### 6.3 MCP stdio 协议交互的简化

本测试的 user-journey 用了**简化的 MCP 协议**(initialize + notifications/initialized + tools/call 三条消息),省略了:
- `notifications/progress`(对 tools/call 不是必需)
- 错误重试 / 心跳(测试用一次性请求,无需)

对 P0-2 验收而言这已足够(实测能拿到 isError + content[0].text 真实响应)。完整 MCP 协议测试是 P0-3 引入正式 test 基建后的范畴。

---

## 7. 测试执行结果(实地复跑原始输出节选)

### 7.1 4 道既有闸门复跑(对齐 03-代码审查 §0.2)

```
=== build (npm run build) ===
> tsc
(零输出,exit 0)

=== npm test (check-schema.mjs) ===
默认 video provider = agnes → allowedNumFrames [81,121,161,241,441] / allowedFrameRates [24]
[G1] create_video schema enum === videoConstraints  ✅ ✅
[G2] 19 工具齐全                                       ✅
[G3] mode/resolution schema enum                       ✅ ✅
=== 5 passed, 0 failed ===

=== smoke (node --test test/error-format.smoke.mjs) ===
ℹ tests 27    ℹ pass 27    ℹ fail 0
ℹ duration_ms 520.541862

=== grep (node scripts/check-error-text.mjs) ===
=== 11 passed, 0 failed ===
```

### 7.2 3 道新增闸门(本测试阶段交付)

```
=== unit (node --test test/unit.error-format.mjs) ===
ℹ tests 30    ℹ pass 30    ℹ fail 0
ℹ duration_ms 131.086543

=== integration (node --test test/integration.error-format.mjs) ===
ℹ tests 14    ℹ pass 14    ℹ fail 0
ℹ duration_ms 194.423295

=== user-journey (node --test test/user-journey.error-format.mjs) ===
✔ user journey Graphviz: 非法 DOT → 收到归一化中文消息 (1063ms)
✔ user journey Vega-Lite: type:'quantitativ' 拼写错 → 收到 4 个合法值提示 (1053ms)
✔ user journey render_svg: 缺 xmlns 的 SVG → 收到 xmlns 修复动词 (8362ms)
✔ user journey D2: 用户调 generate_diagram 喂浮点 stroke-width → 收到 LLM 友好错误 (2463ms)
✔ user journey happy D2: 合法 a -> b → 成功返回 local_path (2662ms)
✔ user journey happy Graphviz: 合法 DOT → 成功返回 svg (1057ms)
✔ user journey 工具枚举: tools/list 仍返回 19 个工具 (1056ms)
ℹ tests 7    ℹ pass 7    ℹ fail 0
ℹ duration_ms 17818
```

### 7.3 F1 修复前后行为对比(REPL 实跑)

```
=== F1 修复前(代码审查时点,REPL 复现)===
[vega-lite] some unknown vega error xyz123
(未识别的错误形态,...原始输入前 80 字符:[object Object])
含 [object Object]?  true
含对象片段(mark)?  false

=== F1 修复后(本测试阶段,REPL 复现)===
[vega-lite] some unknown vega error xyz123
(未识别的错误形态,...原始输入前 80 字符:{"mark":"bar","encoding":{"x":{"field":"a","type":"quantitativ"}}})
含 [object Object]?  false
含对象片段(mark)?  true
```

---

## 8. 与下游阶段的关系

### 8.1 与 P0-3(node:test 基建)的衔接

- 本测试新增 3 个文件(`test/unit.error-format.mjs` / `test/integration.error-format.mjs` / `test/user-journey.error-format.mjs`)与既有 `test/error-format.smoke.mjs` 一起,**共 4 个测试文件 78 case**。
- 全部用 `node --test` 单文件运行,**不接入 `npm test`**(保 P0-3 立场)。
- P0-3 引入 test/ 目录结构 + tsconfig.test.json + CI stale gate + `npm test` 接入后,4 个测试文件自动接入(P0-3 只需在 test 脚本追加 `&& node --test test/`,无需改本 4 个文件)。
- **F1 修复消除了 P0-3 的一个待办**(R3 §5.3 转交清单里 P0-3 的 F1 项已闭环)。

### 8.2 与 P0-4(产物守门人)的正交

- P0-2 管错误消息文本质量(失败路径);P0-4 管成功产物质量(成功路径)。两者正交。
- 本测试新增的 user-journey happy path case(UJ-5/UJ-6)只验证 isError 不为 true + 返回字段,不验证产物质量(产物质量由 P0-4 加钩子)。

### 8.3 与 P0-5(第 20 工具)的正交

- 本测试 UJ-7 锁死 19 工具枚举不变。
- P0-5 加第 20 工具时 check-schema.mjs G2 计数 19→20 是唯一允许的例外改动,P0-5 须同步更新 UJ-7 的断言(`tools.length === 19` → `tools.length === 20`)。

---

## 9. 总评

### 9.1 P0-2 测试交付物完整度

| 维度 | 交付 | 状态 |
|---|---|---|
| **单元测试** | 30 case(覆盖 14 pattern 边界 + helper 白盒 + F1 锁死 + engineHint + 反崩溃 + 元契约) | ✅ |
| **集成测试** | 14 case(handler 仿真 + 模块级 e2e + 契约对称性) | ✅ |
| **集成冒烟测试(完整用户旅程)** | 7 case(MCP stdio JSON-RPC 端到端) | ✅ |
| **用户测试(用户视角旅程)** | 同上 7 case,以用户口吻真实 spawn MCP server 发 tools/call | ✅ |
| **向后兼容验证** | 字节级(19 工具 inputSchema + happy path + err() 协议 + check-schema.mjs) | ✅ |
| **F1 medium 残留闭环** | 1 行修复 + 3 条锁死测试 | ✅ |
| **无法自动验证项诚实声明** | 6 项 + 性能边界 + 协议简化 | ✅ |

### 9.2 PRD 对齐度

PRD §1.2 五条成功标准 + 反崩溃硬契约 **全部达标**(详见 §3 检查表)。F1 修复后,§1.2.2 offending 片段回显的兜底场景软指标也达标。

### 9.3 测试工程师立场

- **真实跑过**:全部 7 道闸门 + 78 case 实地复跑 green(输出节选见 §7)。
- **不绿就修**:R3 登记的 F1 medium 残留经测试阶段闭环(1 行修复 + 3 条锁死测试)。
- **诚实标注**:6 项无法自动验证项已在 §6 声明,均不阻断 P0-2 验收。
- **向后兼容**:19 工具 inputSchema 字节级与 P0-1 基线一致(`git diff` 4 个 hunk 全在 handler catch 块内);happy path 返回字段零变更。

**test_ok = true**。P0-2 可进入 commit / push 流程。

---

## 附录 A · 测试文件清单

| 文件(绝对路径) | 类型 | case 数 | 内容 |
|---|---|---|---|
| `/Users/wangdong/Documents/Project/Agnes AI接入/media-gen-mcp/test/error-format.smoke.mjs` | 既有(实施者交付) | 27 | 14 pattern 正样本 + 反崩溃 + 元契约 + Graphviz bug 锁死 + chart [resvg] 透传 |
| `/Users/wangdong/Documents/Project/Agnes AI接入/media-gen-mcp/test/unit.error-format.mjs` | **新建(本阶段)** | 30 | pattern 边界 + helper 白盒 + F1 修复锁死 + engineHint + 元契约 |
| `/Users/wangdong/Documents/Project/Agnes AI接入/media-gen-mcp/test/integration.error-format.mjs` | **新建(本阶段)** | 14 | handler 包装仿真 + 模块级 e2e + 契约对称性 + 顶层 err 协议 |
| `/Users/wangdong/Documents/Project/Agnes AI接入/media-gen-mcp/test/user-journey.error-format.mjs` | **新建(本阶段)** | 7 | MCP stdio JSON-RPC 完整旅程(用户视角) |
| `/Users/wangdong/Documents/Project/Agnes AI接入/media-gen-mcp/src/handlers/error-format.ts` | **修改(本阶段)** | - | F1 修复:兜底分支 line 410 加 `typeof ctx.input === "string"` check |

**总计**:4 测试文件 78 case + 1 源码 1 行修复。

## 附录 B · 给主控的 commit 建议(测试阶段视角)

如主控确认本测试阶段验收通过,建议在 P0-2 单 commit 中追加:

```
test(P0-2): 三层测试 + F1 medium 残留闭环

- 新增 test/unit.error-format.mjs(30 case):14 pattern 边界 + helper 白盒 +
  F1 修复锁死(兜底分支 object ctx.input 输出 JSON 片段而非 [object Object])+
  engineHint 路由边界 + 反崩溃 regex 元契约
- 新增 test/integration.error-format.mjs(14 case):handler 层 try/catch 仿真
  + 模块级 GraphvizEngine happy/error + 4 处 PNG 复用路径对称性 +
  顶层 err() 协议锁死
- 新增 test/user-journey.error-format.mjs(7 case):经真实 MCP stdio JSON-RPC
  spawn node dist/index.js,以用户视角验证 4 错输入 + 2 happy path + tools/list
  19 工具枚举
- 修复 src/handlers/error-format.ts:410(R3 F1 medium 残留):兜底分支对
  object ctx.input(Vega-Lite spec 对象)输出 [object Object] 失诊断价值,
  改 JSON.stringify(与 R2 已修的 make 函数 + 主函数 make 调用层对称,1 行修复)
- 三层测试零重叠:smoke 广度正样本 / unit 边界白盒 / integration 模块接通 /
  user-journey MCP stdio e2e
- 全部 7 道闸门 green:build / npm test / smoke 27 / grep 11 / unit 30 /
  integration 14 / user-journey 7(合计 78 case)
- 向后兼容:19 工具 inputSchema 字节级与 P0-1 基线一致(git diff 4 hunk 全在
  handler catch 块);happy path 字节返回;check-schema.mjs G2 仍守 19 工具

Co-Authored-By: Claude <noreply@anthropic.com>
```

(具体 commit 由主控决定,本测试工程师仅给建议。)
