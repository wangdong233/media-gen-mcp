# P0-2 代码审查 · LLM 友好错误契约(handler 层引擎 stderr 归一化)

> **阶段**: P0-2(代码审查 / 第 3 轮独立复审)
> **角色**: 代码审查员(独立于实施者,亦独立于前 2 轮审查)
> **审查日期**: 2026-07-21
> **审查依据**: PRD(`doc_v11/P0-2-LLM友好错误契约实施规划.md`)+ `01-功能分析.md`(plan_ok=true)+ `02-实施记录.md`(impl_ok=true)
> **审查范围**: P0-2 全部代码改动面 —— 5 个修改源文件 + 2 个新建文件 + 1 个 grep 防御脚本
> **审查结论**: **review_ok = true · GO-WITH-CONDITIONS**(0 critical / 0 high / **1 medium 残留**[建议 P0-2 内修,1 行改动] / 7 low 全部分类闭环)

---

## 0. 结论速览

### 0.1 数字一览

- **本次审查(Round 3)发现**:8 条 —— **0 critical / 0 high / 1 medium / 6 low / 1 正面观察**
- **历史审查(Round 1+2)成果(从代码注释 + smoke test 反推)**:Round 1 主审发现 ≥4 条真实 bug,Round 2 闭环修复 + 新增 3 条锁死测试
- **P0-2 范围内代码修复建议**:**1 处**(F1 medium,1 行 typeof check —— 兜底分支同步 R2 已修的同源问题)
- **可选改进(不阻断)**:6 条 low(契约不对称债 / 文档不一致 / 冗余 / 死分支 / 启发式简化版 / 注释描述错误)
- **最终残留**:0 critical / 0 high / **1 medium(F1,建议 P0-2 内修**)/ 7 low(全部已分类闭环)
- **复审结论**:**GO-WITH-CONDITIONS** —— 4 闸门(build / npm test / smoke / grep)实地复跑全绿;唯一 medium 是 R2 修复不完全(同源问题漏修),修复成本 1 行,建议在 commit 前由实施者补上;若主控接受"留作 P0-3 候选",也不构成 P0-2 阻断(因不违 PRD 反崩溃硬契约,仅诊断质量降级)。

### 0.2 硬指标实地复跑

| 闸门 | 命令 | 本审查复跑结果 | 02-实施记录 §0 自证 | 一致? |
|---|---|---|---|---|
| TypeScript build | `npm run build` | ✅ 0 error(`tsc` `noUnusedLocals:true` 严) | ✅ | ✅ |
| Schema 防御 | `npm test`(check-schema.mjs) | ✅ 5 passed, 0 failed(G2 仍守 19 工具) | ✅ | ✅ |
| 冒烟测试 | `node --test test/error-format.smoke.mjs` | ✅ **27 passed**, 0 failed | ⚠️ 写 "24 passed" | ❌ **数字滞后**(详见 F4) |
| 跨引擎回退防御 | `node scripts/check-error-text.mjs` | ✅ 11 passed, 0 failed | ✅ | ✅ |

**结论**:4 闸门全绿;唯一与实施记录不一致的是 smoke test 用例数(实际 27 vs 文档 24)—— 第 2 轮审查新增 3 条 case 后未回填实施记录,不影响代码正确性。

### 0.3 改动面实地核对(git status + git diff --stat)

| 文件 | 实地 git status | 02-实施记录 §1.1 自证 | 一致? |
|---|---|---|---|
| `src/diagram/graphviz.ts` | M(+27 / -10) | M | ✅ |
| `src/diagram/d2.ts` | M(+28 / -23) | M | ✅ |
| `src/chart.ts` | M(+8 / -2 实测 +13 / -5) | M | ⚠️ 行数略有出入 |
| `src/index.ts` | M(+62 / -15 实测 +71 / -10) | M | ⚠️ 行数略有出入 |
| **`src/render-svg.ts`** | **M(+10 / -1)** | **❌ 未列** | **❌ 漏列**(详见 F4) |
| `src/handlers/error-format.ts`(新建) | ?? | + | ✅ |
| `test/error-format.smoke.mjs`(新建) | ?? | + | ✅ |
| `scripts/check-error-text.mjs`(新建) | ?? | + | ✅ |

**结论**:`src/render-svg.ts` 也被修改(第 2 轮审查加 [resvg] 前缀对称包装),但 02-实施记录 §1.1 漏列;`src/chart.ts` / `src/index.ts` 行数略有出入(实施记录 §1.1 写 +8/-2 / +62/-15,实测 +13/-5 / +71/-10,差异源于 §1.1 数字是部分节点而非整文件 diff)。**均不影响代码正确性**,仅文档滞后。

---

## 1. 审查设置

### 1.1 审查目标

作为**独立于实施者、亦独立于前 2 轮审查的第三方**,对 P0-2 的全部代码改动做第 3 轮复审,回答四问:

1. **R1+R2 已修的是否真修彻底**(同源问题有无漏网)?
2. **R1+R2 是否引入新问题**(修复代码本身的回归)?
3. **4 闸门全绿是否真实**(不仅看 02-实施记录 §3 自证,要实地复跑)?
4. **是否有 R1+R2 视野之外的新发现**?

### 1.2 审查范围

| 范围 | 文件 / 对象 | 触点 |
|---|---|---|
| **主审(新建)** | `src/handlers/error-format.ts`(417 行,14 patterns + 6 helper + 主函数 + 类型) | 全文 |
| **主审(修改)** | `src/diagram/graphviz.ts`(97 行)、`src/diagram/d2.ts`(177 行)、`src/chart.ts`(132 行)、`src/index.ts`(handler 3 处:939-973 / 992-1015 / 1080-1112)、`src/render-svg.ts`(renderWithResvg 加 [resvg] 前缀) | 全文 |
| **主审(新建测试)** | `test/error-format.smoke.mjs`(400 行,27 case) | 全文 |
| **主审(新建脚本)** | `scripts/check-error-text.mjs`(86 行) | 全文 |
| **伴生核校(不动)** | `src/index.ts:1365-1368` 顶层 catch + `err()` helper(line 1418)、19 工具 `name` / `description` / `inputSchema` | 仅核实 P0-2 未越界触碰 |
| **对照基准** | `doc_v11/P0-2-LLM友好错误契约实施规划.md`(PRD,1073 行)+ `01-功能分析.md` + `02-实施记录.md` | -- |

### 1.3 审查维度(5 个,与 P0-1 审查对齐)

| 维度 | 关注点 | 依据 |
|---|---|---|
| **代码规范** | 格式一致性、跨文件对称性、dead code、注释描述与代码一致性 | PRD §4 / 实施记录 §1 |
| **数据逻辑** | 正则覆盖与边界、对象 vs 字符串 ctx.input 一视同仁、兜底不崩溃、pattern 表与样本一一对应 | PRD §1.2 / §3.2 / §4.2 |
| **业务逻辑(PRD 对齐)** | PRD §1.2 五条成功标准达标、契约三件套齐、反崩溃硬契约(/TypeError/Cannot read/is not a function/ 禁穿透)、向后兼容零感知 | PRD §1.2 / §3.2 |
| **简单架构审查** | 模块边界清晰、错误归一化入口单一、HINT 知识无跨层泄漏、PNG 复用路径对称 | PRD §4.3 / memory(`media-gen-mcp-project`) |
| **正面观察** | 测试技巧亮点、修复彻底度亮点、模式可复用 | -- |

### 1.4 审查方法

- **静态阅读**:Read 全部 8 个文件(error-format.ts / graphviz.ts / d2.ts / chart.ts / index.ts handler 3 处 / render-svg.ts diff / smoke.mjs / check-error-text.mjs)
- **实地复跑 4 闸门**:`npm run build` + `npm test` + `node --test test/error-format.smoke.mjs` + `node scripts/check-error-text.mjs`
- **git diff 核对**:`git status` + `git diff --stat` + `git diff src/render-svg.ts`(发现实施记录漏列文件)
- **运行时验证**:Node REPL 直接调 `dist/handlers/error-format.js` 验证 F1 假设(兜底分支对 object ctx.input 输出 `[object Object]`)
- **PRD 反查**:每条疑似 finding 上溯至 PRD 原文段落,判定"实现偏差" vs "PRD 文案可商榷" vs "R1+R2 修复不完全"
- **未依赖**:WebSearch(配额耗尽,且本审查范围纯属本地静态核校 + 实地复跑,无外部依赖)

---

## 2. 审查轮次总览(3 轮,本次为第 3 轮)

P0-2 的代码注释 + smoke test 反映审查实际经历了 **3 轮**(前 2 轮由实施者内嵌的审查动作完成,本轮为独立第三方复审):

| 轮次 | 类型 | 输入 | 输出 | 证据来源 |
|---|---|---|---|---|
| **Round 1** | 多维度并行主审(实施阶段内嵌) | PRD + 实施中实测的 4 类引擎 stderr | ≥4 条真实 bug(pattern 1 `\w+` 漏 -、pickD2Line 0-indexed vs 1-indexed、resvg pattern 2 `[^']` 漏匹配、normalizeEngineError ctx.raw 覆盖矛盾)+ 7 条 PRD 分析文档订正(open_point #14 等) | `02-实施记录.md §3` 自述 + `01-功能分析.md §9` |
| **Round 2** | 修复闭环 + 新增锁死测试(实施阶段内嵌) | R1 全部 finding | 4 条代码修复(\w→[\w-]、errmsg 1-indexed 优先、[^']→.{0,15}、ctx.raw 不覆盖)+ **2 处新发现的反崩溃修复** + 3 条 smoke test 锁死 | `src/handlers/error-format.ts:315-323` / `:395-404` 注释 "P0-2 第 2 轮审查修复" + `test/error-format.smoke.mjs:203-236` / `:330-399` 三条新 case |
| **Round 3(本次)** | 独立第三方复审 | R1+R2 全部成果 + P0-2 全部代码改动面 | 8 条新 finding(0C / 0H / **1M** / 6L / 1 正面) | 本文档 |

### 2.1 Round 2 修复的两处反崩溃缺陷(代码注释明示)

实施阶段的 Round 2 在 smoke test 覆盖前发现了两处反崩溃契约违例并修复:

| # | 位置 | 历史 bug | R2 修复 | smoke test 锁死 |
|---|---|---|---|---|
| **R2-1** | `src/handlers/error-format.ts:315-323`(resvg pattern 1 make 函数) | chart handler 传 Vega-Lite spec 对象作 ctx.input(`src/index.ts:1008`),`(ctx.input as string).slice` 抛 `TypeError: ctx.input.slice is not a function`,穿透顶层 catch,违 PRD §1.2.4 + §3.2 反崩溃硬契约 | 加 `typeof ctx.input === "string" ? ctx.input : JSON.stringify(ctx.input)` 双分支 | `test/error-format.smoke.mjs:203-219`(`resvg pattern 1: object ctx.input does not crash`)+ `:224-236`(circular ref anti-crash) |
| **R2-2** | `src/handlers/error-format.ts:395-404`(normalizeEngineError 主函数 make 调用处) | p.make 内部任何未预见异常(如 circular ref 让 JSON.stringify 抛 "Converting circular structure to JSON")穿透 | 主函数加 try/catch,make 抛错时回退到 `已识别错误形态但归一化失败` 兜底文本 | 同 R2-1 第二条 case |

**R2 修复质量评估**:R2-1 修了**单点 make 函数**;R2-2 修了**主函数 make 调用层**。两层防护都到位,反崩溃契约 /TypeError|Cannot read|is not a function/ 禁穿透在 pattern 命中分支已彻底守住。

**但 R2 的视野盲区在兜底分支**(详见 §4.2 F1)—— 兜底分支(line 410)同样有 `String(ctx.input).slice(0, 80)`,对 object ctx.input 不崩但输出 `[object Object]`,失去诊断价值。这是 R2 修复不完全的同源漏修。

### 2.2 Round 2 新增的 3 条锁死测试

| # | 测试名 | 锁死的契约 |
|---|---|---|
| **R2-T1** | `resvg pattern 1: object ctx.input (chart handler PNG reuse path) does not crash`(行 203-219) | R2-1 修复:对象 ctx.input 不崩,仍输出契约三件套 |
| **R2-T2** | `anti-crash: p.make throwing (circular JSON.stringify) is caught, returns fallback`(行 224-236) | R2-2 修复:circular ref 不穿透,返回兜底文本 |
| **R2-T3** | `regression: chart PNG resvg errors keep [resvg] prefix (not wrapped by outer catch)`(行 341-399) | chart.ts PNG 复用路径移出 outer try 的修复 —— 用 `node:module register()` 装 loader mock @resvg/resvg-js 验证 [resvg] 前缀不被 "Vega-Lite render error:" 二次包装 |

**R2-T3 是高级测试技巧** —— ESM 命名空间 immutable 无法 monkey-patch,用 `resolve+load` 双 hook 把 `@resvg/resvg-js` 重定向到 `virtual:mock-resvg`(必改 specifier 解析路径,不能只改 load,否则对已缓存 URL 不生效)。详见 §5 F8 正面观察。

---

## 3. Round 3 · 多维度复审

### 3.1 按 severity × dimension 矩阵(本次新发现 8 条)

| Severity \ Dimension | 代码规范 | 数据逻辑 | 业务逻辑(PRD 对齐) | 简单架构审查 | 正面观察 | **合计** |
|---|---|---|---|---|---|---|
| critical | 0 | 0 | 0 | 0 | 0 | **0** |
| high | 0 | 0 | 0 | 0 | 0 | **0** |
| medium | 0 | **1(F1)** | 0 | 0 | 0 | **1** |
| low | 2(F3, F4) | 2(F5, F6) | 1(F2) | 1(F7) | 1(F8) | **7** |
| **合计** | **2** | **3** | **1** | **1** | **1** | **8** |

### 3.2 按 file:line 热点分布

| 位置 | 模块 | 命中数 | finding ID |
|---|---|---|---|
| `src/handlers/error-format.ts:410` | normalizeEngineError 兜底分支 | 1 | **F1(medium)** |
| `src/handlers/error-format.ts:280` | Vega-Lite pattern 1 make | 1 | F6(死分支) |
| `src/handlers/error-format.ts:108` | pickUnbalancedBrace | 1 | F7(简化版) |
| `src/chart.ts:97-102, 113-115` | chart.ts 外层 catch vh HINT | 1 | F2(契约不对称) |
| `src/index.ts:1103-1104` | render_svg handler 内容 rx 兜底 | 1 | F5(冗余) |
| `scripts/check-error-text.mjs:37-38` | 注释描述 | 1 | F4(注释错误) |
| `02-实施记录.md §0 / §1.1` | 文档数字 | 1 | F3(文档滞后) |
| `test/error-format.smoke.mjs:341-399` | R2-T3 测试技巧 | 1 | F8(正面观察) |

**热点观察**:
- **error-format.ts 命中 3 条(F1/F6/F7)**,集中在 helper 与兜底分支。F1 是 R2 修复盲区(主战场),F6/F7 是代码风格层面的小瑕疵,不阻断。
- **chart.ts + index.ts handler 命中 2 条(F2/F5)**,都是契约不对称的延伸 —— chart.ts 外层 catch 仍拼 HINT(被 normalizeEngineError 的 remediation 覆盖,功能正确但形成死代码),render_svg handler 多了一层内容 rx 兜底(因 render-svg.ts 已加 [resvg] 前缀而冗余)。
- **去重后实际独立物理问题 = 7 个**(兜底分支漏修 + 文档滞后 + 注释描述错 + 契约不对称 + 冗余 rx + 死分支 + 简化启发式 + 1 正面观察)。

### 3.3 完整 finding 清单(R3 输出,按 severity 降序、同 severity 按 file:line 升序)

| ID | sev | dim | file:line | 摘要 | R3 处置 |
|---|---|---|---|---|---|
| **F1** | **medium** | 数据逻辑 | `error-format.ts:410` | normalizeEngineError 兜底分支 `String(ctx.input).slice(0, 80)`,对 Vega-Lite object ctx.input 输出 `[object Object]`,失去诊断价值。R2 已修同源问题(resvg pattern 1 make + 主函数 make 调用),兜底分支是漏修。 | **建议 P0-2 内修**(1 行 typeof check),或主控接受转 P0-3 |
| F2 | low | 业务逻辑 | `chart.ts:97-102, 113-115` | chart.ts 外层 catch 仍拼 `vh HINT`(`gradient 写法 / condition.test 用 datum`),与 P0-2 把 HINT 全量迁入 knownErrorPatterns 的方向不一致;vh 进了 raw 但被 normalizeEngineError 的 remediation 覆盖,功能正确,但代码成死代码 | 不改(P0-2 §5.5 已识别契约不对称,实施者选保守路径) |
| F3 | low | 代码规范 | `02-实施记录.md §0 / §1.1` | 实施记录 §1.1 漏列 `src/render-svg.ts`(实测 M,+10/-1);§0 表 "smoke 24 passed" 实测 27 passed | 改实施记录(本审查文档已 flag) |
| F4 | low | 代码规范 | `scripts/check-error-text.mjs:37-38` | 注释 "'SVG data parsing' 在 src/render-svg.ts 的兜底 rx 里出现是允许的" 描述错误 —— render-svg.ts 没有 rx,实际在 `src/index.ts:1103-1104` | 改注释(1 行) |
| F5 | low | 数据逻辑 | `index.ts:1103-1104` | render_svg handler 的内容 rx 兜底(`/SVG data parsing failed|default font-family.../`)与 `[resvg]` 前缀检测冗余 —— render-svg.ts:208 已加前缀,前缀检测 100% 可靠,内容 rx 永不触发 | 不改(作为纵深防御保留,注释已说明"兜底") |
| F6 | low | 数据逻辑 | `error-format.ts:280` | Vega-Lite pattern 1 的 `m[1] === "undefined" ? "<缩写或空>" : m[1]` 是死分支 —— vega-lite 实际抛的 m[1] 永远是字面 "undefined"(内部已标准化),else 分支永不走 | 不改(代码可读,显式表达"若是 undefined 字面,给更友好提示"的意图) |
| F7 | low | 简单架构 | `error-format.ts:97-109` | pickUnbalancedBrace 简化版:① 字符串/注释里的 `{` `}` 也计数;② depth 变负后永远不回 0;③ suspectLine 是"最后一个含 `{` 的行",可能不是真正出错行 | 不改(注释已标"简化版";D2 map 未闭合的常见场景有效;P1-1 DSL lint 层负责) |
| F8 | low | 正面观察 | `smoke.mjs:341-399` | R2-T3 用 `node:module register()` 装 resolve+load 双 hook mock @resvg/resvg-js,把 `@resvg/resvg-js` 重定向到 `virtual:mock-resvg` —— 高级 ESM 测试技巧,锁死 chart.ts PNG 复用路径 [resvg] 前缀不被 outer catch 二次包装 | 无需动作(记入 media-gen-mcp 测试模式库) |

---

## 4. Round 3 · 每维度详查

### 4.1 代码规范(2 条:F3 / F4)

#### F3 [low] · 实施记录数字与文件清单滞后

- **现象**:
  - `02-实施记录.md §0` 表格 "smoke ✅ 24 passed" —— 本审查实地复跑 `node --test test/error-format.smoke.mjs` 得 **27 passed**。差异 3 条 = R2 新增的 R2-T1 / R2-T2 / R2-T3(见 §2.2)。
  - `02-实施记录.md §1.1` 表格列 "4 个源码文件"(graphviz.ts / d2.ts / chart.ts / index.ts)—— 本审查 `git status` 得 **5 个源码文件**,漏列 `src/render-svg.ts`(M,+10/-1,第 2 轮审查加 [resvg] 前缀对称包装)。
  - `02-实施记录.md §1.1` 行数变化(+27/-10 等)与 `git diff --stat` 略有出入(chart.ts 实测 +13/-5 vs 文档 +8/-2;index.ts 实测 +71/-10 vs 文档 +62/-15)。
- **根因**:R2 在实施记录定稿后追加修复 + 新增测试,未回填文档数字。
- **影响**:文档与代码不一致,后续阶段(尤其 P0-3 引入 test 基建时)若以实施记录数字为准会低估覆盖面。
- **R3 处置**:**改实施记录**(本审查文档已 flag;实施者可在 commit 前同步,或主控接受"文档滞后不阻断 P0-2")。
- **是否阻断 P0-2**:**否**(代码行为正确,仅文档数字滞后)。

#### F4 [low] · check-error-text.mjs 注释描述错误

- **现象**:`scripts/check-error-text.mjs:37-38` 注释:"'SVG data parsing' 在 src/render-svg.ts 的兜底 rx 里出现是允许的(handler 层兜底识别,不是跨引擎回退触发器),但本脚本不检 render-svg.ts。"
- **实地核对**:
  - `src/render-svg.ts` 全文 grep `SVG data parsing` —— **零命中**(render-svg.ts 只在 `renderWithResvg` 内 try/catch 加 `[resvg]` 前缀,无任何内容 rx)。
  - `src/index.ts:1103-1104` render_svg handler 才有内容 rx:`/SVG data parsing failed|default font-family.{0,15}not found|.../i.test(msg)`。
- **根因**:注释作者把 `src/render-svg.ts` 与 `src/index.ts` 的 render_svg handler 混淆。
- **影响**:维护者读注释会去错误的位置找"允许的 SVG data parsing",增加理解成本。
- **R3 处置**:**改注释**(1 行,把 "src/render-svg.ts" 改为 "src/index.ts render_svg handler")。
- **是否阻断 P0-2**:**否**。

### 4.2 数据逻辑(3 条:F1 / F5 / F6)

#### F1 [medium] · normalizeEngineError 兜底分支对 object ctx.input 输出 [object Object]

- **现象**:`src/handlers/error-format.ts:410` 兜底分支(未命中任何 pattern 时):
  ```ts
  return `[${engine}] ${rawMsg}\n(未识别的错误形态,请把此消息反馈给 media-gen-mcp 维护者补 knownErrorPatterns 表。原始输入前 80 字符:${String(ctx.input).slice(0, 80)})`;
  ```
  `String(ctx.input)` 对 object 输出字面 `"[object Object]"`,`slice(0, 80)` 仍是 `"[object Object]"`。
- **本审查实地复跑(Node REPL)**:
  ```
  === F1 vega-lite object ctx.input, unknown err ===
  [vega-lite] some unknown vega error xyz123
  (未识别的错误形态,请把此消息反馈给 media-gen-mcp 维护者补 knownErrorPatterns 表。原始输入前 80 字符:[object Object])
  ```
  对比同函数的 R2 已修分支(resvg pattern 1 make,行 320-323):
  ```
  === control: resvg pattern 1 object ctx.input (make fixed R2) ===
  [resvg] resvg 解析 SVG 失败。... | offending: {"mark":"bar"} | 修复:...
  ```
  R2 修了 make 内的同类问题,兜底分支未同步。
- **触发路径(真实可达,非理论)**:
  - `src/index.ts:1008` generate_chart handler 传 `{ input: a.spec as Record<string, unknown>, raw: msg }` —— **ctx.input 是 Vega-Lite spec 对象**。
  - Vega-Lite 错误形态多样(3 条 pattern 之外的未识别错误),落到兜底分支是常态。
  - 兜底输出 `原始输入前 80 字符:[object Object]` —— LLM 看到这串零信息字符串,失去定位能力。
- **与 PRD 契约的关系**:
  - **不违反** PRD §1.2.4 + §3.2 反崩溃硬契约(不抛 TypeError,函数返回字符串)。
  - **部分违反** PRD §1.2.2 "offending 片段回显"契约 —— 该条要求"D2/Graphviz 回显出错行原文,Vega-Lite 指出 channel 字段路径,resvg 回显 SVG 前 80 字符",兜底分支不在该条明示范围内,但"offending 回显"的精神被破坏。
  - **诊断质量降级到零** —— 这是 LLM 友好错误契约的核心价值所在,兜底场景虽然概率不高,但一旦触发对 LLM 完全不可读。
- **根因**:R2 修复视野集中在 pattern 命中分支(make 函数 + make 调用层),漏掉兜底分支。同源问题的修复不完全。
- **R3 处置**:**建议 P0-2 内修**(1 行改动,与 R2 已修的 make 函数对称):
  ```ts
  // 修复建议(error-format.ts:410)
  const inputPreview = typeof ctx.input === "string"
    ? ctx.input
    : JSON.stringify(ctx.input);
  return `[${engine}] ${rawMsg}\n(未识别的错误形态,请把此消息反馈给 media-gen-mcp 维护者补 knownErrorPatterns 表。原始输入前 80 字符:${inputPreview.slice(0, 80)})`;
  ```
  - 同步加 1 条 smoke test 锁死(vega-lite + object ctx.input + 未识别错误 → 兜底文本含 `"mark"` 等对象片段,且不含 `[object Object]`)。
- **是否阻断 P0-2**:**不构成硬阻断**(不违 PRD §1.2.4 / §3.2 反崩溃硬契约),但**强烈建议 P0-2 内修**(1 行 + 1 test case,与 R2 修复彻底对称,避免留债给 P0-3)。
- **主控可选**:
  - 选项 A(推荐):实施者补 1 行修复 + 1 条 smoke test,P0-2 内闭环。
  - 选项 B:接受转 P0-3(契约三件套测试覆盖时一并补),不阻断 P0-2 commit。

#### F5 [low] · render_svg handler 内容 rx 兜底冗余

- **现象**:`src/index.ts:1103-1104` render_svg handler:
  ```ts
  const isResvgErr = /^\[resvg\] /i.test(msg)
    || /SVG data parsing failed|default font-family.{0,15}not found|No match for.{0,30}font-family|Failed to load a font face|malformed font|font doesn't have a family name/i.test(msg);
  ```
  两条检测:① `[resvg]` 前缀(来自 `render-svg.ts:208`);② 内容 rx。
- **实地核对**:`src/render-svg.ts:200-209` `renderWithResvg` 已包 try/catch 加 `[resvg]` 前缀,所有 resvg 错误都带前缀。内容 rx 永不触发(前缀检测 100% 命中)。
- **根因**:R2 加 `[resvg]` 前缀到 render-svg.ts 后,内容 rx 成冗余。注释说"兜底用内容 rx"是历史遗留。
- **影响**:维护成本(内容 rx 若过时无人察觉);但作为纵深防御保留也合理(render-svg.ts 的前缀若被误删,内容 rx 兜底)。
- **R3 处置**:**不改**(作为纵深防御保留;注释已说明"兜底")。

#### F6 [low] · Vega-Lite pattern 1 死分支

- **现象**:`src/handlers/error-format.ts:280` Vega-Lite pattern 1 make:
  ```ts
  offendingConstruct: `encoding.<channel>.type="${m[1] === "undefined" ? "<缩写或空>" : m[1]}"`,
  message: `encoding 通道的 type 值 "${m[1]}" 不合法(${m[1] === "undefined" ? "通常是拼错或用了缩写,被静默转 undefined" : "不在合法集合内"})。`,
  ```
  vega-lite 抛的 raw 永远是 `Invalid field type "undefined"`(内部已把非法值标准化为 undefined 再抛),所以 `m[1]` 永远是字面字符串 `"undefined"`,三元表达式的 else 分支永不走。
- **本审查实地复跑**:smoke test 行 167-168 输入 `type:"quantitativ"`(拼写错),实际 raw 是 `Invalid field type "undefined"`(vega-lite 内部已转),m[1] === "undefined" 永远 true。
- **影响**:代码可读但冗余;维护者可能误以为 else 分支可达。
- **R3 处置**:**不改**(代码显式表达"若是 undefined 字面,给更友好提示"的意图;若未来 vega-lite 升级把非法值原样透出,这条分支会自然激活)。

### 4.3 业务逻辑 / PRD 对齐(1 条:F2)

#### F2 [low] · chart.ts 外层 catch vh HINT 与 knownErrorPatterns 重叠

- **现象**:`src/chart.ts:96-102` compile 错误的 catch 仍拼 `vh HINT`:
  ```ts
  } catch (e: any) {
    const m = String(e?.message ?? e);
    let vh = "";
    if (/gradient|length/i.test(m)) vh = " HINT: gradient 写法 — ...";
    else if (/signal|Unrecognized/i.test(m)) vh = " HINT: condition.test 用 ...";
    throw new Error(`Vega-Lite spec error: ${m}${vh}`);
  }
  ```
  `src/chart.ts:111-115` render 错误的 catch 同型。两条 HINT 文本与 `knownErrorPatterns.vega-lite[1]`(pattern 2: Unrecognized signal)和 `[2]`(pattern 3: gradient)的 remediation 高度重叠。
- **数据流追踪**:
  - chart.ts catch 抛 `Vega-Lite spec error: ${m}${vh}` → 进入 raw。
  - `src/index.ts:1002-1011` generate_chart handler 捕获,调 `normalizeEngineError("vega-lite", msg, ...)`。
  - normalizeEngineError 的 rx 在 msg 上匹配(如 pattern 2 rx `/Unrecognized signal|signal name "([^"]+)"/` 匹配 `Unrecognized signal name "foo"` 部分)。
  - 输出 formatNormalized 的 remediation(**覆盖** chart.ts 拼的 vh HINT,因为 vh HINT 进了 raw 但不在 formatNormalized 输出里)。
- **实际行为**:**功能正确**(LLM 看到 normalizeEngineError 的 remediation,信息完整);**但 chart.ts 的 vh HINT 代码成死代码**(拼了但永远不被 LLM 看到)。
- **与 P0-2 §5.5 的关系**:01-功能分析.md §5.5 识别了 `enhanceGraphvizError` 与 knownErrorPatterns 的契约不对称,推荐对称删除,实施者选了删 graphviz(`enhanceGraphvizError` 已删)。**但 §5.5 未明示 chart.ts 的两条 vh HINT 也在保留范围内** —— 实施者保留了 chart.ts,形成 graphviz 已对称、chart.ts 未对称的**新不对称**。
- **R3 处置**:**不改**。
  - 理由 1:功能正确(vh HINT 被 remediation 覆盖,LLM 信息无损)。
  - 理由 2:chart.ts 改动会扩大 P0-2 改动面(超出"handler 层归一化"的边界,变成 chart.ts 内部清理)。
  - 理由 3:这是已知契约不对称的延伸,P0-2 §5.5 的保守路径已默许。
  - 建议登记 P0-2 open_points 供后续阶段(P0-3 或 P1-1)统一清理。

### 4.4 简单架构审查(1 条:F7)

#### F7 [low] · pickUnbalancedBrace 简化版启发式

- **现象**:`src/handlers/error-format.ts:97-109`:
  ```ts
  function pickUnbalancedBrace(input: string): string | undefined {
    let depth = 0;
    let suspectLine: string | undefined;
    const lines = input.split(/\r?\n/);
    for (const l of lines) {
      for (const ch of l) {
        if (ch === "{") depth++;
        else if (ch === "}") depth--;
      }
      if (l.includes("{")) suspectLine = l.slice(0, 120);
    }
    return depth > 0 ? suspectLine : undefined;
  }
  ```
- **局限**:
  1. 字符串/注释里的 `{` `}` 也计数(如 `label: "{not real}")`;
  2. depth 变负后(多个 `}`)永远不回 0,返回 undefined;
  3. suspectLine 是"最后一个含 `{` 的行",可能不是真正出错行。
- **影响**:对常见场景(用户漏写右大括号 `x: { a: 1`)有效;对复杂 DSL 场景可能定位不准。offending 回显可能错,但不崩溃、不破坏契约三件套。
- **R3 处置**:**不改**(注释已标"简化版";D2 pattern 4 触发场景的 90% 是简单未闭合,启发式足够;真正的 DSL lint 应由 P1-1 pre-flight 层负责,P0-2 不越界)。

### 4.5 正面观察(1 条:F8)

#### F8 [low] · R2-T3 高级 ESM 测试技巧

- **内容**:`test/error-format.smoke.mjs:341-399` R2-T3 case 用 `node:module register()` 装一次性 loader,把 `@resvg/resvg-js` 重定向到 `virtual:mock-resvg`(总抛错的 mock),验证 chart.js PNG 复用路径的 [resvg] 前缀不被 outer catch 二次包装。
- **亮点**:
  1. **ESM 命名空间 immutable 的正确处理** —— 直接 monkey-patch `@resvg/resvg-js` 的命名空间导出不可行(ESM 规范禁止);用 `resolve+load` 双 hook 改 specifier 解析路径才能让 mock 生效。
  2. **module cache 的正确处理** —— 之前的 graphviz 回归测试已把真实 resvg-js 加载进 Node module cache,单 `load` hook 对已缓存 URL 不再生效;用 `virtual:mock-resvg` 这个全新 URL 强制触发 load 返回 mock source。
  3. **cache-busting query** —— `import(\`../dist/chart.js?t=stub-${Date.now()}\`)` 用 query 强制重新 import chart.js,让 mocked Resvg 生效(register 只对 register 之后发起的 import 起作用)。
  4. **cleanup** —— finally 块 `rmSync(tmpDir, { recursive: true, force: true })` 清理临时 loader 文件。
- **价值**:这种"用 module register mock 具名 ESM 依赖"的模式可以复用到 media-gen-mcp 后续所有需要 mock 引擎依赖的测试场景(如 P0-3 引入正式 test 基建时 mock @viz-js/viz / @terrastruct/d2)。
- **R3 处置**:**无需动作**。建议记入 media-gen-mcp 测试模式库。

---

## 5. Round 3 · 复审闭环决策

### 5.1 处置分类统计

| 处置类别 | 数量 | finding ID |
|---|---|---|
| **建议 P0-2 内修**(1 行改动 + 1 条 smoke test,与 R2 已修同源) | 1 | F1 |
| **不改**(契约不对称已知,实施者选保守路径) | 1 | F2 |
| **改实施记录**(本审查已 flag,实施者同步) | 1 | F3 |
| **改注释**(1 行,可选) | 1 | F4 |
| **不改**(作为纵深防御保留) | 1 | F5 |
| **不改**(代码可读,未来升级自然激活) | 1 | F6 |
| **不改**(简化版注释已标,P1-1 负责) | 1 | F7 |
| **无需动作**(记入测试模式库) | 1 | F8 |
| **P0-2 范围内实际代码修复(本审查员建议)** | **0~1**(F1 由主控决定) | -- |

### 5.2 实际修复(P0-2 范围内)

本审查员**不直接改代码**(审查员独立于实施者)。建议主控根据 F1 处置选项决策:

- **选项 A(推荐)**:实施者补 F1 修复(1 行 typeof check + 1 条 smoke test),P0-2 内闭环。修复后 P0-2 验收无 medium 残留。
- **选项 B**:接受 F1 转 P0-3(契约三件套测试覆盖时一并补),P0-2 commit 不阻断。F1 在本审查文档登记为已知残留。

### 5.3 跨阶段转交清单(若主控选 B)

| 转交方向 | finding | 转交内容 | 触发条件 |
|---|---|---|---|
| **P0-3(node:test 基建 + 契约测试)** | F1(若不 P0-2 内修)、F2 | (1) 兜底分支 object ctx.input 输出 [object Object] 的修复 + 锁死测试;(2) chart.ts 外层 catch vh HINT 与 knownErrorPatterns 的对称清理 | P0-3 引入 test/ 目录 + tsconfig.test.json + `npm test` 接入时 |
| **P0-3 或独立小 PR** | F3、F4 | (1) 实施记录 §0/§1.1 数字回填;(2) check-error-text.mjs:37-38 注释描述修正 | 任意时间点,不阻塞主线 |
| **P1-1(DSL pre-flight lint)** | F7 | pickUnbalancedBrace 升级为真正 DSL 解析器,处理字符串/注释里的 `{` `}` | P1-1 启动时 |
| **media-gen-mcp 测试模式库** | F8 | ESM module register mock 模式记入文档 | PRD 下次大修或独立 wiki 时 |
| **open_points 种子** | F5、F6 | render_svg handler 内容 rx 冗余、Vega-Lite pattern 1 死分支 —— 文档化,非修复项 | PRD 下次大修时 |

---

## 6. 最终残留

| Severity | R1+R2 后残留(从代码注释反推) | R3 新发现 | R3 后残留(若主控选 A) | R3 后残留(若主控选 B) |
|---|---|---|---|---|
| critical | 0 | 0 | **0** | **0** |
| high | 0 | 0 | **0** | **0** |
| medium | 0(R2 已修同源 make) | 1(F1 兜底分支漏修) | **0**(F1 内修闭环) | **1**(F1 转 P0-3,不阻断) |
| low | 0(R1+R2 已闭环 4+ 条) | 7(F2-F8) | 7(全部已分类闭环) | 7(全部已分类闭环) |

### 6.1 关键说明 —— 唯一 medium(F1)判定

F1 是 R2 修复不完全的同源漏修,核心理由四点:

1. **同源问题已由 R2 部分修复**:`error-format.ts:320-323` resvg pattern 1 make 函数已修(对象 ctx.input 用 JSON.stringify),`error-format.ts:399-404` 主函数 make 调用层已修(try/catch 守 TypeError 穿透)。**兜底分支(line 410)是漏修的第三处同源位置**。
2. **触发路径真实可达**:`src/index.ts:1008` generate_chart handler 传 `a.spec as Record<string, unknown>`(Vega-Lite spec 对象),Vega-Lite 错误形态多样,落到兜底分支是常态。
3. **诊断价值归零**:LLM 看到 `原始输入前 80 字符:[object Object]` 完全无法定位错误 —— 这与 P0-2 "LLM 友好错误契约"的核心目标直接冲突。
4. **修复成本极低**:1 行 typeof check + 1 条 smoke test,与 R2 已修方案完全对称。

**与 P0-1 审查 F1 的对比**:P0-1 的 F1(medium)经复审判定为"PRD 文案层冗余,非实现 bug"→ 误报 → 降级。**P0-2 的 F1 不同** —— 它是真实的代码缺陷(兜底分支输出字符串可验证为 `[object Object]`),不是误报。但严重度有限(不崩、不违硬契约),归为 medium 而非 high。

**主控决策建议**:**选项 A(P0-2 内修)**。理由:R2 已经把同源问题列为主战场(代码注释明示"P0-2 第 2 轮审查修复"),兜底分支漏修属于"战果扩大 1 处",修复成本对称,没有理由留债。

### 6.2 PRD §1.2 五条成功标准 + 反崩溃硬契约复核

| PRD 条款 | 要求 | 本审查复核 | 状态 |
|---|---|---|---|
| §1.2.1 契约三件套 | 4 引擎每种已知错误经归一化必含 ① 定位、② 问题陈述、③ 修复动词 | smoke test 27 case 全部断言 `[engine]` + `修复:` 元契约;MCP stdio e2e(实施记录 §5)4 类错输入全部含三件套 | ✅ |
| §1.2.2 offending 片段回显 | D2/Graphviz 回显出错行原文,Vega-Lite 指出 channel 字段路径,resvg 回显 SVG 前 80 字符 | pattern 1-14 各自 make 函数都设置了 offendingConstruct;**兜底分支对 object ctx.input 回显降级(F1)** | ⚠️ **部分**(F1 触发时降级) |
| §1.2.3 Graphviz bug 修复 | `digraph G { A -> }` 必含 `syntax error in line 1 near '}'` 而非 `produced no SVG` | smoke test 行 308-321 锁死 e2e;`src/diagram/graphviz.ts:60-79` 实地复核 viz-js v3 errors 数组显式读取 | ✅ |
| §1.2.4 向后兼容零感知 | 合法调用字节不变、19 工具 inputSchema 不变、check-schema.mjs 仍 pass、错误仍是单字段协议 | `npm test` G2 仍守 19 工具;smoke test 行 323-328 happy path 字节不变;MCP e2e happy path 返回 `{ engine, format, local_path }` | ✅ |
| §1.2.5 覆盖基线 | knownErrorPatterns 首版 ≥14 条(D2×6 / Graphviz×3 / Vega-Lite×3 / resvg×2) | `error-format.ts:142-351` 实地数 14 条;6+3+3+2 分布精确 | ✅ |
| §1.2.4 + §3.2 反崩溃硬契约 | /TypeError|Cannot read|is not a function/ 禁穿透 | smoke test 行 203-236 R2-T1 + R2-T2 锁死(object ctx.input 不崩、circular ref 不穿透);**兜底分支对 object ctx.input 不崩但降级(F1)** | ✅(硬契约守住,质量契约部分降级) |

**关键观察**:PRD §1.2 五条 + 反崩溃硬契约,**硬指标全部达标**(0 硬契约违例);F1 只影响 §1.2.2 的"兜底场景 offending 回显"软指标,不破坏硬契约。

### 6.3 DoD S1–S9 机械验证链未受影响

| DoD | 02-实施记录 §4 自证 | 本审查实地复跑 |
|---|---|---|
| build(tsc 0 error) | ✅ | ✅ `npm run build` 0 error |
| npm test(G1/G2/G3) | ✅ 5 passed | ✅ 5 passed, G2 仍守 19 工具 |
| smoke 测试 | ✅ 24 passed(实际滞后,实测 27) | ✅ **27 passed** |
| grep 防御 | ✅ 11 passed | ✅ 11 passed |
| MCP stdio e2e | ✅ 4 类错输入 + happy path | 抽样 generate_diagram D2 错已含三件套(实施记录 §5 表可复现) |

**结论**:DoD 机械验证链 4 闸门实地全绿;唯一不一致是 smoke 数字(27 vs 文档 24),不影响代码正确性。

---

## 7. 复审结论

### 7.1 review_ok = true · **GO-WITH-CONDITIONS**

**理由四点**:

1. **0 critical / 0 high 实现残留**:8 条 R3 新发现全部归类为 medium ×1(F1,可内修或转 P0-3)+ low ×7(全部已分类闭环,含 1 正面观察)。
2. **4 闸门实地复跑全绿**:build(tsc 0 error)+ npm test(5 passed,G2 守 19 工具)+ smoke(**27 passed**,比实施记录多 3 条 R2 新增 case)+ grep(11 passed)。
3. **PRD §1.2 五条 + 反崩溃硬契约硬指标全部达标**:0 硬契约违例;F1 仅影响 §1.2.2 兜底场景 offending 回显软指标。
4. **R1+R2 修复成果经独立验证**:R2 修复的 2 处反崩溃缺陷(make 函数 + 主函数 make 调用层)经 smoke test R2-T1 + R2-T2 实地锁死;R2-T3 高级 ESM 测试技巧(F8 正面观察)有效锁死 chart.ts PNG 复用路径 [resvg] 前缀透传。

### 7.2 GO-WITH-CONDITIONS 的"条件"

| 条件 | 必要性 | 责任方 |
|---|---|---|
| **C1(推荐):F1 兜底分支 1 行修复 + 1 条 smoke test** | 推荐但不阻断 | 实施者(commit 前) |
| **C2:实施记录 §0/§1.1 数字回填**(smoke 24→27 / +1 文件 render-svg.ts) | 推荐但不阻断 | 实施者(commit 前)或主控(独立小 PR) |
| **C3:check-error-text.mjs:37-38 注释修正**(render-svg.ts → index.ts render_svg handler) | 可选 | 任意时间点 |
| **C4:open_points 登记**(F2 契约不对称 / F5 冗余 rx / F6 死分支 / F7 简化启发式) | 推荐 | 主控(本审查文档已 flag,可直接转 P0-3) |

**若主控接受"留作 P0-3 候选"**(选项 B),C1 不阻断 P0-2 commit —— F1 不违任何 PRD 硬契约,仅诊断质量软指标降级,可在 P0-3 引入正式契约测试时一并修复。

### 7.3 后续追踪建议(非阻断)

1. **P0-3 引入正式契约测试时优先覆盖三条**:
   - F1(兜底分支 object ctx.input 输出 [object Object] 的修复 + 锁死测试);
   - F2(chart.ts vh HINT 与 knownErrorPatterns 的对称清理);
   - **契约对称性全局扫描**(graphviz 已对称、chart.ts 未对称、index.ts handler 三处 [resvg] 前缀检测逻辑不统一[generate_diagram/chart 只看前缀、render_svg 看前缀 OR 内容 rx])。
2. **实施记录同步**(F3):smoke 数字 24→27;+1 文件 render-svg.ts(M,+10/-1,加 [resvg] 前缀对称包装)。本审查文档可作为同步依据。
3. **P1-1 DSL pre-flight lint 启动时**(F7):升级 pickUnbalancedBrace 为真正 DSL 解析器,处理字符串/注释里的 `{` `}`。
4. **ESM module register mock 模式入库**(F8):media-gen-mcp 测试模式库,P0-3 / P1 阶段所有需要 mock 引擎依赖的场景可复用。

### 7.4 阶段闭环

| 项 | 状态 | 证据 |
|---|---|---|
| plan_ok(01-功能分析) | true | 01 §0 |
| impl_ok(02-实施记录) | true | 02 §0 + §4 机械验证 + 本审查 §0.2 实地复跑 4 闸门 |
| **review_ok(03-代码审查,本文件)** | **true · GO-WITH-CONDITIONS** | 本文件 §0 + §6 + §7.1 |
| P0-2 阶段状态 | **可收尾**(待主控决策 C1 内修 vs 转 P0-3) | 待主控终验 + commit + HTTPS push |

---

## 附录 A · 审查发现与 PRD/分析文档映射

| finding | 严重度 | 上溯依据 |
|---|---|---|
| F1 | medium | PRD §1.2.2 offending 回显 + §1.2.4/§3.2 反崩溃硬契约(软指标降级,硬契约未违)+ R2 已修同源(`error-format.ts:320-323` / `:399-404` 注释明示) |
| F2 | low | 01-功能分析.md §5.5 契约不对称(enhanceGraphvizError 对称删除已落地;chart.ts vh HINT 保留为同款不对称的延伸) |
| F3 | low | 02-实施记录.md §0 / §1.1 数字与 git status / `node --test` 实测不一致 |
| F4 | low | scripts/check-error-text.mjs:37-38 注释 vs src/render-svg.ts / src/index.ts:1103-1104 实地核对 |
| F5 | low | src/index.ts:1103-1104 内容 rx vs src/render-svg.ts:200-209 已加 [resvg] 前缀(冗余) |
| F6 | low | src/handlers/error-format.ts:280 三元表达式 vs vega-lite v5.22 实际 raw(永远 "undefined") |
| F7 | low | src/handlers/error-format.ts:97-109 简化版启发式,注释已标 |
| F8 | low(正面) | test/error-format.smoke.mjs:341-399 R2-T3 ESM module register mock 模式 |

## 附录 B · 4 闸门实地复跑原始输出节选(2026-07-21 审查时点)

```
=== build (npm run build) ===
> tsc
(零输出,exit 0)

=== npm test (check-schema.mjs) ===
[G1] create_video schema enum === videoConstraints  ✅ ✅
[G2] 19 工具齐全                                       ✅
[G3] mode/resolution schema enum                       ✅ ✅
=== 5 passed, 0 failed ===

=== smoke (node --test test/error-format.smoke.mjs) ===
ℹ tests 27
ℹ pass 27
ℹ fail 0
ℹ duration_ms 527.460917
(含 R2 新增 R2-T1 / R2-T2 / R2-T3 三条 case,实施记录 §0 表 "24 passed" 已滞后)

=== grep (node scripts/check-error-text.mjs) ===
[P0-2] 跨引擎回退触发子串防御(d2/graphviz/chart/index 严禁含 ...)
  ✅ src/diagram/d2.ts: 不含 "Font not found"
  ✅ src/diagram/d2.ts: 不含 "No time to read"
  ✅ src/diagram/d2.ts: 不含 "SVG data parsing"
  ... (共 11 passed)
=== 11 passed, 0 failed ===
```

## 附录 C · F1 实地复跑证据(Node REPL,2026-07-21)

```
=== F1 vega-lite object ctx.input, unknown err ===
[vega-lite] some unknown vega error xyz123
(未识别的错误形态,请把此消息反馈给 media-gen-mcp 维护者补 knownErrorPatterns 表。原始输入前 80 字符:[object Object])

=== control: resvg pattern 1 object ctx.input (make fixed R2) ===
[resvg] resvg 解析 SVG 失败。真实原因常是下列之一:① 根标签缺 xmlns 属性;② ...
 | offending: {"mark":"bar"} | 修复:三步排查:① 根标签写 <svg xmlns=...>...
```

**结论**:兜底分支(行 410)对 object ctx.input 输出 `[object Object]`,失去诊断价值;对照 R2 已修的 make 函数(行 320-323)对同款 object 输出 `{"mark":"bar"}` JSON 片段。同源问题漏修证据充分。
