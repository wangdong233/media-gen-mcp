# P0-1 功能分析 · MCP 工具 description 工作流化改造

> **阶段**: P0-1(首个改动阶段,工作树已确认 clean)
> **角色**: 功能分析师/架构师
> **分析日期**: 2026-07-21
> **依据 PRD**: `/Users/wangdong/Documents/Project/Agnes AI接入/doc_v11/P0-1-工具描述工作流化实施规划.md`
> **目标代码**: `/Users/wangdong/Documents/Project/Agnes AI接入/media-gen-mcp`(MIT,0.11.0,main 分支)
> **核实方法**: 全部基于实地 Read 源码 + `node` 脚本统计 + baseline `npm run build` / `npm test`,**未依赖 WebSearch**(配额耗尽;PRD §9 open_point #2 的一手官方原文核实结论直接继承,本阶段不重复 web 核实)

---

## 0. 结论速览(plan_ok = true)

**执行路径已确认无误,PRD 在当前代码上可原样跑通,无阻断点。** 证据三件:

1. **PRD 全部 19 处 `文件:行号` 引用与实地源码 100% 一致**(逐一核对,见 §3.1)。PRD 是数小时前写的,代码自此后未动,行号、字符数、cross-ref 断链现状全部精确。
2. **PRD 的"改 description 对 `npm test` 100% 透明"核心断言已实地验证**:`scripts/check-schema.mjs` 仅读工具名(G2 sorted 全等)+ `create_video` 的 4 个 enum(G1 numFrames/frameRate、G3 mode/resolution),**全程不读 description 内容**。baseline `npm test` 输出 `=== 5 passed, 0 failed ===`。
3. **零 handler 改动、零 inputSchema 改动、零新依赖**:改 19 个字符串字面量不可能触发 `tsc --noEmit` 类型错(`description: string` 类型不变),不可能触发 check-schema.mjs(不读 description)。

**唯一需要在实施时注意的非阻断细节**:8/19 工具的 description 现含反引号(如 `generate_image` 的 `` `images` ``)。PRD 的 after 文本用反引号包裹 sibling 工具名(如 `` `analyze_chart` ``),**延续现有"双引号字符串内嵌反引号"模式即可**,切勿改成模板字面量(`` `...` ``)以免 `${}` 误插值(见 §7 R9)。

---

## 1. 阶段目标(来自 PRD §1,确认无误)

### 1.1 解决的路由盲区

media-gen-mcp 的 19 工具是 **model-controlled**(Claude 自主选工具),`description` 是路由唯一信号源。当前三处实证缺陷(本阶段实地复核确认):

| 缺陷 | 实地复核证据 | 严重度 |
|---|---|---|
| **互逆操作零 cross-ref**:`generate_chart ↔ analyze_chart` 双向零互指 | 本阶段 cross-ref 脚本:generate_chart 描述 sibling 命中=none;analyze_chart sibling 命中=none | **P0 最高** |
| **memory 实证翻车**:`generate_image` 未指 `generate_card`/`generate_icon` | generate_image sibling 命中=none;用户 memory `prefer-generate-card-for-cards.md` 的存在即翻车证据 | **P0** |
| **单向 cross-ref 缺 reciprocal**:`create_video → render_video`(仅 render_video 反指)、`render_svg ↔ generate_diagram`(互提引擎名"D2"不点工具名) | create_video sibling=[get_video](缺 render_video);render_svg sibling=none(含字面"D2"非工具名);generate_diagram sibling=none(含"hand-writing SVG"非工具名) | P1 |

### 1.2 成功标准(PRD §1.2 的 S1–S9 全部可机械验证,本阶段确认验证手段可行)

- **S1**(19 工具全含 WHEN)、**S3/S4**(双向 cross-ref 文本命中)、**S7**(每工具 ≤1100 字符)→ 全部可由一个不入库的 `node` 脚本(读 `dist/index.js` 的 `tools/list` 或直接 parse 源码)机械验证。
- **S5/S6/S8**(`npm test` + check-schema.mjs G1/G2/G3 全绿 + build 后 tools/list 正常)→ baseline 已 green,description 改动透明。
- **S9**(memory 翻车场景路由修正)→ 本阶段仅做人工推演(P0-3 LLM-as-judge 才做统计验证),记入 open_points 种子。

---

## 2. 架构落地分析

### 2.1 改造形态:纯 prompt engineering,零结构变动

P0-1 的"架构"就是 **改 19 个 TS 字符串字面量的值**。具体形态:

```ts
// src/index.ts buildTools() 返回的 tool 对象(L114–491),结构未变
{
  name: "generate_chart",          // 不动
  description: "<新字符串>",        // ← 仅此字段的 string 值改写(P0-1 唯一改动面)
  inputSchema: { /* 100% 不动 */ }, // 红线
}
```

- **没有新模块、没有新 import、没有新 handler 分支、没有新 inputSchema 属性、没有新 enum 值**。
- buildTools() 仍是单文件 19 元素数组,handler 仍是 L495 的 `CallToolRequestSchema` 单 switch(本阶段实地核实,见 §3.2)。

### 2.2 description 统一模板(PRD §4.2,本阶段确认适用)

```
<开头一句:动词+产物+核心能力清单(给 Claude 关键词匹配)>

WHEN 触发:用户说 "<场景词 A>" / "<场景词 B>" 时用本工具。

AVOID(负向 + cross-reference):
- <反向场景 1> → use <sibling_tool_A> instead.
- <反向场景 2> → use <sibling_tool_B> instead.

NEXT(工作流下一步):
- 调用后通常 <follow-up>(轮询伴生工具 / list_models 探查 / cross-ref 邻居)。

<确定性/免费立场(可选,核心工具 1 句)>

Multilingual triggers: <ja/es/fr/de/ru/pt 触发词列表(保持现有模式)>
```

**字段取舍规则**(本阶段裁决,与 PRD §4.2 一致):
- **5 个最该改工具**(`generate_chart` / `analyze_chart` / `generate_image` / `create_video` / `render_svg`):四段全填(WHEN + AVOID + NEXT + 触发词)。
- **9 个中优先级**:填 WHEN + ≥1 条 AVOID/cross-ref + 触发词(NEXT 可省)。
- **5 个伴生/列表工具**(`get_video` / `get_pdf` / `list_models` / `list_vision_capabilities` / `generate_qrcode`):仅补 NEXT 或保持现状。

### 2.3 与其它 P0 的架构关系

| 关系 | 说明 | 本阶段处置 |
|---|---|---|
| **P0-1 → P0-2/3/4**(测试基建的前置) | PRD §1.2 + 排期明示 P0-1 先行,让后续测试有"正确路由基线"(否则 LLM-as-judge 在错误路由上做 A/B 无意义) | 本阶段不引入 node:test runner(见 §9 dep_handling),仅用 build+grep+ad-hoc 脚本 |
| **P0-1 ↔ P0-5**(generate_diagram description 合并) | P0-1 §4.5 对 generate_diagram 做 **in-place AVOID 改写**(把"hand-writing SVG is appropriate"改为点名 `render_svg`);P0-5 §4.1 在 generate_diagram 末尾 **append NEXT** 指向新工具 `generate_interactive_diagram`。两 PR 都改 L289 单行字符串 → Git 三方合并必报 textual conflict | **裁决:P0-1 先合并 → P0-5 rebase onto P0-1**(成本最低:P0-5 只需在 P0-1 已落地的 canonical 字符串末尾拼一行 NEXT)。P0-1 拥有 generate_diagram description 的权威模板。合并后该 description 必须同时含 `render_svg`(P0-1 AVOID)+ `generate_interactive_diagram`(P0-5 NEXT)+ 恰好 1 个 `NEXT:`(守 PRD §3.3 line 129 单工具最多 1 条 NEXT 策略)。**本阶段为 P0-5 预留的 rebase 前置**:P0-1 改 generate_diagram 时只做中段 AVOID 改写,不加 NEXT 行(NEXT 行让给 P0-5 append);render_svg↔generate_diagram 双向 cross-ref 的 NEXT 在对侧 render_svg 上(P0-1 §4.4(5)after)。 |
| **P0-1 → P0-5 的 check-schema.mjs G2**(工具数 19→20) | P0-5 新增第 20 工具 `generate_interactive_diagram` 会让 check-schema.mjs G2 的 19 工具 sorted 断言失败。**硬范围明示**:这是"唯一允许的例外改动",且由 P0-5 负责,**本阶段 P0-1 严禁触 check-schema.mjs**(本阶段 19 工具不变,G2 仍 green) | 本阶段不动 check-schema.mjs 全文 |

---

## 3. PRD 引用核实(行号 / 字符数 / 结构断言逐项对账)

### 3.1 19 工具 description 行号 + 字符数(PRD 声明 vs 实地)

本阶段 `node` 脚本统计结果(读 `src/index.ts` 双引号字符串字面量):

| # | 工具 | PRD 声明行号 | 实地行号 | PRD 声明字符 | 实地字符 | 一致? |
|---|---|---|---|---|---|---|
| 1 | generate_image | L122-123 | L123(`description:`L122 + 字符串 L123) | 425 | 425 | ✅ |
| 2 | create_video | L150-151 | L151 | 399 | 399 | ✅ |
| 3 | get_video | L180-181 | L181 | 200 | 200 | ✅ |
| 4 | extract_text | L196-197 | L197 | 436 | 436 | ✅ |
| 5 | extract_table | L218-219 | L219 | 378 | 378 | ✅ |
| 6 | analyze_chart | L235-236 | L236 | 268 | 268 | ✅ |
| 7 | describe_image | L252-253 | L253 | 352 | 352 | ✅ |
| 8 | list_models | L269 | L269(单行) | 228 | 228 | ✅ |
| 9 | list_vision_capabilities | L277-278 | L278 | 598 | 598 | ✅ |
| 10 | generate_diagram | L288-289 | L289 | 1064 | 1064 | ✅ |
| 11 | generate_qrcode | L306 | L306(单行) | 270 | 270 | ✅ |
| 12 | generate_chart | L325 | L325(单行) | 606 | 606 | ✅ |
| 13 | generate_formula | L339-340 | L340 | 438 | 438 | ✅ |
| 14 | generate_icon | L359-360 | L360 | 322 | 322 | ✅ |
| 15 | generate_card | L377-378 | L378 | 928 | 928 | ✅ |
| 16 | render_svg | L410-411 | L411 | 462 | 462 | ✅ |
| 17 | render_video | L428-429 | L429 | 527 | 527 | ✅ |
| 18 | extract_pdf | L449-450 | L450 | 575 | 575 | ✅ |
| 19 | get_pdf | L477-478 | L478 | 192 | 192 | ✅ |

**汇总**:19/19 行号精确;19/19 字符数精确(差 0);平均 456 字符(PRD 声明 456);总计 8668 字符。PRD 的现状矩阵 100% 准确。

### 3.2 handler / inputSchema 行号核实

| PRD 声明 | 实地 | 偏差 | 影响 |
|---|---|---|---|
| `buildTools()` 在 L114 | L114(`function buildTools() {`) | 0 | ✅ |
| 19 工具定义在 L114–491 | L114–491(`buildTools()` 闭合于 L491) | 0 | ✅ |
| `CallToolRequestSchema` handler 起 L493 | **实际 L495**(L493 是 `ListToolsRequestSchema` handler) | **+2 行** | ⚠️ 无功能影响(这是"严禁触碰"区,行号略偏不改变"别动它"的结论)。实施者如需精确定位:`grep -n "CallToolRequestSchema" src/index.ts` → L18(import)+ L495(setRequestHandler) |
| inputSchema 在 L120-490 | 实际 L124(generate_image 首个 inputSchema)–L488(get_pdf inputSchema 闭合) | ±4 | ⚠️ 同上,无影响 |

### 3.3 cross-ref 断链 + WHEN/AVOID 现状复核(本阶段脚本输出)

```
generate_image       WHEN:Y AVOID:n NEXT:n  cross-ref:[none]              ← memory 翻车根因
create_video         WHEN:Y AVOID:n NEXT:Y  cross-ref:[get_video]         ← 缺 render_video
get_video            WHEN:n AVOID:n NEXT:Y  cross-ref:[create_video]
extract_text         WHEN:n AVOID:n NEXT:n  cross-ref:[generate_image]
extract_table        WHEN:n AVOID:n NEXT:n  cross-ref:[none]
analyze_chart        WHEN:n AVOID:n NEXT:n  cross-ref:[none]              ← 互逆断链(对侧)
describe_image       WHEN:n AVOID:n NEXT:n  cross-ref:[none]
list_models          WHEN:Y AVOID:n NEXT:n  cross-ref:[generate_image, create_video]
list_vision_capabilities  WHEN:n AVOID:Y NEXT:n  cross-ref:[4 个 vision 邻居]
generate_diagram     WHEN:n AVOID:Y NEXT:n  cross-ref:[none]              ← 提"D2"非工具名
generate_qrcode      WHEN:n AVOID:n NEXT:n  cross-ref:[none]
generate_chart       WHEN:n AVOID:n NEXT:n  cross-ref:[none]              ← 互逆断链(本侧)
generate_formula     WHEN:n AVOID:n NEXT:n  cross-ref:[none]
generate_icon        WHEN:n AVOID:n NEXT:Y  cross-ref:[none]
generate_card        WHEN:n AVOID:Y NEXT:n  cross-ref:[generate_image]    ← 单向,缺 reciprocal
render_svg           WHEN:Y AVOID:n NEXT:n  cross-ref:[none]              ← 提"D2"非工具名
render_video         WHEN:n AVOID:Y NEXT:n  cross-ref:[create_video]      ← 单向,缺 reciprocal
extract_pdf          WHEN:n AVOID:n NEXT:Y  cross-ref:[extract_text, get_pdf]
get_pdf              WHEN:n AVOID:n NEXT:Y  cross-ref:[extract_pdf]
```

(注:WHEN/AVOID 检测用宽泛正则,比 PRD 手工矩阵略宽松,但 **cross-ref 命中** 这条硬指标与 PRD 矩阵完全吻合——PRD 所称的 3 处互逆/单向断链全部实地复现。)

### 3.4 baseline 验证(本阶段实地跑)

```
$ npm run build    → tsc 0 error
$ npm test         → [G1] ✅✅  [G2] ✅  [G3] ✅✅  === 5 passed, 0 failed ===
```

check-schema.mjs 5 个断言明细(与 PRD §2.3 一致):
- G1:`numFrames.enum === agnes allowedNumFrames` + `frameRate.enum === agnes allowedFrameRates`
- G2:`tools.map(t=>t.name).sort()` 全等 19 工具名单(**本阶段不得改这个列表**)
- G3:`mode.enum` 三值 + `resolution.enum` 三值

---

## 4. 文件清单(绝对路径)

### 4.1 修改(唯一代码改动面)

| 文件 | 改动 | 触点 |
|---|---|---|
| `/Users/wangdong/Documents/Project/Agnes AI接入/media-gen-mcp/src/index.ts` | 19 处 `description` 字符串字面量改写(buildTools() 数组内) | L123 / L151 / L181 / L197 / L219 / L236 / L253 / L269 / L278 / L289 / L306 / L325 / L340 / L360 / L378 / L411 / L429 / L450 / L478(见 §3.1 实地行号表) |

**严禁触碰**(红线,本阶段实地核实确认全部 active):
- `src/index.ts` 所有 `inputSchema` 字段(L124–L488 之间):schema/properties/required/enum/defaults
- `src/index.ts` L495 起 `CallToolRequestSchema` handler 全文(`case "..."` 分支不动)
- `src/index.ts` L493 `ListToolsRequestSchema` handler
- `/Users/wangdong/Documents/Project/Agnes AI接入/media-gen-mcp/scripts/check-schema.mjs` 全文(G2 工具名单 19→20 是 P0-5 的专属例外,本阶段不碰)
- `/Users/wangdong/Documents/Project/Agnes AI接入/media-gen-mcp/package.json` 全文(零新依赖)
- `/Users/wangdong/Documents/Project/Agnes AI接入/media-gen-mcp/tsconfig.json` 全文
- 任何 `src/` 下非 index.ts 的文件

### 4.2 新建

| 文件 | 用途 | 入库? |
|---|---|---|
| `/Users/wangdong/Documents/Project/Agnes AI接入/doc_v11/parse/pares1/01-功能分析.md` | **本文件**(阶段分析) | doc_v11(非代码 repo) |
| `/Users/wangdong/Documents/Project/Agnes AI接入/doc_v11/parse/排期.md` | 跨阶段排期(若主控要求本阶段产出) | doc_v11 |
| `/Users/wangdong/Documents/Project/Agnes AI接入/doc_v11/parse/pares1/count-desc-chars.mjs`(可选,实施期产物) | 实施时统计 19 工具字符数 + cross-ref 命中的 ad-hoc 脚本 | **不入 media-gen-mcp repo**(放 doc_v11 下,避免污染代码库) |

---

## 5. 接口签名 & 数据结构(零变化)

### 5.1 类型签名(本阶段无任何变化)

```ts
// src/index.ts L114 buildTools() 返回元素的结构(MCP Tool 形态)
interface Tool {
  name: string;
  description: string;   // ← 仅此字段的 string 值改写(类型仍为 string)
  inputSchema: {
    type: "object";
    properties: Record<string, PropertySchema>;
    required: string[];
  };
  // P0-1 不引入下列可选字段(留给 P0-1.5 follow-up,如启动):
  // annotations?: { readOnlyHint?: boolean; destructiveHint?: boolean; idempotentHint?: boolean; openWorldHint?: boolean };
  // title?: string;
}
```

**MCP SDK 实地核实**(本阶段查 `node_modules/@modelcontextprotocol/sdk/dist/esm/types.d.ts`):
- `Tool.annotations` 是 `z.ZodOptional<z.ZodObject<{...}>>`(L1393/1421/1485...)——**SDK 1.17.4 已支持**。
- `Tool.title` 是 `z.ZodOptional<z.ZodString>`(L1412/1440/...)——**SDK 1.17.4 已支持**。
- `LATEST_PROTOCOL_VERSION = "2025-11-25"`(比 PRD 引用的 "2025-06-18" 更新,annotations/title 更是稳了)。
- 结论:P0-1.5 follow-up 若启动,类型层零障碍;**但本阶段 P0-1 红线 = 零代码改动,不加这两个字段**。

### 5.2 description 字符串的数据结构(写作约定)

本阶段裁决的 19 工具 description 写作约定(给实施者的硬规则):

1. **单行双引号字符串**:延续现有形态(`description:` 单独一行 + 字符串在下一行;或 3 个单行工具 list_models/generate_qrcode/generate_chart 保持 `description: "..."` 同行)。**禁止改成模板字面量 `` `...` ``**(防 `${}` 误插值,见 §7 R9)。
2. **反引号 `` `tool_name` `` 标注 sibling 工具名**:与现有 `generate_image` 描述里的 `` `images` `` 一致(双引号串内嵌反引号是合法 TS,Claude 读到反引号会理解为代码片段)。
3. **段落分隔用 `\n\n`**(双反斜杠 n,TS 字符串里就是真换行):让 Claude 在 tools/list 渲染时看到分段。**注意**:现有 19 工具 description 全是单行无 `\n`,本阶段引入 `\n\n` 是新增形态——`tsc` 不报错(字符串字面量内 `\n` 合法),check-schema.mjs 不读 description(透明)。但需在实施时 `npm run inspect` 肉眼确认 tools/list 渲染正常。
4. **四段标记全英文全大写**:`WHEN:` / `AVOID:` / `NEXT:` / `Multilingual triggers:`(与 PRD §4.2 模板一致,便于 grep 验证 S1/S2/S3/S4)。
5. **中文场景词用括号包裹**:`(柱状图/折线图/饼图/...)`,与现有 19 工具风格一致。

---

## 6. 步骤分解(给实施者的可勾选 TODO)

> 与 PRD §4.6 同源,本阶段补充"实地核实点"。预估总工时 **1 人日(约 8 小时)**。

- [ ] **S0 · 准备(15 min)**:Read `src/index.ts` L114–491 + `scripts/check-schema.mjs`;跑 `npm run build && npm test` 留 baseline(P0-1 已确认 baseline = 5 passed, 0 failed)。
- [ ] **S1 · P0 三工具互逆对改(90 min)**:`generate_chart`(L325 单行)+ `analyze_chart`(L236)+ `generate_image`(L123)。**三处必须同 commit**(PRD §7 R3:单向 cross-ref 比零 cross-ref 更糟)。每改一个 `npm run build` 验证字符串无语法错。
- [ ] **S2 · P1 两工具 reciprocal(45 min)**:`create_video`(L151)+ `render_svg`(L411)。
- [ ] **S3 · 5 工具 cross-ref/AVOID(75 min)**:`generate_diagram`(L289,**只做中段 AVOID in-place 改写,不加 NEXT 行**——NEXT 留给 P0-5 append,见 §2.3)/ `generate_card`(L378)/ `describe_image`(L253)/ `generate_icon`(L360)/ `render_video`(L429)。
- [ ] **S4 · 6 工具 NEXT 补全(60 min)**:`extract_text` / `extract_table` / `generate_formula` / `generate_qrcode` / `list_vision_capabilities` / `extract_pdf`。
- [ ] **S5 · 3 伴生工具复核(15 min)**:`list_models` / `get_video` / `get_pdf`——确认现状已足够,**不动**(PRD §4.5 裁决)。
- [ ] **S6 · 字符数 + build 校验(30 min)**:跑 `count-desc-chars.mjs`(实施期创建于 doc_v11/parse/pares1/),确认全部 ≤1100(generate_diagram DSL 教学型保留 ~1064+NEXT 余量,实际加 P0-5 NEXT 后仍需 ≤1100);`npm run build` 通过。
- [ ] **S7 · 测试不破(15 min)**:`npm test` 必须仍输出 `=== 5 passed, 0 failed ===`(断言数不变,因 check-schema.mjs 不改)。
- [ ] **S8 · 路由抽样人工推演(45 min)**:列 10 个模糊输入("画个图"/"识别图表"/"做个海报"/"生成视频"/"画架构图"...),人工推演新 description 下 Claude 应选工具;记录歧义场景到 P0-3 LLM-as-judge 种子用例。
- [ ] **S9 · commit(10 min)**:`git commit`(本仓库 main 分支,**不 push**——push 由主控终验后走 HTTPS)。commit message 含改动矩阵 + 字符数对比。本阶段单 commit。

---

## 7. 风险识别 + 缓解(本阶段在 PRD §7 基础上新增 R9/R10/R11)

| # | 风险 | 等级 | 缓解 | 本阶段新增? |
|---|---|---|---|---|
| R1 | description 过长吃 context | 中 | 单工具 ≤1100(generate_diagram DSL 教学型例外);19 工具总 ≤14k 字符。`count-desc-chars.mjs` 验证 | PRD 原有 |
| R2 | 触发词重叠反而模糊路由 | 中 | 严格切边界:每反向场景明确 `→ use X instead`;触发词用语言代码区分而非重叠中文词 | PRD 原有 |
| R3 | 双向 cross-ref 写不对称(只改一侧) | **高** | S1 强制 generate_chart + analyze_chart + generate_image **同 commit**;DoD S3/S4 grep 双向命中 | PRD 原有 |
| R4 | Claude 内部对 WHEN/AVOID/cross-ref/字符压缩的权重未知 | 低-中 | **继承 PRD §9 open_point #2 RESOLVED 结论**:Anthropic define-tools 仅背书 WHEN/AVOID/caveats/参数说明 + namespacing + 合并工具;cross-ref 与 ≤1100 硬限列为 P0-3 A/B 验证项,不预设效果。本阶段不重复 web 核实(WebSearch 配额耗尽) | PRD 原有 |
| R5 | memory `prefer-generate-card-for-cards.md` 变冗余但未删 | 低 | memory 是用户私有层,本阶段不动;保留作"路由翻车不再发生"的回归基线 | PRD 原有 |
| R6 | Archify 2023 字符塞满风格在 19 工具空间互相稀释 | 中 | 不借鉴塞满风格,只取骨架;每工具压缩到 ≤800(教学型 ≤1100) | PRD 原有 |
| R7 | license 风险 | **无** | 纯字符串改写,零代码引用;description 内容是 media-gen-mcp 原创 | PRD 原有 |
| R8 | 错别字/中英混排不一致 | 低 | PR 自检 grep:`AVOID`/`WHEN`/`NEXT` 标记全英文全大写;中文场景词括号包裹 | PRD 原有 |
| **R9** | **实施者把双引号字符串改成模板字面量 `` `...` ``**(觉得"方便嵌换行"),导致 `${某变量}` 误插值或 ESLint 风格漂移 | 中 | **本阶段明示**:实施者必须保持双引号 `"..."` 字符串;多段用 `\n\n` 分隔(不是真换行)。code review 时 grep `description:\s*\`` 必须零命中 | **本阶段新增** |
| **R10** | **description 内 `\n\n` 是新增形态**(现有 19 工具全是单行无换行),`npm run inspect` 渲染异常 | 低 | tsc 不报错(合法转义);check-schema.mjs 不读 description(透明);S6/S7 之间插一次 `npm run inspect` 肉眼确认 tools/list 渲染正常 | **本阶段新增** |
| **R11** | **P0-1 + P0-5 在 generate_diagram L289 单行字符串上的 Git 三方合并冲突**(后落地者全量重写覆盖对方 cross-ref) | 中(P1) | **裁决落地顺序:P0-1 先合并 → P0-5 rebase onto P0-1**。P0-1 改 generate_diagram 时**只做中段 AVOID 改写,不加 NEXT**(NEXT 让 P0-5 append)。合并后验证断言(可加进 P0-3 golden byte-compare):`/render_svg/.test(desc)` + `/generate_interactive_diagram/.test(desc)` + `(desc.match(/NEXT:/g)||[]).length===1` + `desc.length<=1100` | 本阶段细化(PRD open_point #9 已 resolved,本阶段补"本阶段如何为 P0-5 预留") |
| **R12** | **实施者误改 check-schema.mjs 的 G2 工具名单**(看到 19 工具想"顺手加注释"等) | 中 | 红线明示:check-schema.mjs 全文禁动;G2 的 19→20 是 P0-5 专属例外。code review 时 `git diff scripts/check-schema.mjs` 必须为空 | **本阶段新增** |

---

## 8. 测试策略

### 8.1 本阶段测试边界(裁决)

**本阶段不引入 node:test runner**。理由:
1. PRD §5.3 明示"本 P0 不引入 LLM-as-judge 测试(那属于 P0-3/P0-4 的 node:test runner 引入范围)"。
2. P0-1 的"测试"本质是"证明没破坏现有行为",而现有行为唯一的机器闸门就是 `npm test`(check-schema.mjs),它已经 green 且对 description 透明。
3. node:test runner 的引入是 P0-2/P0-3 的基建工作,本阶段提前拉会违反"最小改动"原则,且本阶段用不上 runner 的任何能力(assert/build 验证 `node -e` 足够)。

**本阶段测试四件套**:

| 测试 | 方法 | 期望 | 性质 |
|---|---|---|---|
| **T1 · TypeScript 编译** | `npm run build`(`tsc`) | 0 error | 不破坏现有 build |
| **T2 · Schema 单一真源** | `npm test`(check-schema.mjs) | `=== 5 passed, 0 failed ===`(G1/G2/G3 断言数不变) | 不破坏现有 test |
| **T3 · 字符数 + cross-ref grep**(新) | `node doc_v11/parse/pares1/count-desc-chars.mjs` | 19 工具全 ≤1100;5 最该改工具含 `AVOID`+`NEXT`+cross-ref;generate_chart↔analyze_chart 互提;generate_image 提 generate_card+generate_icon | 新增的轻量验证(ad-hoc 脚本,不入库 media-gen-mcp) |
| **T4 · tools/list 渲染**(新) | `npm run inspect` 手动跑 tools/list | 19 工具齐全 + description 字段非空合法 string + `\n\n` 分段正常渲染 | 冒烟 |

### 8.2 路由质量验证(留 P0-3)

本阶段 **不做** LLM-as-judge 测试。仅做 S8 人工推演 10 个模糊输入,结果作为 P0-3 LLM-as-judge 测试用例种子(记入排期.md 的 P0-3 输入清单)。

### 8.3 关键不变量(本阶段必须守)

由于 description 是 LLM 路由提示而非程序输入:
- handler 行为 100% 不变(case 分支不动)
- 产物确定性 100% 不变(description 不参与渲染管线,同输入仍同输出)
- Provider fallback / KeyPool 100% 不变(在 handler 层,本阶段不触)
- inputSchema 100% 不变(properties/required/enum/defaults 全不动)
- check-schema.mjs 100% 不变(G2 的 19 工具名单不动)

**唯一可观察行为变化**:Claude 工具选择阶段的路由决策可能改变(这正是 P0-1 目标,是期望变化不是回归)。

---

## 9. 依赖处理(dep_handling)

**本阶段裁决:零新依赖,零基建引入。**

| 依赖项 | 本阶段是否引入 | 理由 |
|---|---|---|
| `node:test` runner | **否** | P0-2/P0-3 才正式引入。本阶段用 `npm run build` + `npm test` + `node -e`/ad-hoc 脚本足够覆盖 T1–T4。提前拉 runner 违反最小改动 + 用不上其能力 |
| `tsd` / 类型测试框架 | 否 | description 是 `string`,类型签名零变化,无需类型测试 |
| LLM-as-judge 测试框架 | 否 | 留 P0-3(需 node:test runner 做基座) |
| `@modelcontextprotocol/sdk` 升级 | 否 | 当前 1.17.4 已支持 annotations/title(LATEST_PROTOCOL_VERSION="2025-11-25");本阶段又不用这俩字段,无需动 |
| 任何 runtime dep / devDep | 否 | package.json 全文不动(红线) |

**对后续阶段的依赖承诺**:
- 本阶段为 P0-3 LLM-as-judge 预留 **10 个人工推演的模糊输入种子**(S8 产物),写入排期.md 的 P0-3 输入清单。
- 本阶段为 P0-5 generate_diagram rebase 预留 **"只做中段 AVOID 改写,不加 NEXT 行"** 的实施约束(见 §2.3 / §7 R11)。

---

## 10. 未决点已解决记录(open_points_resolved)

本阶段在 PRD §9 的 9 个 open_points 基础上,对涉及本阶段执行路径的点逐一确认/补裁决:

| # | PRD open_point | 本阶段处置 | 依据 |
|---|---|---|---|
| 1 | Anthropic 对 tool description 字符硬上限未实地核实 | **继承 PRD 保守裁决 ≤1100 字符硬限**(19 工具并存经验性压缩,非官方共识)。本阶段不重复 web 核实(WebSearch 配额耗尽) | PRD §9 #1 + §3.3 L132 |
| 2 | Claude 内部对 WHEN/AVOID/cross-ref/字符压缩的权重未知 | **PRD 已 RESOLVED**(基于 Anthropic define-tools.md / tool-use/overview.md 一手原文)。本阶段继承:保留 WHEN/AVOID(官方背书);cross-ref + ≤1100 硬限列为 P0-3 A/B 验证项,不预设 | PRD §9 #2 RESOLVED |
| 3 | "AVOID/cross-ref 写 description" vs "写 CLAUDE.md/README 路由表"未 A/B | **决策点留 P0-2**(项目层路由策略)。本阶段按 PRD 选前者(description 是 model-controlled 路由唯一信号源) | PRD §9 #3 |
| 4 | Archify 塞满风格是否有效未验证 | 本阶段 **不照搬塞满风格**,只取骨架,每工具 ≤800(教学型 ≤1100)。P0-3 A/B 测验证 | PRD §9 #4 |
| 5 | 是否同 PR 加 annotations/title 字段 | **本阶段不加**(红线:零代码改动)。列为 P0-1.5 可选 follow-up(单独 PR)。本阶段实地确认 SDK 1.17.4 类型层已支持,无技术障碍 | PRD §9 #5 + 本阶段 §5.1 SDK 核实 |
| 6 | 路由质量提升无法在本 P0 内量化 | 本阶段仅做 S8 人工推演(10 个模糊输入),作 P0-3 LLM-as-judge 种子 | PRD §9 #6 |
| 7 | generate_qrcode + 3 伴生工具是否加 NEXT | **继承 PRD 裁决**:generate_qrcode 加 NEXT(有清晰 generate_card 嵌入场景);3 伴生工具(get_video/get_pdf/list_models)保持现状 | PRD §9 #7 |
| 8 | memory `prefer-generate-card-for-cards.md` 清理 | **不在本阶段范围**。memory 是用户私有层,本阶段不动。建议 P0-1 上线观察 2 周后由用户自行清理 | PRD §9 #8 |
| 9 | P0-1 ↔ P0-5 generate_diagram description 合并冲突 | **PRD 已 RESOLVED**。本阶段补强:(a) P0-1 改 generate_diagram **只做中段 AVOID in-place 改写,不加 NEXT 行**(NEXT 让 P0-5 append);(b) 推荐落地顺序 P0-1 先→P0-5 rebase;(c) 为 P0-3 golden byte-compare 预留 4 条断言 | PRD §9 #9 RESOLVED + 本阶段 §2.3 / §7 R11 |
| **10**(本阶段新增) | handler 行号 PRD 声明 L493 vs 实地 L495 的偏差 | **已核实并修正**:`grep -n "CallToolRequestSchema" src/index.ts` → L18(import)+ L495(setRequestHandler)。L493 是 ListToolsRequestSchema handler。偏差 +2 行,属"do-not-touch 区行号略偏",不影响实施 | 本阶段 §3.2 |
| **11**(本阶段新增) | 8/19 工具 description 现含反引号(generate_image 的 `` `images` `` 等),实施者若改用模板字面量会触发 `${}` 误插值风险 | **裁决**:实施者必须保持双引号 `"..."` 字符串;多段用 `\n\n`(转义)而非真换行。code review grep `description:\s*\`` 零命中 | 本阶段 §5.2 / §7 R9 |
| **12**(本阶段新增) | description 内 `\n\n` 是本阶段引入的新形态(现有 19 工具全单行),需验证 tools/list 渲染 | **裁决**:T4 测试插一次 `npm run inspect` 肉眼确认;tsc + check-schema.mjs 均透明 | 本阶段 §8.1 T4 / §7 R10 |

---

## 11. 向后兼容(复述 PRD §6,本阶段确认全部成立)

| 调用方 | 兼容性 | 本阶段核实 |
|---|---|---|
| 现有 MCP 客户端(CC / Cline / 其他) | 100% 兼容 | description 是 `string`,改值不改类型 ✅ |
| 旧版本客户端缓存 | 无影响 | MCP `tools/list` 每次连接拉最新,无版本协商 ✅ |
| Handler 行为 | 100% 不变 | L495 `CallToolRequestSchema` handler 不动 ✅ |
| inputSchema | 100% 不变 | L124–L488 所有 properties/required/enum 不动 ✅ |
| check-schema.mjs | 100% 不变 | 全文禁动,G2 仍 19 工具 ✅ |
| Provider fallback / KeyPool | 100% 不变 | 在 handler 层,本阶段不触 ✅ |
| 确定性产物 | 100% 不变 | description 不参与渲染管线 ✅ |

**唯一可观察变化**:Claude 路由决策(期望变化,非回归)。

---

## 12. 立场红线对齐(本阶段逐条确认)

| 红线 | 本阶段符合性 |
|---|---|
| 纯免费(Apache 2.0/MIT 优先,企业可商用) | ✅ 零新依赖;description 内容原创,零代码引用;media-gen-mcp 本身 MIT |
| 同输入同输出可入 git | ✅ description 改写不影响产物确定性(不参与渲染管线);改动是确定性的字符串值改写,可 diff/review |
| 向后兼容 | ✅ 见 §11 |
| 不破坏现有 19 工具签名与行为 | ✅ name/inputSchema/handler 全不动;19 工具名单不变(G2 仍 green) |
| 不破坏现有 `npm run build`(tsc 零错误) | ✅ baseline green;改字符串值不可能触发 tsc 错 |
| 不破坏现有 `node scripts/check-schema.mjs`(G1/G2/G3) | ✅ baseline 5 passed;check-schema.mjs 不读 description |
| P0-5 第 20 工具时 check-schema.mjs G2 19→20 是唯一例外 | ✅ 本阶段是 P0-1(19 工具不变),不触 check-schema.mjs;例外留给 P0-5 |
| 代码改动只在 media-gen-mcp 内 | ✅ 唯一代码改动 = `src/index.ts` 19 处字符串 |
| 阶段文档只写到 doc_v11/parse/pares<N>/ + 排期.md | ✅ 本文件写 doc_v11/parse/pares1/01-功能分析.md |
| 严禁改动 media-gen-mcp 以外代码;严禁改动 doc_v11/parse 以外文档 | ✅ 本阶段仅读 Archify 报告/P0-* 规划(只读),只写 doc_v11/parse/ |
| git commit 用 media-gen-mcp main 分支,每阶段一 commit,不 push | ✅ S9 单 commit,push 由主控终验后走 HTTPS |

---

## 13. 与已有功能的关系(给主控的接力清单)

| 接力项 | 交付对象 | 形态 |
|---|---|---|
| 10 个模糊输入人工推演结果 | P0-3(LLM-as-judge 测试用例种子) | S8 产物,记入 `doc_v11/parse/排期.md` 的 P0-3 输入清单 |
| generate_diagram description "只做中段 AVOID,不加 NEXT"约束 | P0-5(rebase onto P0-1) | 本文件 §2.3 + §7 R11;P0-5 实施者读此协调 |
| generate_diagram 合并后 4 条验证断言 | P0-3(golden byte-compare) | `/render_svg/` + `/generate_interactive_diagram/` + 单一 `NEXT:` + ≤1100 字符 |
| cross-ref ON vs OFF 的 A/B 验证需求 | P0-3 | 本阶段保留 cross-ref(按 PRD §4.4),但标为"P0-3 A/B 验证项不预设效果" |
| 立场声明更新(首句权重降级为写作习惯) | 实施者 | PRD §3.3 L132 / §7 R4 / §4.4 立场声明已同步,本阶段继承 |

---

## 14. 给实施者的 5 条硬规则(浓缩)

1. **只改 `src/index.ts` 的 19 个 description 字符串值**,其它一切(inputSchema/handler/check-schema.mjs/package.json/tsconfig.json)禁动。
2. **保持双引号 `"..."` 字符串**,多段用 `\n\n`(转义)分隔;**禁止模板字面量**。反引号 `` `tool_name` `` 标注 sibling 工具名(双引号内嵌反引号合法)。
3. **S1 三工具(generate_chart + analyze_chart + generate_image)必须同 commit**(单向 cross-ref 比零 cross-ref 更糟)。
4. **generate_diagram 只做中段 AVOID in-place 改写,不加 NEXT 行**(NEXT 留给 P0-5 append,避免合并冲突)。
5. **完工前跑 `npm run build && npm test`**(必须仍 0 error + `5 passed, 0 failed`)+ `npm run inspect` 肉眼确认 tools/list + `count-desc-chars.mjs` 验证 ≤1100 字符。

---

**分析结束**。`plan_ok = true`,执行路径已确认无误,无未决阻断点。交付实施者按 §6 步骤分解 + §14 硬规则执行即可。
