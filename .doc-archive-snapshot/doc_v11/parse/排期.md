# P0 五阶段排期 · media-gen-mcp 路由/错误/确定性/产物守门/交互图

> **文档定位**:跨阶段进度表(单一阶段日历视图请看 `pares<N>/`)
> **生成日期**:2026-07-21(P0-1 收尾时点首次创建)
> **上游依据**:`/Users/wangdong/Documents/Project/Agnes AI接入/doc_v11/P0实施规划-总览.md`
> **维护规则**:每阶段收尾时由该阶段的"文档与收尾工程师"更新本表 —— 仅本阶段一行,其它行保持原状(不得回改历史)
> **状态图例**:🟅 已完成 / 🟡 进行中 / 🟄 未启动 / ⏸️ 等待决策门 / ❌ NO-GO

---

## 0. 一句话进度

**P0-1 + P0-2 + P0-3 + P0-4 + P0-5A 已完成**(commit 见 §1);推荐落地顺序:P0-1 ✅ → P0-2 ✅ → P0-3 ✅(test 基建 + golden byte-compare)→ P0-4 ✅(产物守门员,复用 P0-3 锁定的 pngjs/jsqr + 五段式 test 链)→ **P0-5A ✅(交互式 HTML 图 MVP,第 20 工具 generate_interactive_diagram,2026-07-22)**。P0 全 5 阶段主线闭环,4 条 R2 medium(F11-F14)显式转 P0-5B 跟踪。

---

## 1. 5 阶段进度表

| # | 阶段 | 状态 | 实际工时 | 规划工时 | commit SHA | 关键产出 | 阶段文档 |
|---|---|---|---|---|---|---|---|
| **P0-1** | **工具描述工作流化** | 🟅 **已完成** | **~1 人日**(8h:分析 1.5h + 实施 2h + 审查 1.5h + 测试 2h + 收尾 1h) | 1 人日 | **[`74a7acfc391236720515ace0ccd505170757abfb`](https://github.com/wangdong233/media-gen-mcp/commit/74a7acfc391236720515ace0ccd505170757abfb)**(短 `74a7acf`,main,只 commit 不 push) | 16 处 description 改写补 WHEN/AVOID/NEXT/cross-ref;消除 3 处路由断链 + 1 处 memory 翻车根因;零 handler/inputSchema 触碰;4 闸门全绿(build/test/count-desc-chars/04-tests);代码审查 0C/0H/0M 实现残留;8 份 README 各加 1 条 FAQ Q&A;P0-5 rebase 前置协议已落 | `pares1/{00-阶段总览, 01-功能分析, 02-实施记录, 03-代码审查, 04-测试报告}.md` + `04-tests.mjs` + `count-desc-chars.mjs` |
| **P0-2** | **LLM 友好错误契约** | 🟅 **已完成** | **~1 人日**(8h:分析 1.5h + 实施 2.5h + 审查 1.5h × 3 轮 + 测试 2h + 收尾 0.5h) | 3-4 人日 | **[`c038ec78121651ac1787e0cfdf976a6a83e12d66`](https://github.com/wangdong233/media-gen-mcp/commit/c038ec78121651ac1787e0cfdf976a6a83e12d66)**(短 `c038ec7`,main,只 commit 不 push) | 新建 `src/handlers/error-format.ts`(423 行,14 条 patterns:D2×6 / Graphviz×3 / Vega-Lite×3 / resvg×2);**修复 graphviz.ts 丢弃 `result.errors` 的死代码 bug**;迁移 `enhanceD2Error` 9 条 + `enhanceGraphvizError` 1 条 HINT 入 patterns 表(engine 层只抛裸错);4 处 PNG 复用路径统一加 `[resvg]` 前缀 + `engineHint` 参数做结构性路由;7 道闸门全绿(build/npm test 5/smoke 27/unit 30/integration 14/user-journey 7/check-error-text 11,合计 78 case + 16 断言);3 轮审查 0C/0H/1M(F1 测试阶段闭环)/7L;inputSchema 零 diff、G2 仍守 19 工具、happy path byte-identical | `pares2/{00-阶段总览, 01-功能分析, 02-实施记录, 03-代码审查, 04-测试报告}.md` |
| **P0-3** | Golden byte-compare | 🟅 **已完成** | **~1 人日**(8h:分析 1.5h + 实施 3h + 审查 1.5h × 2 轮 + 测试 1.5h + 收尾 0.5h) | 5 人日(含阶段 A 0.5d node:test 基建) | **[`2edef74534085d117052e262da73e9df19056e30`](https://github.com/wangdong233/media-gen-mcp/commit/2edef74534085d117052e262da73e9df19056e30)**(短 `2edef74`,main,只 commit 不 push) | 从零引入 `node:test`(五段式 npm test 链)+ golden byte-compare 套件;6 个本地确定性工具 8 active + 1 icon skip 覆盖;`tsconfig.test.json`(R-05 路径 A)+ `scripts/render-golden.mjs`(R-06)+ `test/golden/{helpers,golden.config,render}.ts`(含 R-01 normalizeMathJaxIds)+ `.github/workflows/ci.yml`(Node 20+22 矩阵);helpers 7 helper + 35 Unit case + 9 User Journey case(测试工程师补,填 PRD §9.1 空缺);pngjs ^5.0.0(MIT)+ jsqr ^1.4.0(Apache-2.0)devDep 锁定(P0-4 跳过);3 项故障注入灵敏度验证全过 + 刷新器幂等性 byte-identical;2 轮审查(R1 3 HIGH 修复 + R2 独立复审 0C/0H/0M/9L);4 闸门全绿(138 tests / 136 pass / 0 fail / 2 skip + 11 + 5 断言 + 体积零增量);立场红线全部守住(src/ 零改、check-schema 零改、tsconfig.json 零改、19 工具零变化);README 加"测试"小节(主 README,8 翻译不动);CI Linux 首跑待 push | `pares3/{00-阶段总览, 01-功能分析, 02-实施记录, 03-代码审查, 04-测试报告}.md` |
| **P0-4** | 产物守门人脚本 | 🟅 **已完成** | **~1 人日**(8h:分析 1.5h + 实施 3h + 审查 1.5h × 2 轮 + 测试 1.5h + 收尾 0.5h) | ~3.75 人日(+2 周灰度) | **[`aba0ea5b4b5180d399741527311c87f544eda566`](https://github.com/wangdong233/media-gen-mcp/commit/aba0ea5b4b5180d399741527311c87f544eda566)**(短 `aba0ea5`,main,只 commit 不 push) | 新建 `src/checks/output-checker.ts`(1238 行,单一真相源,四方共享 `dist/checks/output-checker.js`)+ `scripts/check-render-output.mjs`(100 行 CLI)+ `scripts/build-p04-fixtures.mjs`(79 行)+ `test/check-render-output.test.mjs`(378 行 / 35 case)+ 8 fixture(6 反例 + 2 正例);**11 handler 钩子**(generate_image/create_video/get_video/generate_diagram/generate_qrcode/generate_chart/generate_formula/generate_icon/generate_card/render_svg/render_video)在 `writeLocalRender`/`downloadAsset`/`fs.writeFile` 后 `return ok` 前调 `assertOutputClean`;**三档 severity**(fatal/error/warning)+ profile 双轨(standard/strict)+ report 与 handler 反应解耦;**工具专属**(QR jsQR decode roundtrip / formula has-glyphs / render-svg chrome-pixel-variance);JPEG SOFn+SOS+EOI 三件齐全防 @napi-rs/canvas native SIGSEGV(F6 正面);**紧急回滚** `MEDIA_GEN_CHECK_DISABLE=1`;唯一新增 devDep `@xmldom/xmldom ^0.9.0`(MIT);**`npm test` 追加第 6 段**(strict CLI 自校验,前 5 段不动 OP-2);`tsconfig.json`/`tsconfig.test.json`/`check-schema.mjs`/`check-error-text.mjs`/`render-golden.mjs` 全零改动(OP-1);6 闸门全绿(build tsc 0 + npm test 173/171/2/0 + check-error-text 11/11 + check-schema G1/G2/G3 5/5 G2 守 19 工具 + P0-4 专项 35/35 + CLI strict PASS png);2 轮审查(R1 实施期内嵌 6 条 P1-P6 全闭环 + R2 独立复审 0C/0H/0M/10L 含 5 正面);P0-3 golden 8/8 byte-identical 验证钩子是"只读 sidecar";warnings 字段条件展开(无 warning byte-identical);两周灰度评估转交灰度评估者 | `pares4/{00-阶段总览, 01-功能分析, 02-实施记录, 03-代码审查, 04-测试报告}.md` |
| **P0-5A** | **交互式 HTML 图(第 20 工具)MVP** | 🟅 **已完成** | **~1.5 人日**(13h:分析/PRD 修正 2h + 实施 5h + 审查 2.5h × 2 轮 + 测试 2.5h + 收尾 1h) | MVP 7-10 人日(实际远低于规划,因 P0-1..P0-4 已铺好基建:node:test 五段链 + golden + 产物守门 + 错误契约全可复用) | **[`4ac3cf0146ee84c38b2a7286b034403e98e60a70`](https://github.com/wangdong233/media-gen-mcp/commit/4ac3cf0146ee84c38b2a7286b034403e98e60a70)**(短 `4ac3cf0`,main,只 commit 不 push) | 新增第 20 工具 `generate_interactive_diagram`:D2 DSL → 单文件自包含 HTML,内嵌 D2 双调色板(@media prefers-color-scheme:dark 自动切换,零 JS)+ 极简 viewer(pan/zoom/theme toggle/SVG export)+ Motion Governor(prefers-reduced-motion 停动画);堵 GitHub README 主题切换盲区。**8 新 src(815 行)+ 1 新 test(179 行/15 契约)+ 2 新 golden + 9 modified + 8 README**;三杠杆(darkThemeID/noXMLTag/salt)条件展开最小加性接入 src/diagram/d2.ts,generate_diagram 路径 byte-identical;HTML handler 跳过 P0-4 assertOutputClean(5 契约 asserts S2/S4/S6/S9/S11 在 renderer 内部替代);零新增依赖,零 Archify 代码引用(只 reimplement markmap 函数式 .replace(callback) 范式,~47 行 fillTemplate);6 闸门全绿(build tsc 0 + npm test 191/189/2 skip/0 fail + P0-5A 契约 15/15 + check-error-text 11/11 + check-schema G1/G2/G3 5/5 含 G2 升 20 + Step 1.2.5 确定性审计 10 次连跑 sha256 全同 + 端到端 MCP tools/call);2 轮审查(R1 实施期内嵌 7 条 4.1-4.7 全闭环 + R2 独立复审 14 条 0C/0H/4M/10L)GO-WITH-CONDITIONS;**R2 4 medium(F11 sentinel 碰撞 / F12 darkTheme 空白串 / F13 契约错归 d2 / F14 resvg 错无前缀)显式走 CONDITION B 转 P0-5B 跟踪**(handler 主路径全不命中 + 触发罕见 + 后果可控 + 总内修量 < 20 行);立场红线全部守住(tsconfig.json/tsconfig.test.json/check-error-text.mjs 全零改动;render-golden.mjs 仅加 html-byte 落盘分支;19 工具 inputSchema byte-identical;generate_diagram 调用路径 byte-identical);version 0.11.0 → 0.12.0 | `pares5/{00-阶段总览, 01-功能分析, 02-实施记录, 03-代码审查, 04-测试报告}.md` |
| **P0-5B** | 交互式 HTML 图 polish + R2 4 medium 内修 + Tier 1.11/1.12 examples | 🟄 **未启动** | -- | 0.5-1 人日(cleanup PR:R2 4 medium < 20 行 + 10 low < 30 行 + examples/ + CHANGELOG) | -- | (a)R2 4 medium 内修(F11 sentinel-collision 校验 + F12 darkEffective 判定 + F13 catch 识别契约错 + F14 resvg 加 [resvg] 前缀);(b)10 low cleanup(F1-F10);(c)Tier 1.11/1.12:examples/interactive-html/ + 手工浏览器截图;(d)render_video description 加 NEXT 反向 cross-ref | (待 `pares6/` 或独立 cleanup PR) |

> **P0-5A 备注**:纯调研文档(`/Users/wangdong/Documents/Project/Agnes AI接入/doc_v11/P0-5-Archify源码与同品类深度调研.md`)已于 2026-07-21 先于 P0 流程交付,作为 P0-5 落地的立项前置依据;裁决 D2 + markmap 范式 reimplement + 极简 viewer 路线。P0-5A 的"第 20 工具 MVP"在本表标已完成。

---

## 2. 推荐落地顺序与依赖

```
                                ┌─────────────┐
                                │  P0-1 ✅    │  已完成
                                │  description │  (本表 §1 第 1 行)
                                │  字符串改写  │
                                └──────┬──────┘
                                       │ generate_diagram description
                                       │ 模板权威 → P0-5 rebase 依赖
                                       ▼
        ┌──────────────────────────────┴──────────────────────────────┐
        │           P0-3 阶段 A:node:test 基建 ✅ 已完成              │
        │      tsconfig.test.json + test/ 目录 + pngjs devDep          │
        └──────────────────────────────┬──────────────────────────────┘
                                       │ 共享 node:test runner 就位 ✅
                ┌──────────────────────┼──────────────────────┐
                ▼                      ▼                      ▼
        ┌──────────────┐       ┌──────────────┐       ┌──────────────┐
        │   P0-2 ✅    │       │ P0-3 剩余 ✅ │       │   P0-4 (P)   │
        │ error-format │       │  golden +   │       │ output-check │
        │ + Graphviz   │       │  helpers +  │       │  + handler   │
        │ bug 修复     │       │  CI gate    │       │  11 钩子     │
        │ 已完成       │       │  已完成     │       │  3.75 人日   │
        └──────┬───────┘       └──────┬───────┘       └──────┬───────┘
               │                      │                      │
               │     P0-2 冒烟测试接入 P0-3 runner ✅         │
               │                      │                      │
               │                      │ pngjs 版本单一来源 ✅ │
               │                      └──────────┬───────────┘
               │                                 │
               ▼                                 ▼
        ┌──────────────────────────────────────────────────┐
        │           P0-5B 立项决策门(见 P0 总览 §6)        │
        │  满足 → MVP 7-10 人日 / 不满足 → 选项 B 0.5-1     │
        └──────────────────────────────────────────────────┘
```

**符号**:`(S)` = 串行(必须前置完成);`(P)` = 可并行(共享基建就位后)。

---

## 3. 关键时序约束(必读)

1. **P0-1 → P0-5 的 description 合并约束**(P1,机械可解):两 PR 都改 `src/index.ts` generate_diagram description 单行字面量,Git 三方合并必报 textual conflict。**推荐落地顺序:P0-1 先合并 → P0-5 rebase**,把 P0-5 的 NEXT append 拼到 P0-1 改过的 canonical 字符串末尾,**禁止全量重写覆盖 P0-1 的 AVOID 句**。合并断言:含 `render_svg` + 含 `generate_interactive_diagram` + `NEXT:` 出现恰好 1 次 + 长度 ≤1100。**P0-1 现状(1096 字符)留给 P0-5 的余量仅 4 字符,P0-5 必须做中段 in-place 重写而非末尾 append**(见 pares1/03-代码审查.md F2/F8)。
2. **P0-2 ↔ P0-3 同批落地**:P0-2 冒烟测试 `test/error-format.smoke.mjs` 依赖 P0-3 引入的 `node --test` runner;建议同 PR 链或紧接落地。
3. **P0-3 pngjs 是 P0-4 的版本单一来源**:P0-4 后合时只新增 `@xmldom/xmldom ^0.9.0` + `jsqr ^1.4.0`,pngjs 版本号不独立选型(跟随 qrcode@1.5.4 传递依赖 ^5.0.0)。

---

## 4. 共享基建(被多个 P0 复用)

| 基建 | 谁先建 | 谁依赖 |
|---|---|---|
| `node:test` runner | **P0-3 阶段 A** | P0-2 冒烟测试 / P0-4 契约测试 / P0-5 测试 |
| `test/` 目录 + golden 对偶范式 | **P0-3** | P0-5(`test/golden/interactive-html/` 子目录) |
| `src/handlers/error-format.ts` | **P0-2** ✅ 已交付(14 patterns + 反崩溃契约 + 结构性 [resvg] 路由) | P0-5 handler 沿用 enhanceD2Error 范式 |
| `src/checks/output-checker.ts` | **P0-4** | 11 个 handler 通过 `assertOutputClean()` 钩子复用 |
| golden 目录 | **P0-3**(`test/golden/`) | P0-5(`test/golden/interactive-html/` 子目录) |
| CI stale gate | **P0-3**(`.github/workflows/ci.yml`) | P0-4/P0-5 测试自动接入 |
| `scripts/check-schema.mjs` | **既有**(G1/G2/G3 三断言) | 所有 P0 必须保留(任何 P0 不得动一行;P0-5 落地时 G2 工具数 19→20 是唯一例外) |

---

## 5. 累计工时(三种范围)

| 范围 | 规划工时 | 已完成 | 当前状态 |
|---|---|---|---|
| **P0-1 + P0-2 + P0-3 + P0-4**(不含 P0-5) | 约 13 人日 | **~4 人日**(P0-1 ~1d + P0-2 ~1d + P0-3 ~1d + P0-4 ~1d) | P0-1 ✅ / P0-2 ✅ / P0-3 ✅ / P0-4 ✅(不含 P0-5 部分全部完成) |
| **P0-1..P0-4 + P0-5A MVP**(实际范围,P0-5A 远低于 MVP 规划) | 约 14-15 人日(规划 21-23,实际 ~5.5d) | **~5.5 人日**(P0-1 ~1d + P0-2 ~1d + P0-3 ~1d + P0-4 ~1d + P0-5A ~1.5d) | P0-1 ✅ / P0-2 ✅ / P0-3 ✅ / P0-4 ✅ / **P0-5A ✅** —— **P0 全 5 阶段主线闭环**;4 条 R2 medium 转 P0-5B 跟踪(cleanup PR < 0.5 人日) |
| **P0-1..P0-5A + P0-5B cleanup**(含 polish) | 约 15-16 人日 | ~5.5 人日(P0-1..P0-5A) | P0-5B cleanup PR 待启动(R2 4 medium + 10 low + examples) |

---

## 6. P0-1 完成度详情(本阶段收尾快照)

### 6.1 完成清单

- [x] S0 准备(baseline green)
- [x] S1 P0 三工具互逆对改(generate_chart + analyze_chart + generate_image 同 commit)
- [x] S2 P1 两工具 reciprocal(create_video + render_svg)
- [x] S3 5 工具 AVOID/cross-ref/NEXT(generate_diagram 只做中段 AVOID 不加 NEXT,为 P0-5 预留)
- [x] S4 6 工具 NEXT 补全
- [x] S5 3 伴生工具复核(不动)
- [x] S6 字符数 + build 校验(max=1096,全 ≤1100)
- [x] S7 测试不破(5 passed, 0 failed)
- [x] S8 路由抽样人工推演(10 个模糊输入,作 P0-3 LLM-as-judge 种子)
- [x] S9 commit(单 commit,不 push)

### 6.2 四闸门快照

| 闸门 | 结果 |
|---|---|
| `npm run build` | ✅ tsc 0 error |
| `npm test` | ✅ `=== 5 passed, 0 failed ===` |
| `node doc_v11/parse/pares1/count-desc-chars.mjs` | ✅ `=== 47 passed, 0 failed ===` |
| `node --test doc_v11/parse/pares1/04-tests.mjs` | ✅ `tests 72 · pass 72 · fail 0` |

### 6.3 关键交付

- **代码**:`src/index.ts` 16 处 description 改写(32 行 +/-,0 行代码逻辑变动)
- **README**:8 份各加 1 条 FAQ Q&A(用户视角,无 paresN 内部标记)
- **阶段文档**:7 份 + 1 个排期(本文件)+ 2 个 .mjs 脚本(实施者 + 测试工程师)
- **P0-5 rebase 前置**:generate_diagram description 含 `render_svg`、不含 `NEXT:`、余量 4 字符(P0-5 实施者按 R11 协议处理)

---

## 7. 跨阶段转交清单(P0-1 → 后续)

| 转交方向 | 内容 | 触发时机 |
|---|---|---|
| **P0-3 LLM-as-judge** | 10 个模糊输入人工推演结果(pares1/00-阶段总览.md §9.2) | P0-3 启动时 |
| **P0-3 golden byte-compare** | generate_diagram description 合并后 4 条验证断言 | P0-3 阶段 D/E |
| **P0-5 rebase** | generate_diagram description canonical 文本(1096 字符)+ "只做中段 AVOID,不加 NEXT"约束 + R11 协议(必须做中段 in-place 重写,不能 append) | P0-5 实施者启动前 |
| **PRD 维护方** | render_video 多语言词清单冗余(F1/F5/F6/F10);analyze_chart NEXT 业务语义可商榷(F9);跨工具多语言语种不一致(F11) | P0-3 LLM-as-judge 验证后 |
| **可选小 PR** | (A) AVOID 全列表式统一(F3);(B) sibling 工具名反引号补齐 2 处(F4) | P0-2 前任意时间点 |

---

## 8. 维护历史

- **2026-07-21**:
  - **首次创建**(P0-1 文档与收尾工程师)。P0-1 标已完成(commit `74a7acf`,9 files changed +40 -16,待主控终验后 HTTPS push);P0-5A 标已交付(纯文档);其它阶段标未启动/等待决策门。
  - **P0-2 收尾更新**(P0-2 文档与收尾工程师)。P0-2 标已完成(commit `c038ec7`,11 files changed +1920 -64,待主控终验后 HTTPS push)。7 道闸门全绿(78 case + 16 断言);3 轮审查 0C/0H/1M(闭环)/7L;inputSchema 零 diff、G2 仍守 19 工具、happy path byte-identical。P0-3 阶段 A 启动时可直接接入 P0-2 留下的 4 个 test 文件(已按 P0-3 命名约定与目录结构写好,无需重写)。
- **2026-07-22**:
  - **P0-3 收尾更新**(P0-3 文档与收尾工程师)。P0-3 标已完成(commit `2edef74`,3 modified + 16 new,待主控终验后 HTTPS push)。4 闸门全绿(138 tests / 136 pass / 0 fail / 2 skip + 11 + 5 断言 + 发布体积零增量);2 轮审查(R1 实施期内嵌审查 3 HIGH 全修 + R2 独立复审 0C/0H/0M/9L);立场红线全部守住(src/ 零改、check-schema 零改、tsconfig.json 零改、4 个 P0-2 .mjs 断言零改、19 工具零变化)。P0-3 兑现了 P0-2 留下的"test/ 故意不接入 npm test,保 P0-3 立场"承诺:五段式 npm test 链 + CI stale gate + golden byte-compare 套件(8 active + 1 icon skip)+ 一键刷新器全部就位;pngjs + jsqr devDep 已锁定(P0-4 实施时跳过)。CI Linux 首跑待 push(open_point 2:D2/Graphviz WASM 跨平台 byte 一致性,skipReason 字段已预留兜底)。P0-4 启动时可直接复用 P0-3 的五段式 test 链 + golden 范式 + helpers.ts(扩展点:加 `compareStrategy: "png-pixel-diff"` + icon-skip 转 active)。
  - **P0-4 收尾更新**(P0-4 文档与收尾工程师)。P0-4 标已完成(commit `aba0ea5`,4 new + 4 modified,待主控终验后 HTTPS push)。6 闸门全绿(build tsc 0 error + npm test 173/171/2 skip/0 fail + check-error-text 11/11 + check-schema G1/G2/G3 5/5 G2 守 19 工具 + P0-4 专项 35/35 + CLI 第 6 段 strict 自校验 PASS png);2 轮审查(R1 实施期内嵌 6 条 P1-P6 全闭环 + R2 独立复审 0C/0H/0M/10L 含 5 正面观察);立场红线全部守住(`tsconfig.json`/`tsconfig.test.json`/`check-schema.mjs`/`check-error-text.mjs`/`render-golden.mjs` 全零改动,19 工具 inputSchema byte-identical,warnings 条件展开)。P0-4 把产物守门员从测试期 byte 回归(P0-3)推到运行时物理合法性:11 个 handler 钩子在落盘后 `return ok` 前调 `assertOutputClean`,三档 severity(fatal/error/warning)+ profile 双轨(standard/strict);P0-3 golden 8/8 byte-identical 验证钩子是"只读 sidecar"。OP-1(output-checker.ts 放 src/checks/,主 tsconfig 编,tsconfig.test.json 不动)+ OP-2(npm test 追加第 6 段不重写)+ OP-3(只新增 @xmldom/xmldom ^0.9.0 MIT,pngjs/jsqr 复用 P0-3)+ OP-4(用结构性锚点不用行号)四条关键裁决全部落地。两周灰度评估 + OP-7 抽 decode-helpers.ts + R4 ffmpeg-static license 迁移均转交后续。P0-5 实施者注意:加第 20 工具时 (a)check-schema.mjs G2 19→20(task 卡已点名);(b)`test/check-render-output.test.mjs:361-372` meta 测试 assertOutputClean 调用数 11→12(同款例外扩展,P0-5 实施者必须同步);(c)src/index.ts handler 钩子可能产生三方合并冲突,用结构性锚点解决。
  - **P0-5A 收尾更新**(P0-5A 文档与收尾工程师)。P0-5A 标已完成(commit `4ac3cf0`,8 new src + 1 new test + 2 new golden + 9 modified + 8 README,待主控终验后 HTTPS push)。**新增第 20 工具 `generate_interactive_diagram`**:D2 DSL → 单文件自包含 HTML,内嵌 D2 双调色板(@media prefers-color-scheme:dark 自动切换,零 JS)+ 极简 viewer(pan/zoom/theme toggle/SVG export)+ Motion Governor(prefers-reduced-motion 停动画);堵 GitHub README 主题切换盲区。6 闸门全绿(build tsc 0 + npm test 191/189/2 skip/0 fail + P0-5A 契约 15/15 + check-error-text 11/11 + check-schema G1/G2/G3 5/5 含 **G2 升 20**(P0-5A 唯一允许例外)+ Step 1.2.5 确定性审计 10 次连跑 sha256 全同(前缀 `146f1b80971985ce`)+ 端到端 MCP tools/call 20 工具产 HTML 浏览器可开);2 轮审查(R1 实施期内嵌 7 条 4.1-4.7 全闭环 + R2 独立复审 14 条 0C/0H/4M/10L)GO-WITH-CONDITIONS。**R2 4 medium(F11 sentinel 碰撞 / F12 darkTheme 空白串 / F13 契约错归 d2 / F14 resvg 错无前缀)经独立复现确认非误报,handler 主路径全不命中 + 触发罕见 + 后果可控 → 本 commit 显式走 CONDITION B 转 P0-5B 跟踪**(总内修量 < 20 行)。立场红线全部守住:tsconfig.json/tsconfig.test.json/check-error-text.mjs 全零改动;render-golden.mjs 仅加 html-byte 落盘分支(P0-3 设计预期扩展点);**19 工具 inputSchema byte-identical**;**generate_diagram 调用路径 byte-identical**(三杠杆 darkThemeID/noXMLTag/salt 条件展开最小加性接入 src/diagram/d2.ts,三展开项全 false → renderOpts 退化原状);**HTML handler 跳过 assertOutputClean**(HTML 是 viewer 容器非 raster/vector,5 契约 asserts S2/S4/S6/S9/S11 在 renderer 内部替代,meta 测试预期 11 调用数不变);零新增依赖(D2 WASM / puppeteer-core / resvg 全复用已有);零 Archify 代码引用(只 reimplement markmap 函数式 `.replace(callback)` 范式,~47 行 fillTemplate,不 import 包)。8 份 README 各加 1 行能力 + version badge 0.11.0 → 0.12.0。**P0 全 5 阶段主线闭环**(P0-1 ✅ + P0-2 ✅ + P0-3 ✅ + P0-4 ✅ + P0-5A ✅),P0-5B cleanup PR(R2 4 medium + 10 low + Tier 1.11/1.12 examples)待启动。

---

**排期文档结束**。每阶段收尾时更新本表本阶段一行,其它行不得回改。决策门触发条件见 P0实施规划-总览.md §6。
