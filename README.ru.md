# media-gen-mcp

> «Швейцарский нож для изображений» в Claude Code — создаём картинки, рисуем идеи, понимаем изображения: всё одной фразой, абсолютно бесплатно.

<p align="center">
  <img src="https://img.shields.io/badge/version-0.11.0-blue">
  <img src="https://img.shields.io/badge/license-MIT-green">
  <img src="https://img.shields.io/badge/MCP-compatible-purple">
</p>

**Установите в Claude Code один раз — и любая работа с изображениями будет делом одной фразы.** Дизайнерам — генерация картинок, программистам — архитектурные диаграммы, маркетингу — карточки для шеринга, финансистам — извлечение таблиц из счетов: генерация изображений / видео + распознавание + рисование диаграмм / карточек / QR-кодов — всё в одном флаконе и **абсолютно бесплатно** (бесплатный провайдер + локальные движки, работает сразу после установки).

Мучаетесь, что каждую неделю нужно сделать пару картинок и для каждой ставить отдельный инструмент и запоминать свои параметры? Здесь — одна установка, и все сценарии работы с изображениями можно смело переложить на Claude.

<div align="center">

[简体中文](README.md) | [English](README.en.md) | [Deutsch](README.de.md) | [Español](README.es.md) | [Français](README.fr.md) | [日本語](README.ja.md) | [Português](README.pt.md) | **Русский**

</div>

## Содержание

- [Что вы получаете одной фразой](#что-вы-получаете-одной-фразой)
- [Старт за 60 секунд](#старт-за-60-секунд)
- [Полный набор возможностей](#полный-набор-возможностей)
- [Подробно о конфигурации](#подробно-о-конфигурации)
- [Частые вопросы](#частые-вопросы)
- [Кому это подходит](#кому-это-подходит)
- [Поддержите автора](#поддержите-автора)
- [License](#license)

---

## Что вы получаете одной фразой

| Вы говорите … | Вы получаете |
|---|---|
| «Нарисуй киберпанк-кота, неоновое свечение» | AI-реалистичное изображение, сохраняется в `output/` |
| «Сгенерируй 5 секунд видео с закатом на море» | AI-видео MP4 (генерируется в фоне, по завершении приходит уведомление) |
| «Нарисуй архитектурную диаграмму: клиент → API-шлюз → сервис заказов + сервис платежей» | Векторная архитектурная диаграмма |
| «Построй столбчатую диаграмму по этим данным о продажах» | HD-диаграмма данных |
| «Сделай QR-код на github.com» | Векторный QR-код |
| «Отрендери E=mc² как HD-формулу» | Векторная формула |
| «Сделай тёмную градиентную карточку для шеринга, заголовок „Июльские новинки 🚀"» | Аккуратно свёрстанная карточка (китайский + emoji автоматически) |
| «Распознай таблицу в этом скриншоте счёта-фактуры» | Таблица HTML/Markdown, готовая к вставке |
| «Считай данные из этой столбчатой диаграммы» | Структурированные данные CSV/JSON |
| «Опиши, что на этой картинке» | Ответ естественным языком |
| «Извлеки весь текст из этого 20-страничного PDF-отчёта» | Цельный текст / Markdown / JSON (для цифровых PDF — мгновенно, для сканов — автоматический постраничный OCR) |
| «Извлеки текст из скана этого договора, водяные знаки и красные печати проигнорируй» | Чистый текст (области водяных знаков / печатей / колонтитулов вырезаются автоматически) |
| «Этот двухколоночный доклад объедини в порядке чтения в один текст» | Одноколоночный связный текст (многоколоночный порядок чтения восстанавливается автоматически, без путаницы строк) |
| «Я сейчас могу распознавать таблицы? Китайский OCR настроен?» | Список текущих возможностей + рекомендации по маршрутизации (что работает / что не настроено / что использовать) |

> Не нужно учить названия инструментов, не нужно ставить системные зависимости — **Claude сам подберёт подходящий способ**.

---

## Старт за 60 секунд

Главная идея: **рисование диаграмм / карточек / QR-кодов / формул — локальные движки, и распознавание изображений (OCR) тоже по умолчанию работает в процессе — ничего не вызывает AI и не требует сети, всё работает сразу после установки**. Только AI-реалистичные изображения / видео требуют бесплатного API Key — поэтому «первую картинку» и «первое распознавание» вы получаете ещё до регистрации.

### 30 секунд | Подключение одной строкой (без Key)

```bash
# Установка одной строкой (без Key, 30 секунд)
claude mcp add media-gen-mcp npx media-gen-mcp-server

# Перезапустите Claude Code → введите /mcp → увидите media-gen-mcp ✓ Connected — готово
```

### 30 секунд | Первая картинка сразу, без Key

Просто скажите Claude одну фразу:

```
Сделай тёмную техно-карточку для шеринга, заголовок: «Швейцарский нож для изображений в Claude Code»
```

→ Векторное изображение автоматически сохраняется в `output/`, можно сразу открывать и использовать. **Вы ещё не зарегистрировали ни одного API Key, а результат уже у вас.**

Вот эти запросы тоже сработают мгновенно — без Key и без сети:

- «Сделай QR-код на github.com»
- «Отрендери E=mc² как HD-формулу»
- «Нарисуй архитектурную диаграмму: клиент → шлюз → сервис заказов + сервис платежей → БД, тёмный техно-стиль»
- «Распознай цифры на этой картинке с капчей» (OCR, по умолчанию в процессе, ничего ставить не нужно)
- «Извлеки английский текст из этого скриншота»

### Хотите китайский OCR SOTA-уровня / ответы по изображению? Добавьте один Zhipu GLM Key (без развёртывания, опционально)

Лёгкий движок по умолчанию неплох для английского / цифр, но на китайском точность средняя. **Не хочется поднимать PaddleX / vLLM, но нужен китайский SOTA + сложные таблицы + ответы по картинке?** Достаточно одной строки с Zhipu GLM Key — облачная модель **GLM-4.6V-Flash навсегда бесплатна**, без развёртывания, без локальных ресурсов:

```bash
# ① Зарегистрируйте бесплатный аккаунт на https://open.bigmodel.cn/console/apikey и получите api_key (формат {id}.{secret})
#    Внимание: принимается только стандартный key от open.bigmodel.cn; Code Plan key (ZAI_API_KEY) НЕ ГОДИТСЯ —
#    он привязан к выделенному эндпоинту Z.ai + белому списку инструментов, нарушение приведёт к блокировке аккаунта

# ② Запишите в ~/.media-gen-mcp/config.json
{
  "providers": {
    "glm-vision": { "apiKey": "ваш-{id}.{secret}" }
  }
}

# ③ Вернитесь в Claude Code и скажите: «Распознай таблицу в этом китайском счёте-фактуре» / «Сколько людей на картинке? Что они делают?»
#    → Китайский SOTA-распознавание + ответы по изображению, результат сохраняется / выдаётся напрямую
```

> После настройки MCP автоматически включает этот уровень в цепочку fallback: **paddle → glm-vision → vlm → tesseract**; если какой-то уровень временно недоступен — переключение происходит автоматически и незаметно для вас. Подробнее см. в разделе [Подробно о конфигурации · Уровень 2](#подробно-о-конфигурации).

### Хотите AI-реалистичные изображения / видео? Добавьте бесплатный API Key (опционально)

```bash
# ① Получите бесплатный API Key (рекомендуется Agnes, провайдер по умолчанию)
#    https://platform.agnes-ai.com/ → регистрация → API Keys → копируйте sk-xxx
#    (у Zhipu cogview-3-flash / cogvideox-flash тоже навсегда бесплатно — можно выбрать одного или обоих)

# ② Запишите в ~/.media-gen-mcp/config.json (можно указать только одного провайдера)
{
  "providers": {
    "agnes": { "apiKey": "sk-ваш-agnes-key" }
  }
}

# ③ Вернитесь в Claude Code и скажите: «Нарисуй киберпанк-рыжего кота, реализм»
#    → AI-реалистичное изображение сохранится. Видео аналогично: «Сгенерируй 5 секунд видео с закатом на море»
```

> Не хотите использовать npx? Подойдёт и глобальная установка: сначала `npm i -g media-gen-mcp-server`, затем `claude mcp add media-gen-mcp -s user "$(which media-gen-mcp-server)"`.

---

## Полный набор возможностей

> Просто скажите Claude, что хотите сделать, — он сам подберёт подходящий способ. Ниже возможности сгруппированы по «что вы хотите сделать»; вам не обязательно знать, как это называется внутри.

### Создать изображение (с нуля)

**Реалистичная фотография или иллюстрация**
> Вы: «Нарисуй киберпанк-рыжего кота, неоновое свечение, реализм»
> Получите: реалистичное изображение в `output/` (также поддерживаются иллюстрации / концепт-арт продуктов / наброски логотипов / научно-фантастические сцены)

**Превратить фразу или картинку в короткое видео**
> Вы: «Сгенерируй 5 секунд видео с закатом на море»
> Получите: MP4-видео (3–18 секунд; длинные видео генерируются в фоне, по завершении приходит уведомление)

**Скачайте иконку или брендовый логотип**
> Вы: «Дай мне логотип GitHub, 128 пикселей»
> Получите: векторный логотип из библиотеки 200 000+ иконок, готовый к использованию (GitHub / Twitter / Material / Lucide / Font Awesome и др.)

### Понять изображение / PDF (превратить картинку и документ в данные)

**Извлечь текст из скриншота**
> Вы: «Прочитай цифры с этой капчи»
> Получите: чистый текст (капчи / номера счетов / отсканированные документы / истории переписки — всё извлекается)

**Превратить изображение таблицы в HTML / Markdown**
> Вы: «Распознай таблицу в этом скриншоте счёта-фактуры»
> Получите: Markdown-таблицу, готовую к вставке (счета / отчёты / сканы больше не нужно перепечатывать вручную)

**Обратное восстановление данных из диаграммы**
> Вы: «Считай данные из этой столбчатой диаграммы»
> Получите: структурированные данные CSV / JSON (годится для столбчатых / линейных / круговых диаграмм)

**Пусть расскажет об изображении простым языком**
> Вы: «Сколько людей на этой картинке? Что они делают?»
> Получите: ответ естественным языком (вопросы по изображению / рукописный текст / формулы / понимание сложных сцен)

**Извлечь текст из всего PDF**
> Вы: «Извлеки весь текст из этого 20-страничного PDF-отчёта, экспортируй в Markdown»
> Получите: цельный текст / Markdown / JSON — для цифровых PDF напрямую вытягивается текстовый слой (мгновенно), для сканов автоматически выполняется постраничный рендер + OCR; поддерживаются диапазоны страниц (`3` / `1-10` / `odd` / `last`), игнорирование областей водяных знаков / колонтитулов, объединение или постраничный вывод; длинные документы обрабатываются в фоне, по завершении придёт уведомление (счета / договоры / финансовые отчёты / статьи / отсканированные книги — всё подходит)

**Сделать результат распознавания / PDF чище и удобнее для чтения**
> Вы: «Извлеки текст из этого скана договора, **водяные знаки и красные печати проигнорируй**» / «Этот **двухколоночный доклад объедини в порядке чтения** в один текст»
> Получите: чистый, связный текст — два переключателя работают во всех инструментах распознавания / извлечения PDF:
> - **Игнорируемые области**: выделите водяные знаки / красные печати / колонтитулы / заголовки таблиц — они автоматически вырезаются из результата; договоры / сертификаты / сканы больше не «заплывают» водяными знаками
> - **Многоколоночный порядок чтения**: статьи / газеты / резюме / двух- и трёхколоночная вёрстка автоматически объединяется в одноколоночный связный текст в порядке человеческого чтения, без путаницы строк

**Сначала спросите: «Что вообще умеют мои настроенные сервисы распознавания?»**
> Вы: «Я сейчас могу распознавать таблицы? Китайский OCR настроен? А рукописный текст?»
> Получите: список текущих возможностей — какой из уровней распознавания уже настроен / какой не настроен / какой в состоянии охлаждения или ошибки, а также рекомендации по маршрутизации («для таблиц используйте вот это, для рукописи — вот то»); **сначала спросите, потом действуйте — так вы избежите ошибки в момент вызова**

### Чётко изобразить идею (без Key, работает сразу после установки)

**Структурные диаграммы**
> Вы: «Нарисуй архитектурную диаграмму: клиент → API-шлюз → сервис заказов + сервис платежей → БД»
> Получите: векторную архитектурную диаграмму (также поддерживаются блок-схемы / диаграммы последовательности / диаграммы классов / ER-диаграммы / интеллект-карты)

**Превратить данные в графики**
> Вы: «Построй столбчатую диаграмму по этим данным о продажах»
> Получите: HD-диаграмму данных (столбчатые / линейные / круговые / площадные / точечные — подойдёт любой набор чисел или CSV)

### Карточки / постеры / QR-коды (чтобы красиво выложить)

**Сделать карточку для шеринга / OG-изображение / карточку с цитатой / обложку / постер**
> Вы: «Сделай тёмную градиентную карточку для шеринга, заголовок „Июльские новинки 🚀"»
> Получите: аккуратно свёрстанную карточку (заголовок, подзаголовок, градиент, свечение, цветные emoji, встроенный логотип — всё автоматически; китайский и японские иероглифы не превращаются в «кракозябры»)

**Сгенерировать QR-код**
> Вы: «Сделай QR-код на github.com»
> Получите: векторный QR-код (подойдёт URL или текст; чёткий даже при печати постера)

**Отрендерить математическую формулу в HD**
> Вы: «Отрендери E=mc² как HD-формулу»
> Получите: векторную формулу (поддерживаются LaTeX, сложные дроби, химические уравнения)

### Крутые эффекты / техно-графика (одинаковый ввод всегда даёт одинаковый вывод)

**Рендеринг SVG в HD PNG**
> Вы: «Нарисуй техно-фон со свечением, звёздным полем и глубиной резкости»
> Получите: крутой PNG; автоматически выбирается лучший способ рендера, чтобы сохранить точность без потерь

**Превратить HTML / CSS-анимацию в видео**
> Вы: «Сделай 3-секундную анимацию интро продукта, градиент + частицы»
> Получите: видео MP4 / GIF / WebM (интро продуктов / брендовые анимации / демо эффектов, покадровый рендер, одинаковый ввод всегда даёт одинаковый вывод)

> **Подсказка**: создание изображений / распознавание идут через онлайн AI; диаграммы / карточки / QR-коды / анимации — локальные движки — **работают сразу после установки, векторное HD-качество, одинаковый ввод всегда даёт одинаковое изображение**.

---

## Подробно о конфигурации

> В двух словах: **структурные возможности (диаграммы / графики / карточки / QR-коды / формулы) работают из коробки без настройки; для AI-генерации нужна одна строка с API Key; распознавание по умолчанию не требует настройки — самохостинг нужен только для SOTA-качества на китайском / таблиц / диаграмм**. То, чем вы хотите пользоваться, и определяет, что настраивать — всё сразу настраивать не обязательно.

### Настройка по принципу «что я хочу сделать»

| Что вы хотите сделать | Что нужно настроить | Что заработает сразу |
|---|---|---|
| Рисовать архитектурные диаграммы / графики данных / карточки / QR-коды / формулы | **Ничего настраивать не нужно** | Локальный движок, работает сразу после установки |
| AI-реалистичные изображения / AI-видео (текст-в-изображение, текст-в-видео) | Один бесплатный API Key (Agnes или Zhipu, на выбор) | Онлайн-генерация, сохранение в `output/` |
| OCR-распознавание текста (английский / капчи / цифры / простые документы) | **Ничего настраивать не нужно** | По умолчанию работает лёгкий движок в процессе, сразу после установки |
| Китайский OCR / таблицы в счетах / считывание данных с диаграмм / вопросы по изображению / рукописный текст / формулы | **Один Zhipu GLM Key** (без развёртывания, облачно и навсегда бесплатно) **или** самохостинг PaddleX / vLLM | С GLM Key — сразу; после запуска самохостинг-сервиса — впишите одну строку baseUrl |
| **Извлечение текста из PDF** (цифровые / сканы / многостраничные) | Установить две зависимости `npm i pdfjs-dist @napi-rs/canvas` (ставятся при первом обращении к PDF) | Цифровые PDF — мгновенно; сканы — через указанные выше уровни OCR (по умолчанию тоже работает) |
| **Удаление водяных знаков / красных печатей / колонтитулов, восстановление многоколоночного порядка чтения** | **Ничего настраивать не нужно** | При вызове инструмента распознавания / PDF просто скажите: «Claude, проигнорируй водяные знаки» или «объедини в порядке чтения» — применится автоматически |
| **Проверка текущих возможностей распознавания** (что работает / что не настроено) | **Ничего настраивать не нужно** | Просто спросите, Claude вернёт список текущих возможностей + рекомендации по маршрутизации |

---

### I. Настройка генерации (AI-изображения / видео)

**Провайдер по умолчанию: Agnes** (бесплатный уровень действует бессрочно, текст-в-изображение + текст-в-видео полностью открыты). Zhipu — запасной вариант (нативно оптимизирован под китайский).

**Достаточно настроить одного провайдера** (ниже полный `config.json`, можно указать только одного):

```json
{
  "providers": {
    "agnes": { "apiKey": "sk-ваш-agnes-key" },
    "zhipu": { "apiKey": "ваш-zhipu-key" }
  },
  "defaultProvider": "agnes",
  "outDir": "/absolute/path/to/output"
}
```

**Как получить бесплатный API Key**:

- **Agnes** (рекомендуется, по умолчанию): https://platform.agnes-ai.com/ → регистрация → API Keys → копируйте `sk-xxx`
- **Zhipu**: https://open.bigmodel.cn/ → регистрация → API Keys (бесплатные модели: `cogview-3-flash` / `cogvideox-flash`, навсегда бесплатно)

**Настройка обоих провайдеров повышает надёжность**: если один временно «упадёт» (rate limit / колебания сервиса), второй автоматически подхватит — вы этого даже не заметите, никакого двойного списания средств.

**Расположение файла конфигурации**: `~/.media-gen-mcp/config.json` (macOS / Linux) или `%USERPROFILE%\.media-gen-mcp\config.json` (Windows).

> Если этого файла нет — **ничего не падает**: структурные возможности и OCR по умолчанию продолжат работать, просто AI-генерация будет недоступна.

---

### II. Настройка распознавания (изображения / OCR / таблицы / графики / визуальное понимание)

Возможности распознавания делятся **на четыре уровня** — выбирайте по необходимости; первый уровень доступен сразу.

#### Уровень 1: лёгкий движок по умолчанию (без настройки, работает сразу)

- **Что умеет**: OCR английского / цифр / капчей / простых документов
- **Нужно ли поднимать сервис**: **нет**, упакован в MCP-процесс в виде WASM, языковая модель подгружается автоматически при первом вызове
- **Минимальные требования к ресурсам**:
  - CPU: любой (чисто CPU, без зависимости от GPU)
  - GPU: не требуется
  - Память: ~200–500 МБ (колеблется в зависимости от размера изображения)
  - Диск: ~30–50 МБ (WASM-движок + языковые пакеты)
  - Размер модели: включён в указанный объём диска (языковой пакет для английского, несколько МБ)
- **Скорость**: ~3–5 секунд на одно изображение
- **Кому подойдёт**: 90% лёгких сценариев OCR, зарубежные документы, распознавание капчей

> Большинству пользователей этого уровня достаточно, следующие три — опциональное усиление.

#### Уровень 2: Zhipu GLM-4.6V-Flash (облако, бесплатно, без развёртывания; китайский SOTA + VQA)

- **Что умеет**: китайский OCR (уровня SOTA), сложные таблицы (многоуровневые заголовки / объединённые ячейки), анализ диаграмм, ответы по изображению (VQA) — все 4 задачи через облачную модель GLM-4.6V-Flash
- **Нужно ли поднимать сервис**: **нет**, облачный API открытой платформы Zhipu — достаточно зарегистрировать аккаунт и получить api_key
- **Минимальные требования к ресурсам**: **ноль** (чистый HTTP-вызов, без нагрузки на CPU / GPU / диск)
- **Скорость**: ~1–3 секунды на одно изображение (облако, включая сетевой round-trip)
- **Стоимость**: **GLM-4.6V-Flash навсегда бесплатен** (контекст 128K + вывод 32K), по аналогии с бесплатной стратегией текстовой модели GLM-4-Flash
- **Кому подойдёт**: тем, кому нужен китайский SOTA + VQA, но **не хочется поднимать PaddleX / vLLM**; идеально закрывает пробел между порогом входа самохостинга уровней 3/4
- **Как настроить**: на [open.bigmodel.cn](https://open.bigmodel.cn/console/apikey) зарегистрируйте бесплатный аккаунт + получите api_key (формат `{id}.{secret}`), добавьте в `config.json`:

  ```json
  {
    "providers": {
      "glm-vision": { "apiKey": "ваш-{id}.{secret}" }
    }
  }
  ```

  Модель по умолчанию — `glm-4.6v-flash`; через `providers["glm-vision"].model` можно переключиться на `glm-4v-flash` (бесплатная облегчённая) или платные визуальные модели (`glm-4.6v` / `glm-ocr` и т. п.). После настройки MCP автоматически включает этот уровень в цепочку fallback: **paddle(10) → glm-vision(9) → vlm(8) → tesseract(1)**.

- ⚠️ **Комплаенс (важно)**:
  - Принимается **только стандартный api_key от open.bigmodel.cn**; **Code Plan key (ZAI_API_KEY) не годится** — он привязан к выделенному эндпоинту Z.ai + ограничен белым списком из 9 инструментов (Claude Code / Cline / Cursor и т. д., media-gen-mcp среди них нет); за 3 нарушения аккаунт блокируется, подписка не возвращается
  - Ротация нескольких key (`apiKeys: ["k1", "k2", ...]`) технически поддерживается, но **Пользовательское соглашение Zhipu §2/§3 запрещает множественные аккаунты / совместное использование аккаунтов** — ротация нескольких key может расцениваться как нарушение, платформа вправе заблокировать аккаунт. Убедитесь, что все key принадлежат вашим собственным легитимным аккаунтам

#### Уровень 3: PaddleX / PP-StructureV3 (SOTA на китайском + распознавание таблиц)

- **Что умеет**: китайский OCR (заметно точнее дефолтного движка), анализ вёрстки, **счета / отчёты / сканы → таблицы HTML/Markdown**, считывание данных с диаграмм
- **Нужно ли поднимать сервис**: **да**, самохостинг PaddleX REST-сервиса, MCP обращается к нему через `baseUrl`
- **Минимальные требования к ресурсам** (по замерам):

  | Режим | Минимум | Рекомендуется | Примечание |
  |---|---|---|---|
  | GPU-режим | RTX 3060 12 ГБ VRAM | RTX 3060 12 ГБ / Tesla T4 | Загрузка модели ~2.4 ГБ, при обработке сложных PDF пиковое ~6 ГБ |
  | CPU-режим | 4 ядра CPU + 8 ГБ ОЗУ | 8 ядер + 16 ГБ ОЗУ | Запустится (годится для лёгких документов), но на пакетной обработке / сложных PDF медленнее в 3–5 раз |
  | Диск | ~3 ГБ | ~5 ГБ | paddlepaddle + paddlex + веса моделей |
  | Размер модели | ~100–300 МБ (один pipeline) | — | Несколько pipeline складываются |

- **Требования к CUDA**: Compute Capability ≥ 7.0 (V100 / T4 / RTX 20/30/40 серии; 50-я серия пока поддерживается не полностью); для GPU-ускорения нужны CUDA 11.8 + cuDNN 8.9 + TensorRT 8.6
- **Как установить**:

  ```bash
  pip install paddlex paddlepaddle          # GPU-версия: paddlepaddle-gpu
  paddlex --serve --pipeline PP-StructureV3.yaml --port 8080
  ```

  Затем добавьте в `config.json` одну строку:

  ```json
  {
    "providers": {
      "paddle": { "baseUrl": "http://127.0.0.1:8080" }
    }
  }
  ```

#### Уровень 4: vLLM + Qwen2.5-VL (универсальная визуальная VLM)

- **Что умеет**: вопросы по изображению, распознавание рукописного текста, распознавание формул, описание сложных сцен естественным языком — задачи «понимания», с которыми не справляется PaddleX
- **Нужно ли поднимать сервис**: **да**, собственный vLLM-сервис вывода
- **Минимальные требования к ресурсам** (по замерам):

  | Режим | Минимум | Рекомендуется | Примечание |
  |---|---|---|---|
  | GPU 7B полная точность (FP16) | 16 ГБ VRAM | **24 ГБ VRAM** (RTX 3090 / 4090 / A5000) | Веса модели ~15–16 ГБ + KV cache, vLLM по умолчанию занимает 90% видеопамяти |
  | GPU 7B квантованная (INT8/AWQ) | 10–12 ГБ VRAM | 16 ГБ VRAM | Квантованная версия помещается в RTX 4080 / 4060 Ti 16 ГБ |
  | GPU лёгкая 3B | 6–8 ГБ VRAM | GTX 1660 / 3060 6–8 ГБ | FP16 ~6–8 ГБ, INT4 ~3–4 ГБ — «золотая середина» для индивидуальных разработчиков |
  | CPU-режим | Не рекомендуется | — | Запустится, но в 5–10 раз медленнее; для продакшена берите GPU |
  | ОЗУ | 16 ГБ | 16–32 ГБ | — |
  | Диск | ~14 ГБ (веса 7B) | — | 3B ~6 ГБ |
  | Требования к CUDA | Compute Capability ≥ 7.0 | — | От Tesla T4 (7.5) и выше: V100 / A100 / RTX 30/40 серии |

- **Как установить**:
  ```bash
  pip install vllm
  vllm serve Qwen/Qwen2.5-VL-7B-Instruct --port 8000
  # Появится «Uvicorn running on http://0.0.0.0:8000» — сервис готов
  ```
  Дополнительные параметры (выбор GPU / квантованные версии / лимит параллелизма) см. в [официальной документации vLLM](https://docs.vllm.ai). Затем добавьте в `config.json`:

  ```json
  {
    "providers": {
      "vlm": { "baseUrl": "http://127.0.0.1:8000" }
    }
  }
  ```

##### Продвинутый: Unlimited-OCR для парсинга длинных документов (самохостинг на SGLang/vLLM)

По умолчанию на уровне 4 Qwen2.5-VL — это универсальная VLM (сильна в VQA / описании сцен). Если же вам нужен **OCR длинных документов / сложные таблицы / разбор многостраничного PDF за один проход** (тысячи — десятки тысяч знаков на изображение), переключитесь на [baidu/Unlimited-OCR](https://github.com/baidu/Unlimited-OCR) (MIT; продвигает линию Deepseek-OCR на шаг вперёд). Модель **обучена с использованием всего 2-словного промпта** `document parsing.`; длинный вывод защищён от деградации через `custom_logit_processor` (DeepseekOCRNoRepeatNGram) — это другой класс инструмента, нежели Qwen2.5-VL.

**Когда настроен Unlimited-OCR, провайдер `vlm` автоматически разблокирует все 4 таски** (extract-text / extract-table / describe-image / analyze-chart), причём `extract-text` / `extract-table` идут по контракту короткого промпта для одиночного изображения из README, а `describe-image` (VQA) и `analyze-chart` (JSON-извлечение) остаются на исходном длинном промпте — переопределять промпт вручную не нужно, MCP выбирает по модели.

**Развёртывание (SGLang, рекомендуется — поддерживает полный набор возможностей `custom_logit_processor`)**:

```bash
# Pull образа (детали — в README Unlimited-OCR)
docker pull vllm/vllm-openai:unlimited-ocr          # по умолчанию CUDA 13.0
# Для GPU Hopper используйте cu129:
# docker pull vllm/vllm-openai:unlimited-ocr-cu129

# Запуск сервера SGLang (объяснение ключей — в секции «SGLang» README Unlimited-OCR)
python -m sglang.launch_server \
  --model baidu/Unlimited-OCR \
  --served-model-name Unlimited-OCR \
  --attention-backend fa3 --page-size 1 \
  --mem-fraction-static 0.8 --context-length 32768 \
  --enable-custom-logit-processor \
  --host 0.0.0.0 --port 10000
```

`custom_logit_processor` — это сериализованная строка, которую возвращает Python-метод `DeepseekOCRNoRepeatNGramLogitProcessor.to_str()` (формат сериализации внутренний для SGLang, синтезировать из TS нельзя). **Выполните один раз при развёртывании** и вставьте строку в `config.json`:

```bash
# В Python-окружении с установленным sglang запустите однострочник:
python -c "from sglang.srt.sampling.custom_logit_processor import DeepseekOCRNoRepeatNGramLogitProcessor as P; print(P.to_str())"
# Напечатает длинную строку — скопируйте её в поле custom_logit_processor ниже
```

**Пример config.json** (переключаем `vlm` на Unlimited-OCR + настраиваем расширение `extra_body`):

```json
{
  "providers": {
    "vlm": {
      "baseUrl": "http://127.0.0.1:10000",
      "models": { "default": "Unlimited-OCR" },
      "extra_body": {
        "images_config": { "image_mode": "gundam" },
        "custom_params": { "ngram_size": 35, "window_size": 128 },
        "custom_logit_processor": "<строка, напечатанная python -c выше>",
        "skip_special_tokens": false
      }
    }
  }
}
```

Назначение полей (все верхнего уровня, принимаются OpenAI-совместимым API SGLang; MCP просто разворачивает их через `Object.assign` в тело fetch):

| Поле | Значение | Примечания |
|---|---|---|
| `images_config.image_mode` | `gundam` / `base` | Высокая точность на одном изображении: `gundam` (base_size=1024, image_size=640, crop_mode=true); для многостраничного PDF: `base` (image_size=1024, crop_mode=false). media-gen-mcp работает по **контракту одного изображения**, по умолчанию оптимален `gundam` |
| `custom_params.ngram_size` | `35` (рекомендуется) | Длина NoRepeatNGram; 35 — рекомендованное в README значение |
| `custom_params.window_size` | `128` (одно изображение) / `1024` (много страниц) | Одно изображение: 128; для контракта одного изображения в media-gen-mcp рекомендуется 128 |
| `custom_logit_processor` | вывод `.to_str()` из Python | Обязательно (без него длинный вывод деградирует повторами); TS не может синтезировать — нужно один раз запустить Python и забрать строку |
| `skip_special_tokens` | `false` | Для OCR-задач необходимо сохранить специальные токены, не пропускайте их |

> ⚠️ **Таск-гейтинг (важно)**: `extra_body` (включая `custom_logit_processor` / `skip_special_tokens:false` / `images_config.image_mode:gundam`) разворачивается в тело fetch только для `extract-text` / `extract-table` (OCR-путь) — `describe-image` (VQA) и `analyze-chart` (JSON-извлечение) **этих полей не получают**. Причина: NoRepeatNGram (ngram_size=35) подавляет оправданные повторы слов в VQA-описаниях; `skip_special_tokens:false` протекает OCR-структурными токенами в description и ломает `JSON.parse` в `analyze-chart`; `image_mode:gundam` (crop_mode=true) режет целое изображение на фрагменты и разрушает целостное понимание сцены в VQA. Это симметричная пара к model-aware гейтингу коротких промптов (`promptForUnlimited`) — `describe-image` / `analyze-chart` остаются и с исходным длинным промптом, и с чистым телом. Если нужно принудительно прокинуть расширенные поля на `describe-image` / `analyze-chart`, используйте per-call параметр `extra` (аргумент `extra` инструментов `extract_text` / `extract_table` / `describe_image` / `analyze_chart`) — на него таск-гейтинг не распространяется.

**Вызов**: явно передайте `provider=vlm` в `extract_text` (иначе сработает defaultVisionProvider=tesseract):

```
extract_text(image="data:image/png;base64,...", provider="vlm")
```

**Важные ограничения**:

- **Режим без стриминга**: media-gen-mcp использует **non-stream** endpoint `/v1/chat/completions` от vLLM/SGLang (JSON возвращается целиком за раз) — подходит для одностраничных / средне-коротких документов. В `infer.py` у Unlimited-OCR по умолчанию `stream:true` — **НЕ копируйте `stream:true` в `extra_body`** — MCP это обнаружит и отреджектит с подсказкой «remove extra.stream». Для очень длинных PDF рекомендуется сначала [нарезать страницы через PyMuPDF](https://github.com/baidu/Unlimited-OCR#transformers) (в README есть сниппет `pdf_to_images`), а затем вызывать `extract_text` по каждой странице — независимые запросы на каждую страницу естественно избавляют от слишком длинных выводов.
- **Таймаут сервера**: длинные документы требуют времени на генерацию; если дефолтного vLLM-таймаута в 60 с не хватает, поднимите `REQUEST_TIMEOUT` в SGLang или `--timeout-keepalive` в vLLM.
- **Порог по GPU**: 16–24 ГБ VRAM (как на уровне 4); если машина не тянет — оставайтесь на цепочке paddle(10)/glm-vision(9).

**Лицензия**: [MIT](https://github.com/baidu/Unlimited-OCR/blob/main/LICENSE) (соответствует чисто-бесплатной позиции; тот же класс, что Apache-2.0 у Qwen; коммерческое использование разрешено).

#### Сводная таблица четырёх уровней

| Уровень | Поднимать сервис? | Порог ресурсов | Китайский | Таблицы | Вопросы по картинке | License / источник |
|---|---|---|---|---|---|---|
| **1 по умолчанию** (tesseract) | Нет | Ноль (чистый CPU WASM) | Средне | ❌ | ❌ | Apache 2.0 (самохостинг) |
| **2 Zhipu GLM-4.6V-Flash** | Нет (облачный API) | Ноль (чистый HTTP) | ✅ SOTA | ✅ | ✅ | key Zhipu от пользователя (навсегда бесплатно) |
| **3 PaddleX** | Да | GPU 12 ГБ или CPU 4 ядра 8 ГБ | ✅ SOTA | ✅ | ❌ | Apache 2.0 (самохостинг) |
| **4 vLLM Qwen2.5-VL** | Да | **GPU 16–24 ГБ** (CPU не годится) | ✅ | Средне | ✅ | Apache 2.0 (самохостинг) |

> Три самохостинг-уровня (1/3/4) намеренно выбраны только на **Apache 2.0** движках (tesseract.js + PaddleOCR + Qwen2.5-VL), чтобы обойти ловушки AGPL / GPL / коммерческих разрешений. **Можно сразу использовать в коммерческих продуктах на предприятии.** Уровень 2 (Zhipu) — облачный API (GLM-4.6V-Flash навсегда бесплатен, key предоставляет пользователь), не самохостинг; подходит тем, кто не хочет разворачивать сервер, но нуждается в китайском SOTA + VQA.

---

### III. Автоматический fallback (настроил и забыл)

- **Сторона генерации**: Agnes ↔ Zhipu, при сбое одного автоматически переключается на другого (мягкое переключение после неудач в течение 60 секунд — вам не нужно перезапускать MCP или менять конфигурацию)
- **Сторона распознавания**: лёгкий движок по умолчанию (фолбэк внутри процесса) → Zhipu GLM (облако) → PaddleX → vLLM, автоматическая деградация по возможностям
- **Единственное исключение**: при опросе статуса видео **переключение не выполняется** (чтобы не получить чужой ошибочный результат)
- Что нужно сделать вам: настроить API Key для двух провайдеров генерации + опционально поднять один уровень сервиса распознавания — остальное Claude возьмёт на себя

> Ваша машина не тянет PaddleX или vLLM? **Продолжайте использовать лёгкий движок по умолчанию** — MCP не упадёт из-за отсутствия локального сервиса, просто будут недоступны SOTA на китайском / таблицы / вопросы по изображению, всё остальное продолжит работать как обычно.

---

## Частые вопросы

**В: Можно ли пользоваться, ничего не устанавливая?**
О: Да. После установки MCP у вас сразу есть рисование диаграмм / карточек / QR-кодов / формул / графиков данных + английский / капчи OCR — всё работает локально, без сети.

**В: Китайский распознаётся с «кракозябрами»?**
О: Лёгкий движок по умолчанию годится для английского / цифр / простых документов, но точность на китайском средняя. Для SOTA на китайском поднимите свой PaddleX (GPU 12 ГБ или CPU 4 ядра 8 ГБ), подробности см. в разделе [Подробно о конфигурации](#подробно-о-конфигурации).

**В: Сколько ждать AI-видео?**
О: 5-секундное видео — примерно 1–3 минуты, 18-секундное — возможно 5–10 минут. Генерация идёт асинхронно в фоне, по завершении вы получаете уведомление; то, что оценивается ≤60 секунд, дождётся синхронно.

**В: Мой RTX 3060 потянет распознавание таблиц?**
О: Да. Минимум для GPU-режима PaddleX — 12 ГБ VRAM (RTX 3060 12 ГБ как раз подходит), для CPU-режима хватит 4 ядер + 8 ГБ ОЗУ (медленнее в 3–5 раз). Подробнее см. в [Подробно о конфигурации](#подробно-о-конфигурации).

**В: Китайский / emoji / градиенты рендерятся нормально?**
О: Да. В карточках через встроенные китайские шрифты + движок вёрстки полностью автоматически поддерживаются китайский, японские иероглифы, цветные emoji, градиентные заголовки и эффекты свечения — никакой дополнительной настройки шрифтов не требуется.

**В: Поддерживается ли Mermaid?**
О: Нет (требуется браузер). Используйте взамен D2 или Graphviz — возможности те же, но стабильнее, и векторный вывод.

**В: Попадаю на rate limit (429)?**
О: На бесплатном уровне есть лимит запросов в минуту. После настройки двух провайдеров (Agnes + Zhipu) переключение происходит автоматически, вы почти ничего не замечаете.

**В: Какие ограничения по количеству кадров в видео?**
О: Уменьшаются с ростом разрешения — 1080p ≤ 241 кадр (~10 секунд), 720p — до 441 кадра (~18 секунд). Можете попросить Claude проверить актуальные ограничения.

**В: npx не подключается / медленно запускается?**
О: Подойдёт и глобальная установка: сначала `npm i -g media-gen-mcp-server`, затем `claude mcp add media-gen-mcp -s user "$(which media-gen-mcp-server)"`.

**В: Можно ли использовать чувствительные слова / оружие / военную тематику?**
О: Слова про реальное оружие активируют фильтр контента. Замените их на научно-фантастические термины (например, «боевой экзоскелет будущего», «меха») — это обойдёт фильтр, эффект тот же.

**В: Может ли Claude выбрать не тот инструмент?(напр., сгенерировать изображение, когда вы просите открытку)**
О: Маршрутизация таких неоднозначных запросов уже настроена — «сделай открытку / постер / OG-изображение», «считай данные из этого графика», «сделай анимацию интро продукта», «нарисуй архитектурную блок-схему / flowchart», «визуализируй эти данные» и тому подобное теперь автоматически направляются в нужный специализированный инструмент, без ручной коррекции. Вы также можете явно назвать инструмент в запросе.

---

## Кому это подходит

- **Активные пользователи Claude Code** — те, кто несколько раз в неделю занимается задачами с изображениями и не хочет для каждой ставить отдельный MCP и запоминать свой набор параметров.
- **Разработчики, пишущие техническую документацию / блоги** — кому регулярно нужны архитектурные диаграммы, диаграммы последовательности, ER-схемы, графики данных, формулы, и кто не хочет выходить из рабочего процесса.
- **Индивидуальные разработчики / самостоятельные продукты** — те, кто следит за расходами (всё бесплатно) и предсказуемостью (одинаковый ввод → одинаковый вывод), и кто не хочет поднимать отдельный бэкенд только ради задач с изображениями.
- **Аналитики данных / финансы / юристы** — сценарий в обе стороны: превратить данные в графики, а из скриншотов / счетов / **PDF-отчётов / договоров** обратно извлечь точки данных (водяные знаки / красные печати можно игнорировать, двухколоночные статьи объединяются в порядке чтения).
- **Образование / академическая сфера** — студенты извлекают текст из скриншотов лекций / отсканированных конспектов / PDF-статей, объединяют двухколоночные статьи в связный текст, задают вопросы по данным из диаграмм; преподаватели превращают сканы бумажных экзаменационных работ в редактируемый текст.
- **Маркетинг / контент-мейкеры / авторы публичных аккаунтов** — карточки для шеринга / OG-изображения / постеры / QR-коды: китайский + цветные emoji + градиенты работают из коробки.

> **Не очень подходит**: пользователям без Claude Code; командам, которым нужно одно конкретное умение и у которых уже выстроен пайплайн; сценариям, требующим платных коммерческих моделей / обучения и тонкой настройки / OCR видео в реальном времени (это выходит за рамки бесплатного MCP).

---

## 💝 Поддержите автора

Если media-gen-mcp вам помог, угостите автора кофе ☕

<div align="center">

WeChat | Alipay
:-: | :-:
<img src="doc/support-wechat.jpg" height="200" alt="WeChat"> | <img src="doc/support-alipay.jpg" height="200" alt="Alipay">

</div>

Или ⭐ [Star](../../stargazers) / [Issue](../../issues) / [PR](../../pulls) — любая форма поддержки приветствуется.

---

## License

**MIT** — основной код используйте как угодно.

Стек зависимостей для распознавания полностью на **Apache 2.0** (tesseract.js + PaddleOCR + Qwen2.5-VL) — коммерческое использование на предприятии без лицензионных рисков.

---

> Технические детали: провайдеры и движки — подключаемые; структурные инструменты с одинаковым вводом дают одинаковый вывод и годятся для git; при сбое провайдер переключается автоматически. Список контрибьюторов см. в `CONTRIBUTING.md`, полную документацию — в каталоге `docs/`.

<p align="center">
  <sub>Built for everyone who'd rather <strong>say it</strong> than <strong>script it</strong>.</sub><br>
  <sub>Установите один раз — и все задачи с изображениями станут делом одной фразы.</sub>
</p>
