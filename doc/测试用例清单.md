# media-gen-mcp 测试用例清单(21 工具矩阵 + 审美改动 + gap)

> 依据:22 工具现状 + 2026-08-12 审美优化改动(parseHex/Geist/teal/chart/a11y/MP4)+ extract_image_meta(P0-6 新增)+ 现有测试体系。工作流按本清单逐个测试 + 白盒诊断。
>
> 测试体系三层:① `test/*.mjs`(契约/端到端,11 文件)② `dist-test/*.test.js`(.ts 单元编译,3 文件)③ `test/golden/`(byte-identical 黄金,8 工具 fixture)。

---

## 0. 测试命令速查

```bash
npm test                    # 全量:build + build:tests + node --test(dist-test + test/*.mjs)+ check-error-text + check-schema + check-render-output(qr/url.png strict)
npm run render:golden       # 重渲染所有 golden expected(改产物后跑)
node --test test/<file>.mjs # 单文件
node --test --test-name-pattern="MP4" test/check-render-output.test.mjs  # 单用例
```

---

## 1. 21 工具测试矩阵

| # | 工具 | 现有测试 | golden fixture | 状态 | gap / 备注 |
|---|---|---|---|---|---|
| 1 | generate_image | check-render-output(产物守门) | — | ⚠ 弱 | 无 callTool 功能测试(provider 调用 + 网络);仅产物校验 |
| 2 | create_video | — | — | ❌ gap | 无测试(provider 异步 + 网络) |
| 3 | get_video | — | — | ❌ gap | 无测试(create_video 伴侣) |
| 4 | extract_text | — | — | ❌ gap | 无 callTool(pares5 本地 _test_pares5.mjs 不入库) |
| 5 | extract_table | — | — | ❌ gap | 无测试 |
| 6 | list_models | — | — | ❌ gap | 无测试(自省) |
| 7 | list_vision_capabilities | — | — | ❌ gap | 无测试(自省) |
| 8 | generate_diagram | user-journey.golden(d2/graphviz-svg)+ integration.error-format | diagram/d2-basic + graphviz-basic | ✅ 强 | D2/Graphviz 双引擎 + 错误路由覆盖 |
| 9 | generate_interactive_diagram | interactive-html-contract + user-journey + golden | interactive-html/architecture.golden.html | ✅ 强 | S1-S5 契约 + 双调色板 + golden |
| 10 | generate_nested_diagram | nested-html.contract + manifest-layers.contract + manifest-validate.unit + nested-viewer.e2e + user-journey + golden | nested-diagram/ecommerce.golden.html | ✅ 最强 | 6 维契约 + INV-2 e2e + DFS 序 + S_NESTED |
| 11 | generate_qrcode | user-journey.golden(qr-basic-svg + qr-url-png jsQR 解码)+ check-render-output | qr/basic.svg + url.png | ✅ 强 | 编码往返 + 产物守门 |
| 12 | generate_chart | user-journey.golden(chart-bar-svg)+ check-render-output + integration.error-format | chart/bar-basic.svg | ✅ 强 | Vega compile + arc/theta 引导 + 审美 config |
| 13 | generate_formula | user-journey.golden(formula-basic-svg MathJax ID normalize)+ check-render-output | formula/basic.svg | ✅ 强 | MathJax ID 稳定性 |
| 14 | generate_icon | — | —(golden skip,网络) | ❌ gap | 无测试(Iconify API 网络,byte 不稳定) |
| 15 | generate_card | user-journey.golden(card-cjk-svg Noto Sans SC 0-fetch)+ check-render-output | card/cjk-og.svg | ✅ 强 | CJK 离线 + 审美(parseHex/Geist/teal) |
| 16 | render_svg | user-journey.golden(rsvg-passthrough)+ integration.error-format + golden | render-svg/passthrough.svg | ✅ 强 | resvg passthrough + 错误路由 |
| 17 | render_video | — | — | ❌ gap | 无 callTool 测试(Chrome+ffmpeg 慢);output-check MP4 守门刚修 |
| 18 | extract_pdf | — | — | ❌ gap | 无 callTool(pares6 本地 _test_*.mjs 不入库) |
| 19 | get_pdf | — | — | ❌ gap | 无测试(extract_pdf 伴侣) |
| 20 | extract_image_meta | 手动验证(img_26916486-507.png 完整提取 ComfyUI 正负向 prompt+模型+采样+seed+尺寸) | — | ⚠ 手动 | **新增 P0-6**:纯本地 PNG chunks 解析(ComfyUI/A1111 + NaN 清洗),手动验证通过;待加单元测试(dist-test) |

**覆盖统计**(22 工具):✅ 强覆盖 9 / ⚠ 弱 1 + 手动验证 1(extract_image_meta)/ ❌ gap 11(gap 多为 AI 生成+识别+自省,provider/网络依赖,本地 _test_*.mjs 按约定不入库)。

---

## 2. 审美优化改动测试用例(2026-08-12,本次重点验证)

| 改动 | 文件 | 测试用例 | 验证方法 | 状态 |
|---|---|---|---|---|
| **parseHex 正则调序**(P0) | color-utils.ts:8/18 + card.ts:152 | parseHex('#0f172a')→[0,23,42](非 [0,255,11]);isLightBg('#0f172a')→false(非 true);默认暗底卡正文 #94a3b8(7:1,非 #475569 2.36:1) | `node -e "import dist/color-utils.js; 测 parseHex/isLightBg"` + golden card/cjk-og.svg 重跑 | ✅ 已验证 |
| **render_video wrapper**(P0) | render-video.ts:263 | 完整 HTML 文档(<!doctype html>)原样透传,片段包裹;不再误裹 | 单元:regex 测 `<!doctype html>` vs `<div>` | ✅ 已验证 |
| **Geist bundle + ss03**(P1) | card.ts + @fontsource/geist-sans | 默认 fontFamily Geist;loadGeistFont 命中本地 bundle(400/700 存在,不走 CDN);Satori 接受 fontFeatureSettings ss03;viewer font 栈前置 Geist | `require.resolve("@fontsource/geist-sans")` + 渲染默认卡无 fetch + golden card 重跑 | ✅ 已验证 |
| **indigo→teal accent**(P1) | theme.ts + card.ts:547 + animations.ts:69 + index.ts:442 | 亮 #0d9488 / 暗 #2dd4bf(非 #6366f1/#818cf8);3 工具(card+2 viewer)同改 | grep `#6366f1` 残留 0 + 渲染默认卡 teal + golden 重跑 | ✅ 已验证 |
| **card 样式契约**(P1) | card.ts:618/620/640/642 | title fontWeight 600(非 700)+ letterSpacing -字号×4%(76→-3)+ subNode 500 + body lineHeight 1.5 + fontFeatureSettings ss03 | 渲染默认卡视觉 + golden cjk-og 重跑 | ✅ 已验证 |
| **chart Wong CVD-safe config**(P1) | chart.ts renderChart | 注入 config.range.category(Wong 8 色)+ config.font(Geist)+ config.mark.color(#0072B2);用户 spec.config 永赢 | golden chart/bar-basic.svg 重跑(font Geist + Wong 色命中) | ✅ 已验证 |
| **a11y**(P1) | nested-template.ts + viewer-stack.ts + viewer-min.ts | nested:aria-live 容器 + showView 切层播报 + 焦点回退;两 viewer:motion aria-pressed | 读 HTML 源码 + golden nested/interactive 重跑 | ✅ 已验证 |
| **MP4 flaky timeout**(本次) | output-checker.ts:271 + 985 | probeContainer timeout 5s→15s(CPU 过载防 flaky)+ early return 设 mp4HasVideoTrack(防御 undefined) | `node --test test/check-render-output.test.mjs` 全 ✔ | ✅ 已修复 |

---

## 3. 测试 gap(无 callTool 测试的 11 工具)

| 工具 | gap 原因 | 风险 | 建议 |
|---|---|---|---|
| create_video / get_video | provider 异步 + 网络(Agnes/智谱) | 高(核心功能无测) | mock provider 单元(_test_*.mjs 入库或 dist-test) |
| extract_text / extract_table | tesseract/paddle/vlm 依赖 | 中(本地 _test_pares5.mjs 有,不入库) | _test_pares5.mjs 整理入库 |
| extract_pdf / get_pdf | pdfjs + 异步 + provider | 中(本地 _test_pares6.mjs 有) | _test_pares6.mjs 整理入库 |
| list_models / list_vision_capabilities | 自省(读 config) | 低(简单) | 加最小单元 |
| generate_image | provider + 网络 | 中 | mock provider |
| generate_icon | Iconify API 网络 | 低(简单) | mock fetch |
| render_video | Chrome + ffmpeg 慢 | 中(output-check 守门已修) | 加最小 HTML→MP4 smoke |

---

## 4. 工作流测试执行清单(逐个白盒验证)

工作流 agent 按下表逐个跑 + 白盒诊断(读源码 + 实际跑命令,拒绝黑盒猜测):

| 组 | 测试文件 | 命令 | 白盒要点 |
|---|---|---|---|
| A 契约-嵌套图 | nested-html.contract + manifest-layers.contract + manifest-validate.unit + nested-viewer.e2e | `node --test test/nested-html.contract.test.mjs test/manifest-layers.contract.test.mjs test/manifest-validate.unit.test.mjs test/nested-viewer.e2e.test.mjs` | INV-2 防 hash 逃树 + DFS 序 + a11y 改动后契约仍持 |
| B 契约-交互图 | interactive-html-contract | `node --test test/interactive-html-contract.test.mjs` | S1-S5 + 双调色板 + theme teal 改动后 |
| C 端到端 golden | user-journey.golden | `node --test test/user-journey.golden.mjs` | chart/card/qr/formula/diagram SVG byte ≡ golden(审美改动后) |
| D 产物守门 | check-render-output | `node --test test/check-render-output.test.mjs` | MP4 timeout 修复 + card/chart PNG/SVG PASS |
| E 错误路由 | error-format(unit + integration + user-journey + smoke) | `node --test test/unit.error-format.mjs test/integration.error-format.mjs test/user-journey.error-format.mjs test/error-format.smoke.mjs` | [tool] 前缀 + S\d+ + 引擎路由 |
| F 单元-determinism/golden-helpers | dist-test | `node --test dist-test/determinism.test.js dist-test/golden-helpers.unit.test.js dist-test/golden.test.js` | 确定性 + golden helper + golden 比对 |
| G 校验脚本 | check-error-text + check-schema + check-render-output(qr strict) | `node scripts/check-error-text.mjs && node scripts/check-schema.mjs && MEDIA_GEN_CHECK_PROFILE=strict node scripts/check-render-output.mjs test/golden/expected/qr/url.png` | G1/G2/G3 + 错误文本 + qr strict |

每组 agent:跑命令 → 若 fail,白盒读源码 + 复现 + 诊断根因(不猜)→ 报告(文件:行 + 根因 + 修复建议)。
