# 画布式 AIGC 管线软件调研 与 media-gen-mcp 融合可行性报告

> 版本:v1.0(2026-09-24)|调研窗口截至 2026-09-24
> 性质:**可行性分析报告**(用户裁决主诉求:装「有画布+管线管理+导演台」的软件;评估本项目 MCP 能否作为基底/融合进去;多 Agent 隔离并发深析;白盒源码分析)
> 方法:5 路并行调研(品类发现/ComfyUI 白盒/开源竞品白盒/商业导演台解构/MCP 融合路径)+ 本地白盒(media-gen-mcp 源码亲析,作者级)
> 关联:[[director-stage-project]](用户自建导演台愿景——本报告为其「造 vs 融 vs 借」决策输入)、[[comfyui-workstation-project]](本地工作站规划)

---

## 0. 执行摘要(先给结论;按用户修正标准 C1 管线完整度/C2 渠道整合/C3 二开许可)

**总裁定:GO——首选 NodeTool 二开,渠道层直接搬我们的 MIT 代码。**

1. **底座裁定:NodeTool(唯一同时满足全部硬标准的候选)**——①**端到端管线 6/6 ✔**(源码级核验):生图(fal 216 t2i)→图生视频(i2v 220+首尾帧 28 节点)→视频拼接(Concat+RenderTimeline WebGPU GPU 合成,mp4/webm/mov-alpha)→音频生成(TTS 34/Suno 音乐)→音频拼接混音(AudioMixer/Concat/Overlay 全套)→成片导出,**每环多 provider 冗余**;②**导演台是真功能且闭环**:storyboard 节点链(LoadStoryboard→…→RenderClips→AssembleTimeline)+timeline 包(175+ commits/~100 测试/290 UI 组件完整多轨剪辑器)——正是用户要的「完整管线管理台」;③TS monorepo 与我们同栈,极活跃(09-23 仍 push);④**已内置 ComfyUI 集成节点**=本地 GPU 引擎顺手收编。
2. **渠道整合三路径全通(C2 任一即达标)**:a)原生 MCP client(stdio 挂入 27 工具,**0 改造**);b)**我们的渠道层是 MIT,可直接流入其 TS 节点体系**(同语言代码搬运);c)factory+manifest 范式(**higgsfield-factory 仅 89 行**——9 渠道≈1 base+1 factory+1 份 manifest,我们的 wire 契约沉淀直接转写,近零代码)。
3. **多 Agent 隔离并发(白盒终论)**:media-gen-mcp 三层状态模型(进程内存/跨进程文件/账号级外部态)——进程级竞态全部在文档化可接受窗口;真正需全局仲裁的只有**账号级配额挤兑(HF ZeroGPU/pixverse 积分)与 gemini 单会话串行**,两者恰好都该建在画布调度层(唯一能看到全局的位置;NodeTool Actor DAG=现成的全局调度者)。**融合不仅不恶化并发,反而把调度责任上收到正确层**。
4. **主要风险两条**:🔴 RenderTimeline 的 WebGPU 合成在 Monterey(macOS 12,Chrome 要求 13+)大概率走 ffmpeg 回退丢特效——第一步实测项;AGPL-3.0 仅在「闭源商用分发」时是边界(自用/开源二开无碍;且我们的渠道层保持独立 MIT 服务进程,迁移门永远开着)。
5. **被证伪/降位的候选**:ComfyUI(13.5 万★,作底座 4.5/10——拼接/音频/时间线弱,不是完整管控台;**归位为 NodeTool 之下的本地推理引擎**);Flowy 开源版已 404 消失;Vibe-Workflow 引擎全在闭源 MuAPI 云;Diffusion Studio 停更+v2 商用收费;hypit/waoowaoo 伪开源(NOASSERTION)。

> 报告导航:§1 修正后标准 / §2 品类全景(27+ 家+死坑)/ §3 开源白盒(ComfyUI+四竞品源码级)/ §4 商业导演台功能基线(14 MUST+14 PLUS)/ §5 MCP 融合四模式+八坑 / §6 media-gen-mcp 白盒并发(状态三层模型,本报告独家)/ §7 管线核验+路线矩阵+四层调度栈 / §8 风险 / §9 结论。

---

## 1. 需求定义(用户主诉求解构)

> 🔴 **用户裁决修正(2026-09-24,报告初稿后)**:评估方向不得偏差——
> ①**对接方式不限**:不是「画布必须消费 MCP 协议」;对开源画布**二次开发**、或任何其它方式都可以,只要能把 **MCP 已接的渠道能力**(9 生成渠道+识别+确定性渲染,即「先期渠道预接入」的成果)整合进画布;
> ②**画布两条硬标准**:标准一=**能力要强**——最希望是**完整管线**:生图→图生视频→视频拼接→音频→音频拼接……好用的完整管线管理台/管控台(+导演台类附加能力);标准二=**与我们已接渠道的对接可行性**;
> ③**可基于开源软件二次开发**(明示 OK)——许可证权重重排:AGPL 从「红线」降为「仅闭源商用分发才受限」,自用/开源二开完全可接受。

据此,评估标准从初稿的「MCP 协议直连优先」修正为:

| 标准 | 权重 | 说明 |
|---|---|---|
| **C1 端到端管线完整度** | 最高 | 六环节逐项核验:生图→图生视频→视频拼接/合成→音频生成(TTS/音效)→音频拼接/混音→成片导出;导演台(分镜/镜头管理)为加分项 |
| **C2 渠道整合可行性** | 高 | 三条整合路径任一即可:a)画布消费我们的 MCP(零改造/桥/自建节点);b)**二开:把我们的渠道代码(MIT)嵌入或对接其节点体系**(我们是 MIT=可流入任何底座,含 AGPL fork);c)按其 provider 抽象重写渠道适配层(我们已有 9 家渠道的完整 wire 契约沉淀,重写成本低) |
| **C3 二开许可** | 中 | 二开自用/开源发布=几乎无约束;仅「闭源商用分发」触发 GPL/AGPL 边界(进程隔离/API 调用仍安全) |
| C4 架构健康度 | 中 | 活跃度/技术栈与我们生态的亲和(TS 优先)/并发模型 |

## 2. 品类全景与候选清单(调研 agent:disc-landscape,已回收)

> 四通道发现:①ai-bot.cn 榜单(命中 Seko/updream/白日梦)②中文媒体(命中 updream Skill 画布/SkyProduction/LibTV 细节)③英文社区 Reddit/Medium/YouTube(命中 Wireflow/Astorie/Comfy Canvas)④GitHub search/awesome(命中 TongFlow/Velorn/Mix-Studio/Vibe-Workflow/open-storyboard-canvas/Inline Studio)。stars/push/license 均经 GitHub API 实时核验(2026-09-24)。

### 2.1 候选总表(★=开源,节选关键列;全表 27+ 家见 agent 原始回执)

| 名称 | 开源/license★/活跃 | 画布形态 | 管线能力 | 导演台 | 视频模型(2025-26 世代) |
|---|---|---|---|---|---|
| **ComfyUI** ★ | GPL-3.0·134,759★·09-24 当日仍在 push | 节点图 | DAG+子图+缓存+队列,生态最强 | 靠 workflow 拼装,无原生分镜 | 本地 Wan2.2/LTX-2/HunyuanVideo;官方 API Nodes 接 Seedance/Grok Imagine(Sora2 节点 2026-09 官方弃用) |
| **SwarmUI** ★ | MIT·4,597★·09-23 | 分层表单+图 | ComfyUI 后端,工作流复用 | 无 | 本地 Wan/HunyuanVideo |
| **NodeTool** ★ | AGPL·542★·09-23 | 节点画布 | "Agent-first Creative Workspace" 图/视/音/文统一 | 弱 | 本地+API 混合 |
| **TongFlow** ★ | AGPL·1,029★·09-22 | 节点画布 | 多模态编排平台 | 无 | 多模型接入 |
| **Vibe-Workflow** ★ | MIT·595★·09-17 | 无限画布 | 自称 Weavy/Krea Nodes/Flora 开源平替(出生数周) | 无 | API 模型 |
| **Velorn** ★ | GPL·486★·09-15 | 时间线 | AI-native 剪辑+生成 | 剪辑向 | 多模型 |
| **Inline Studio** ★ | 开源(Pinokio 分发) | 节点画布 | versioned takes | AI filmmaking 定位 | **唯一宣称本地跑 MiniMax H3** +Krea2/FLUX.2/Z-Image |
| **open-storyboard-canvas** ★ | 自定义 lic·353★·08-15 | 无限画布 | 自定义供应商调用 | **分镜+导演台+全景图+摄像机控制**(功能正中需求) | 按供应商接 |
| **Mix-Studio** ★ | GPL·290★·09-22 | 工作台 | 精调图像/视频工作流 | 无 | 多模型 |
| LibTV | 商业(LiblibAI) | 无限画布+节点 | 多模型聚合+缓存 | **最强导演台**(§4) | 可灵3.0 等,¥569-8499/年 |
| LTX Studio / Flora / Krea / Higgsfield / Google Flow | 商业 | 见 §4 | 见 §4 | 见 §4 | 见 §4 |
| **PixVerse Canvas** | 商业(**用户已有订阅**) | 画布 | 素材/分镜/批量任务/多模型一处管理(2026-06 发布) | 分镜整理 | PixVerse 系 |
| Morph Studio | 商业 | 单画布 | 风格迁移+故事板 | 故事板 | Wan2.6/Kling2.6 Pro/Seedance/Sora2/Veo3 聚合 |
| SkyProduction 天工 | 商业 | 短剧工作台 | Seedance2.5 全流程(0.4 元/秒价格战) | 剧本→分镜→成片 | Seedance2.5 |
| Seko / updream / 白日梦 | 商业 | 自动生成画布/Skill 画布/工作台 | 工作流市场 | 自动分镜 | 多模型 |

### 2.2 开源梯队

- **T1(事实标准)**:ComfyUI——唯一同时具备节点 DAG+缓存+13.5 万星生态+2026 新模型双向支持(本地 Wan2.2/LTX-2,云端 Seedance API Node)。任何自建导演台的底座首选。
- **T2(2026-09 仍活跃,值得跟踪)**:SwarmUI(本地视频模型易用壳)/ TongFlow / NodeTool(Agent-first)/ **Vibe-Workflow**(商业画布开源平替,最年轻)/ Velorn(AI 剪辑时间线)/ **Inline Studio**(本地 H3 电影画布)/ open-storyboard-canvas(功能最对口但 353★ 小项目)
- **T3(停滞/垂危)**:Storyboarder(2024-03 死)/ **Diffusion Studio(2025-11 停推)**/ Pixelle-Video(低活跃)

### 2.3 死坑与证伪(选型红线)

1. 🔴 **Flowy(Tread)开源版已消失**:`github.com/tread-technology/flowy` 实测 404——种子核验失败,**勿再按开源选型**(影响此前所有把 Flowy 列为候选的假设)
2. 🔴 **Puppetx 无法定位**:GitHub 无相关产品、puppetx.ai 连接失败,疑似已死
3. 🔴 **hypit / waoowaoo 伪开源**:15,677★/14,276★ 但 license 均 NOASSERTION(非 OSI)——高星≠可商用,条款必读
4. **ComfyUI Sora2 API 节点官方弃用**(2026-09,随 OpenAI 停服)——依赖单一闭源 API 节点的工作流有断供前科;反例即官方 Seedance/Grok API Node 的存在证明云端模型接入路线仍在演进
5. **行业形态结论**:商业界「导演台」=无限画布/分镜板 × 主体资产库 × 镜头级重拍 × 内置时间线四件套;**纯节点图(ComfyUI 式)被业界认为门槛过高,正被「自动铺画布」取代**(Seko 剧本→自动铺画布;LibTV Agent 一句话拍片)

## 3. 开源白盒深析

### 3.1 ComfyUI(调研 agent:wb-comfyui,已回收;zread 快照+官方文档核验)

**架构文字图**:
```
外部驱动面(无 UI 可用)
  HTTP: POST /prompt(提交即校验) GET /queue /history /object_info /view
        POST /interrupt(支持定向 prompt_id) /free /queue
  WS:   /ws?clientId=xx → status/executing/executed/progress/execution_error/execution_cached
        ↓ aiohttp(server.py)
PromptQueue(execution.py:1198,heapq+RLock+Condition,front:true 负数插队)
        ↓ 🔴 唯一 daemon worker 线程(main.py prompt_worker)——prompt 级 FIFO 串行
PromptExecutor.execute → 每 prompt 一个事件循环 → 拓扑序逐节点
  缓存: HierarchicalCache(键=输入签名,跨 prompt 复用)/LRU/RAM压力/None
节点层: nodes.py + comfy_extras/ + custom_nodes/ + comfy_api_nodes/(云 API ~35 家)
前端: Comfy-Org/ComfyUI_frontend(Vue3+litegraph→Nodes2.0,GPL-3.0-only)
```

**并发模型(多 Agent 分析核心)**:
- 🔴 **单 worker、prompt 级串行**:多 Agent 并发提交=安全排队 FIFO,不并行(currently_running 恒 1);无「请求锁」概念——消费端天生单线程;真并行只能多进程多实例(官方 --port/--cuda-device/--database-url 分离)
- 提交侧并发安全:PromptQueue RLock+Condition;/prompt 即 validate_prompt 错误 400 带 node_errors(agent 友好)
- **异步节点可重叠**:云 API 节点 return PENDING 后台化(execution.py:546-553),与后续无关节点并行——「GPU 空转等云」已缓解
- **缓存=确定性重跑的现成机制**:输入签名键,未变子图直接命中并发 execution_cached 事件——导演台「改一镜只重渲该镜」在引擎层免费获得

**视频模型原生支持**:`comfy_extras/nodes_wan.py`(1541 行 20+ 节点:WanImageToVideo/**WanFirstLastFrameToVideo 首尾帧**/WanCameraImageToVideo 运镜)+ vae2_2.py(Wan2.2 VAE);Wan2.7 走 Partner Node;LTX-2(lightricks/ 三件套);HunyuanVideo/Cosmos/Mochi;**MiniMax H3=云端 API 节点族(comfy_api_nodes/nodes_minimax.py,H3/H3 Max/Turbo 免 GPU)——主仓 core 无本地 H3 推理代码**。

**API 面**:完全 headless 可驱动(官方 basic_api_example.py 纯 urllib);双格式(API 扁平格式 vs UI 画布 JSON,后端只认 API 格式);WS 事件按 client_id 定向推送(**多 Agent 各持 clientId 可分流事件**);/object_info 全量节点元数据发现。

**许可证(GPL 边界四条,产品化关键)**:主仓+前端均 GPL-3.0;①**独立进程+HTTP/WS API 调用不构成衍生作品→自研导演台客户端闭源可行**;②fork/嵌入再分发→整体 GPL;③GPL(非 AGPL)网络服务化不分发二进制无开源义务;④custom_nodes 独立程序各自选 license。

**MCP 生态**:官方 Comfy MCP 已存在(图/视/音/3D 生成+搜模型+跑 workflow,LLM→ComfyUI 方向已被验证且官方下场);反向(节点调外部)核心无通用 HTTP 节点(刻意留白)但 custom node 10 行可发请求,异步节点后台轮询不阻塞队列。

**导演台能力差距**:画布=数据流执行图非叙事工具。可复用积木:**SubgraphManager(子图=可复用镜头模板)+ 动态子图展开(execution.py:567-601,一个节点运行时生成整段子图=「镜头数参数化」的现成钩子)**+ OUTPUT_IS_LIST 批处理 + ~40 个 blueprints 模板;缺:镜头级状态管理/时码/多版本对比/资产一致性追踪——全要导演台自建。

**双路评分**:路线 A 作为底座(fork/嵌入)**4.5/10 不推荐**(双 GPL+前端重栈+画布范式与叙事范式错位);路线 B 作为执行后端(HTTP/WS/MCP 驱动)**9/10 强烈推荐**(API 完整+缓存免费确定性重跑+35 家云节点+进程隔离绕 GPL)。

### 3.2 开源竞品白盒:NodeTool / Vibe-Workflow / Diffusion Studio / Refly(调研 agent:wb-rivals,已回收)

> 🔴 前置证伪两则(任务假设修正):①**Flowy(Tread)不存在**——GitHub/HN/gh CLI 全通道无此仓(仅 alyssaxuu/flowy 流程图库,2019 弃更),与 §2.3 印证;②**nodetool-ai/nodetool 已整体重写**——zread 索引的是旧 Python 后端,当前 main(2026-09-23 仍在 push)已是 **TypeScript monorepo(58+ npm workspace 包+turbo)**,下述以 main 为准(gh API 逐文件核实)。

#### NodeTool——四家中唯一可行底座(★★★★☆)

- **架构**:TS monorepo——packages/protocol(Zod)/kernel/execution/workflow-runner/websocket/agents/storage(SQLite+Drizzle)/auth(JWT)/vectorstore(sqlite-vec)/sandbox-compiler/deploy(RunPod/GCP)+ web(React 19+Vite+MUI+zustand)+ electron/mobile(Expo)/cli(INK)/chrome-extension
- **画布**:`@xyflow/react ~12.11.2`(React Flow v12)+ elkjs
- **管线模型**:**Actor 式并发 DAG**——Graph.fromDict() 拓扑排序+环检测;每节点一个 NodeActor **并发**跑;NodeInbox 按 handle FIFO 队列;EOS 按边计数传播;四种执行模式(buffered/streaming-output/streaming-IO/controlled);执行策略可插拔(进程内/子进程/Docker)
- **AI 调用层**:per-provider node pack(fal/replicate/**minimax**/higgsfield/reve/kie/together-nodes 等)+ **fal/replicate/kie-codegen 从 API schema 自动生成节点包**——加一个视频渠道=新建一个 packages/xxx-nodes 包(BaseNode)+注册 workspace,1 处主改动
- **🔴 外部 MCP 是原生双向能力(对本项目最大加分)**:
  - 作 **MCP client**:`packages/agents/src/tools/external-mcp-tools.ts`——用户配一个 server(stdio command/args/env/cwd 或 HTTP streamable+SSE 兜底,`${SECRET}` 加密引用),其工具成为普通 Tool 命名 `mcp_<server>_<tool>`;McpClientPool 管连接生命周期/超时/密钥脱敏。**media-gen-mcp 以 stdio 挂入即可,27 工具自动进 agent toolbelt,workflow 里 Agent 节点直接调**
  - 作 **MCP server**:`/mcp` 端点暴露 workflow/assets/nodes/jobs 工具带(OAuth+令牌粘贴)——**可反向被 Claude Code 驱动**(与用户 cc 生态天然亲和)
  - 另有 code-nodes(节点内任意代码 fetch)+ integration-nodes/comfy.ts(已内置 ComfyUI 集成)
- **并发/隔离**:多 workflow 并发=每 job 一个 WorkflowRunner(kernel);状态落 SQLite;streaming-first(WebSocket 增量推送+可取消);JWT 多用户
- **导演台语义已有包**:packages/timeline、packages/storyboard、packages/image-editor
- **License/活跃**:🔴 **AGPL-3.0**(传染性强,闭源商用不可行);2026-09-23 仍在 push,极活跃

#### Vibe-Workflow——开源外壳+闭源引擎(★☆☆☆☆,不可作底座)

MIT/595★,Next.js 16+React Flow v11+Python FastAPI;🔴 **致命伤(源码证据)**:`server/app/utils/workflow_helper.py` 所有核心操作(create/run/run-node/node-schemas/publish)全部 `proxy_request_helper` 转发到 `https://api.muapi.ai/workflow/*`,`.env.example` 仅 MU_API_KEY——**DAG 执行引擎、节点 schema、run 状态全在 MuAPI 闭源云端,本仓库只有画布 UI+代理层**,"self-hostable" 名不副实(MuAPI 付费聚合服务的营销载体)。仅可参考其单节点试跑 UX(`POST /{workflow_id}/node/{node_id}/run`)。

#### Diffusion Studio——不是画布,是浏览器合成引擎(★★☆☆☆,仅组件)

`src/`(clips/tracks/composition/encoders)证实是**代码优先的时间线/图层视频合成库**(Composition→Layer→Clip 类 Premiere 模型),WebCodecs 硬编码 100% 客户端 75KB;无节点画布/无 DAG/无 provider 抽象。AI 集成在闭源 app 侧。🔴 许可陷阱:公开仓是 v1 遗留(MPL-2.0,1245★,2025-11 停推 10 个月),**v2 起源码邀请制+Non-Commercial,商用须购许可**。对本项目定位=导演台时间线合成组件的**Remotion 替代候选**(同为浏览器渲染;但须先验证 Monterey 下 WebCodecs 可用性——与 Remotion compositor dyld 阻断同域风险)。

#### Refly——已转型+许可带钩子(★★☆☆☆)

7530★但 2026-07-29 后未 push;战略转向 "agent skills builder(Claude Code/Cursor/Codex)";画布执行模型是 **LLM 生成 workflow plan**(task+dependentTasks 的 Zod schema,每 task 由 agent 执行)——**非确定性 DAG,与导演台需要的确定性管线相悖**;MCP 是策展目录非任意挂载;Apache-2.0 附加条款(组织商用须购许可+LOGO 不可移除)=source-available。

#### 横向对比(四家)

| 维度 | NodeTool | Vibe-Workflow | Diffusion Studio | Refly |
|---|---|---|---|---|
| 形态 | 画布+DAG 运行时+agent 平台 | 画布 UI 壳 | 时间线渲染库 | 画布+agent skills |
| 管线模型 | Actor 式并发 DAG,流式 EOS | 闭源(MuAPI 云) | 图层时间线 | LLM 生成任务计划 |
| 加视频 provider | 1 个 node 包(+codegen) | 不可(依赖 MuAPI) | 无此概念 | 平台侧 |
| 外部 MCP 融合 | **原生双向**(stdio/HTTP) | 无 | 无 | 策展目录 |
| 并发/状态 | 每 job 一个 Runner+SQLite,可取消 | 云端黑盒 | 单浏览器页 | 服务端任务 |
| License | 🔴 AGPL-3.0 | MIT | v1 MPL-2/v2 商用收费 | Apache2+商用限制 |
| 底座适配度 | ★★★★☆ | ★☆☆☆☆ | ★★☆☆☆(仅组件) | ★★☆☆☆ |
| MCP 融合难度 | **低** | 极高/不可行 | 不适用 | 中 |

## 4. 商业导演台功能基线(调研 agent:disc-director,已回收)

### 4.1 逐家解构要点(10 家,来源见附录)

| 产品 | 形态 | 导演台链路完整度 | 可编程面 | 定价锚点 |
|---|---|---|---|---|
| **LTX Studio** | 剧本→分镜板→镜头卡→时间线(板+卡+线混合) | ★★★★★ 全链最完整:AI 编剧→自动分镜→角色 casting 库→运镜预设→剪辑配音→导出;LTX-2.3 竖屏/HDR/音生视频/20s 片段;**LTX-2 开源权重** | 官方 API 按秒计费($0.04-0.37/s);LTX Desktop 免费开源本地剪辑器 | Free/Lite $15/Standard $35(商用)/Pro $125 |
| **LibTV**(LiblibAI) | 无限画布+节点工作流 | ★★★★★ 中文区最强:25 宫格连贯分镜→**导演台 3D 摆位+机位预调**→多机位九宫格→剧情推演(3 秒后/5 秒前)→电影级光影校正→角色三视图;LibTV Agent 一句话全自动拍片;「**为人类和 Agent 双入口**」设计 | **OpenAPI**(建会话/生图/生视频/编辑/传文件/查任务)+可安装 Skills | 会员制,年卡 39 折比竞品低 ~70% |
| **Flora** | 无限画布+节点块(FAUNA Agent 补叙事层) | ★★★☆ 无分镜板/时间线;一致性靠节点引用+模板 | **REST API 全量开放**(Starter $18 起含 API)=可编程面最佳实践 | Free 500 积分/月~Max $200/席 |
| **Krea** | 实时画布(<50ms)+Edit 画布+时间线 | ★★★ 生成强叙事弱;片段级延长(半确定性重跑) | App 与 API 分开计费 | Basic $10/Pro $35/Max $60 |
| **Higgsfield** | Popcorn 分镜板(场景描述+4 参考图→连贯帧序列) | ★★★★ 剧本→storyboard+shot list 一体化;8 种导演模式;聚合 15 家视频模型(Seedance2.0/Kling3.0/Veo3.1/Wan2.7/Sora2) | 有 API | Basic $9~Ultra $129 积分制 |
| **Google Flow** | 素材库+镜头序列(无自由画布) | ★★★★ Ingredients 素材一致性范式+首尾帧+extend 续镜(确定性重跑雏形);Veo3.1 4K/竖屏/角色一致性 | **无 API**(仅网页) | 捆 Google AI 订阅($4.99~$249.99) |
| **即梦 Jimeng** | 故事模式+画布模式 | ★★★★ Octo(2026.4)大纲→资产→分镜→成片闭环;Seedance2.0/Seedream5.0 | 付费协议含 API(有限) | 积分制,免费 66/天;旗舰 ¥6710/年 |
| **Runway** | Workflows 节点图 | ★★★★ Gen-4 单参考图跨镜一致;Story Panels 模板;**改一节点重跑一节点=图级确定性重跑**;官方 ComfyUI Partner Nodes | API(t2v/首尾帧);$5.3B 估值+$10M 生态基金 | 积分制 |
| **Pika** | Scene Ingredients | ★★★ 局部换物(v2v)快便宜社交向,无分镜管理 | 有 API | 积分制 |
| **Veed/Opus Clip** | 自动剪辑 | ★ **只有剪辑后处理自动化,无分镜/一致性/镜头语言——与导演台是两个物种**(伪需求警示) | API | 订阅制 |

**行业动态**:Flora FAUNA Agent $52M 融资(Nike/Netflix 在用)/Runway $5.3B 估值/昆仑万维 SkyReels AI 短剧月流水超 $6500 万/LibTV「人类+Agent 双入口」——**Agent 化是 2026 导演台的共同方向**。

### 4.2 「导演台」功能基线(开源底座的需求标准)

**MUST(缺任一即不构成导演台)**:
1. 无限画布工作区(图/分镜/视频同空间) 2. 剧本→分镜自动生成且产出**结构化分镜数据**(JSON schema 非纯文本) 3. 镜头卡片管理(时长/运镜/提示词/状态/产物,增删调序) 4. 角色一致性锁(角色卡+参考图跨镜 casting) 5. 场景/道具一致性 6. 首尾帧接力(frame relay) 7. i2v 主通路+t2v 辅路 8. **逐镜确定性重跑**(改一镜只重渲该镜) 9. 运镜语言控制(预设或描述式) 10. 多模型聚合(每镜可换模型 ≥3 家) 11. 生成历史与抽卡(seed/参数留存多候选取优) 12. 简易时间线(排序/裁剪/拼接/配乐) 13. 成片导出 ≥1080p 含音频 14. 项目结构化导出(可版本化可迁移)

**PLUS(竞争力分水岭)**:
15. AI 编剧分层(大纲→分场→对白) 16. **3D 摆台空间预演**(LibTV 独门) 17. 多机位九宫格 18. 分镜组宫格批量 19. 剧情推演/角色三视图 20. 一键全自动 Agent 21. 光影色调统一校正 22. TTS 配音+音画对齐 23. **公开任务级 API** 24. 可安装 Skills/模板市场 25. 工作流 JSON 互通(ComfyUI 图) 26. 本地开源模型通路 27. 4K upscale+原生竖屏 28. 多人协作

**判定标准**:开源底座 = MUST 1-14 全量 + PLUS 至少 6 项(建议 15/16/20/23/25/26)才算「类 LibTV/LTX 导演台」。**护城河排序:结构化分镜数据 > 确定性重跑 > 一致性资产库 > 模型聚合**——注意最后两项恰是 media-gen-mcp 的强项(渠道聚合 9 家 + 确定性渲染管线),前两项是任何开源底座都缺的、需要自建的数据模型层。

## 5. MCP 融入非 LLM 宿主的路径(调研 agent:disc-mcp-bridge,已回收)

### 5.1 宿主侧 MCP 消费现状

| 宿主 | 消费方式 | 传输 | 工具→节点映射 | 成熟度/坑 |
|---|---|---|---|---|
| **n8n** | MCP Client Tool(子节点)+ 独立 **MCP Client 核心节点**(可作普通工作流步骤) | 官方**仅 SSE+Streamable HTTP,不支持 stdio**(社区节点补 stdio) | 独立节点**自动拉取 tools/list**,一节点选一工具;含 Convert to Binary(图像/音频转二进制)+Timeout 参数 | 对生图/生视频 server 是**现成消费面** |
| **Flowise** | Agent 节点挂 Custom MCP tool | stdio(仅本地)+Streamable HTTP | 动态拉取全工具再勾选 | 🔴 每次执行 ~4s 固定开销(issue #4856) |
| **Langflow** | MCP Tools 组件 | **stdio/SSE/Streamable HTTP 三种全支持**(1.7 版) | 动态发现+勾选 | 画布里传输覆盖最全 |
| **Dify** | Agent Strategy 插件 | HTTP(SSE/Streamable) | 插件转 Dify Tools | 确定性 DAG 直调非其主路径 |
| **LangGraph** | langchain-mcp-adapters(3658★)代码层注入 | stdio+HTTP 全支持 | 代码定义无画布拖拽 | 代码优先 |

**ComfyUI↔MCP 桥**:生态**几乎全是 LLM→ComfyUI 方向**(官方 comfy.org/mcp 云端+本地 comfy-mcp,含「agent 花积分前向用户确认」——与我们的 confirm-token 同构问题的官方解法;artokun/comfyui-mcp 762★ 活跃);**反向(ComfyUI 节点内消费 MCP)无成熟通用项目**——若底座选 ComfyUI,需自建 Python custom node 内嵌 MCP client。

### 5.2 MCP 规范并发语义(与本报告 §6 直接咬合)

- **stdio**:client 把 server 作子进程启动,一条管道=一个 client(spec 设计上一进程服务一 client);**并行分支共用一个 stdio 子进程会串行排队**
- **Streamable HTTP**(spec 2025-06-18):server 独立进程**可同时服务多 client**;单 endpoint POST+可选 SSE 流;可选 Mcp-Session-Id;**可恢复流**;天然支持并发多路复用;TS SDK 有 `statelessMode` 可水平并发

### 5.3 四种落地模式对比

| 模式 | 代表 | 改造成本 | 实时性 | 维护面 |
|---|---|---|---|---|
| a. 宿主内嵌 MCP client 节点 | n8n/Flowise/Langflow | 宿主已建好,**server 侧零改造**(须先解决传输) | 好 | 低;受宿主节点能力上限 |
| b. stdio 包一层 HTTP | supergateway(2864★)/mcp-proxy(2766★) | **一条命令零代码** | 好(并发经单管道串行) | 极低;鉴权自加 |
| c. MCP→REST/OpenAPI 网关 | **mcpo(4385★)**:tools/list 自动生成 REST 端点+OpenAPI+Swagger UI | 一条命令 | 好 | 低;覆盖一切有 HTTP Request 节点的软件(含 ComfyUI 的 HTTP 节点) |
| d. CLI 直调 spawn | — | 需 bin 入口 | 差(冷启动每次重付) | 低频场景 |

### 5.4 八个坑(融合设计约束)

1. stdio 在浏览器/Electron renderer 无法 spawn——必须后端进程;2. n8n 官方节点拒收 stdio(Docker 不能起任意进程);3. **两段式 confirm-token 在 DAG 的表达 = 节点对**(调用节点输出 {needConfirm, estimatedCost, confirmToken},确认节点以 token 为入参复调;token 10min TTL→工作流排队延迟会作废需重取;官方 Comfy MCP 的积分确认证明此问题在生图场景是**标配而非特例**);4. **工具描述的 LLM 触发语义(WHEN/AVOID/NEXT)在确定性画布里无用——inputSchema 才是画布侧的真正接口**(27 工具长描述变 tooltip 死重);5. 长任务须 submit/poll 节点对(我们已有的 handle 异步模式恰好匹配);6. 并发需 Streamable HTTP(statelessMode)非 stdio;7. 产物回传:REST 路径 base64 JSON 视频体积爆炸→需产物 URL 化/文件服务;8. 密钥留 server 侧,outDir 绝对路径依赖宿主环境一致。

### 5.5 推荐落地路径(对本项目 27 工具 Node stdio server)

- **路径 1(~0.5-1 天,零改动验证)**:supergateway/mcp-proxy 包成 Streamable HTTP+反代 Bearer → 立即被 n8n(独立节点)/Flowise/Langflow 原生消费;若愿改 server:原生加 `StreamableHTTPServerTransport`(TS SDK statelessMode,~2-4 天含测试)长期更干净
- **路径 2(1-2 天,广覆盖)**:mcpo 起 REST+OpenAPI 网关 → 覆盖非 MCP 原生画布(任意 HTTP Request 节点软件/自研画布/ComfyUI HTTP 节点);需补产物 URL 化
- **路径 3(1-2 周,产品化)**:薄适配层自有网关,把 27 工具收敛为**三类画布节点语义**(同步工具表单[inputschema 驱动]/异步 submit-poll 对/确认对),负责 token 生命周期+产物 URL 化+密钥托管——对接自研导演台的终极形态

---

## 6. media-gen-mcp 白盒:状态三层模型与多 Agent 隔离并发深析(作者亲析)

> 本章基于源码逐文件核验(证据=文件:行号)。结论先行:**本项目当前架构是「单用户本地单会话」威胁模型;多 Agent 并发=多进程 spawn,三层状态各有不同的隔离语义与竞态面**。

### 6.1 会话与传输层

- `src/index.ts:16` StdioServerTransport —— **一进程一 client**(MCP stdio 规范)。多 Agent 并发的天然形态 = 每 Agent spawn 一个 server 进程(CC 即此形态:N 个 CC 会话 = N 个进程)。
- 无 HTTP/SSE/streamable-HTTP 传输:画布类宿主(浏览器/Electron 前端直连)无法直接消费,须经后端代理或包装层(§5 路径)。

### 6.2 状态三层模型(隔离分析的核心框架)

| 层 | 状态 | 位置 | 多进程(N Agent)语义 | 竞态/风险 | 证据 |
|---|---|---|---|---|---|
| **L1 进程内存** | cooldownUntil(60s 软熔断)/lastErrorAt | provider 实例字段 | N 进程=**互不可见**,熔断只护本进程 | 失效渠道被 N 进程各打一次才各自熔断(放大 N 倍失败流量) | `agnes.ts:148,153`;`hfspaces.ts:185-189` |
| L1 | rateLimits(agnes,TTL 默认 7 天) | 实例内 Record | **限速计数被进程数稀释**(N 进程=N×配额) | 上游 429 风险放大 | `agnes.ts:95,108`;`config.ts rateLimitTtlMs` |
| L1 | dynamicCatalog 缓存(flow 10min)/confirmSecretCache | 实例内 | 只读缓存,各持一份无害 | 无 | `flow.ts` |
| **L2 跨进程文件**(~/.media-gen-mcp/,原子 tmp+rename) | *-confirm-secret(安装级密钥) | 文件 0600 | **刻意共享**:跨进程令牌可验证(2026-08-31 事故修复) | 无(同值) | `flow.ts:103`;`pixverse.ts:89` |
| L2 | *-confirm-consumed(单次消费表) | 内存 Map+文件双写;读时合并、写时并集 | 阻断**顺序**跨进程重放 | 🔴 **并发首消费毫秒级 TOCTOU**(已文档化诚实边界;多 Agent 高并发放大窗口,但重放需同 digest 同参数,后果=多扣一次积分) | `flow.ts:878-893` 注释块原文 |
| L2 | flow-project.json v2(scope→pid 映射) | 文件;**合并写**(重读盘取最新 map) | 防多场景互抹 | read-merge-write 本身有丢失更新窗(两进程同窗写不同 scope,后者覆盖前者的新键);窗口窄+键幂等,低危 | `flow.ts:1240-1255` |
| L2 | pixverse-cost-ledger | 文件;read-modify-原子替换 | entries 按 key 幂等 | 并发丢更新只影响观测值新鲜度(best-effort,失败不阻断) | `pixverse.ts:947-960` |
| **L3 外部账号级**(进程/文件隔离均无效) | HF ZeroGPU 配额(账号/IP,免费 5min GPU/天) | HF 侧 | **天然不可隔离**,N Agent 抢同一池 | 配额枯竭互相挤兑;data:null 拒绝 | `hfspaces.ts:194-195` |
| L3 | pixverse 订阅积分 / cloudflare neurons / siliconflow 余额 | 供应商侧 | 不可隔离;确认门防**单次误耗**,不防多 Agent 各自确认后合力烧干 | 需画布/编排层做全局配额仲裁 | `pixverse.ts` 确认门族 |
| L3 | 渲染浏览器(lasso render 档 attach) | 单实例共享(端口级) | 并发渲染争抢同一 Chrome | P0-29 已证多实例互杀;隔离=三 env 套装或串行 | 项目记忆 P0-28/29 |
| L3 | CDP 单页面渠道(flow 9223 / gemini 9225) | 单浏览器单页 | **结构性串行**:gemini S303 交错守卫拒绝并发第二提交 | 多 Agent 并发点 gemini 必撞 S303(设计如此,防烧配额) | `gemini-web.ts` S303 |

### 6.3 已内置的隔离旋钮(白盒确认,融合时可复用)

1. **`MEDIA_GEN_MCP_CONFIG` env**:每进程独立 config —— 白名单/密钥/策略可 per-Agent 配(测试 fixture 已大量使用该缝,`test/*.test.ts` 头部范式)
2. **flow scope = cwd 末两段**(flowScopeKeyOf):不同工作目录=不同 Flow 项目=资产天然分域(`flow.ts` v2 映射;AIGC 产线已用 `MEDIA_GEN_FLOW_PROJECT_ID` env 钉死通道)
3. **计费确认门两段式**:needConfirm→confirmToken→复调 —— 在编排层等价于「人工/上层审批 checkpoint」,画布可把它建模为节点间闸门
4. **providerStrategy=ordered + enabledProviders 顺序**:per-Agent 差异化渠道路由(0.23.0 新增)
5. **输出域**:默认 outDir=./output(cwd 级),不同 cwd 天然隔离;`name -2/-3` 避让有窄竞态窗(`download.ts:57` existsSync→createWriteStream 窗;同 outDir+同名+同刻双进程才触发,丢失更新形态=数据交错,建议融合时给画布层传唯一 name 或分 outDir)

### 6.4 多 Agent 并发判定矩阵

| 并发场景 | 现架构行为 | 隔离方案 | 融合进画布后 |
|---|---|---|---|
| N Agent 同渠道生图(agnes/zhipu 免费) | 各进程独立限速/熔断 → 失败流量放大 | 每 Agent 独立 config(可各自 key 池);或上层单宿主进程串行化 | 画布执行器天然全局队列 → **反改善** |
| N Agent 抢 HF ZeroGPU | 配额账号级,先到先得,data:null 拒后来者 | 无进程级解;需全局调度(建议:画布 DAG 层按配额预算排任务) | 画布可做配额预算节点 |
| N Agent 各自过确认门烧 pixverse | 各自确认合法 → 合力烧干订阅 | 账号级无解;上限策略(config pixverse.* 限额?)/编排层全局账本 | 画布做全局 credit 预算闸 |
| N Agent 并发首消费同一 confirmToken | 毫秒 TOCTOU 双放行(顺序重放已阻断) | 威胁模型内可接受;彻底封窗=mkdir 锁(文档已判过度设计,多 Agent 场景需重评) | 单宿主进程内 Map 即封窗 |
| N Agent 并发渲染(render_video 等) | 争抢 lasso render 单例 | LASSO_RENDER_PORT+三 env 隔离套装 / 串行 | 画布队列串行化最简 |
| N Agent 并发写 flow-project.json | 合并写防互抹,残余窄丢失更新窗 | per-Agent STATE_DIR 注入(测试缝已有,可产品化) | 单宿主进程消失 |

### 6.5 白盒结论

- **单 Agent 场景(CC 会话)**:现架构威胁模型完全覆盖,零改造。
- **多 Agent 并发(2-5 进程)**:L2 竞态均在文档化可接受范围;真正的痛点在 **L1 限速稀释** 与 **L3 账号级配额挤兑**——两者都不是「加锁」能解的,需要**全局调度者**。
- **关键洞察**:画布软件的 DAG 执行器恰好就是那个全局调度者。**融合不仅不恶化并发,反而把 L1/L3 的调度责任上收到唯一有权看到全局的地方**——这是「MCP 作为引擎、画布作为编排」架构的并发学论据。

---

## 7. 融合可行性综合评估(按 §1 修正后标准 C1-C4;管线核验 agent 已回收)

### 7.1 NodeTool 端到端管线核验(C1 最高权重判据,源码级)

> 主仓 main=TS monorepo(zread/文档站索引的 master 是旧 Python 结构,勿据其评估)。节点覆盖按 manifest 实测统计:

| 端到端环节 | 判定 | 源码证据 |
|---|---|---|
| 1 生图 | **✔**(多 provider 冗余) | fal 216 t2i+418 i2i/replicate 237/atlas 80/topaz 增强 |
| 2 图生视频 | **✔** | fal i2v 220/higgsfield 62/kie 83;**首尾帧 28 节点**(Veo3.1 FirstLastFrame/WanVACE)+ KlingO3FirstLastFrame |
| 3 视频拼接/合成 | **✔** | video-nodes 3252 行 33 节点:Concat(无损+含音轨)/Trim/Overlay/Transition(xfade)/ChromaKey/**RenderTimeline**(timeline.ts:889,1736 行,WebGPU 逐帧 GPU 合成,mp4/webm/mov-alpha/png-seq;无 GPU 回退 ffmpeg rough cut) |
| 4 音频生成 | **✔** | TTS 34(OpenAI/HF/MLX+voice clone)/音乐 Suno·fal·MiniMax/音效 t2a 51 |
| 5 音频拼接/混音 | **✔** | AudioMixer/ConcatAudio/ConcatAudioList/OverlayAudio/Normalize/RemoveSilence/Fade + 时间线混音 |
| 6 成片导出 | **✔** | RenderTimeline mp4/webm/MOV-ProRes4444-alpha/png-seq + ExportVideoDialog + AssembleTimeline |
| 加分:导演台 | **✔ 闭环** | storyboard.ts:LoadStoryboard→StoryboardShots→Recast→RenderStills→**RenderClips→AssembleTimeline**;timeline 包 175+ commits/~100 测试文件/290 UI 组件(完整多轨剪辑器:Player/VersionHistory/Inspector 25+ 面板);storyboard 61 UI 组件+成品配方(storyboard-to-trailer/multilingual-video-dubber) |

小 GAP(不阻塞):首尾帧覆盖面偏窄(28 节点)/无一键 beat 切点(有 beats.ts 基础)/**RenderTimeline WebGPU 合成依赖目标环境适配器**(🔴 本机 Monterey=macOS 12,Chrome WebGPU 官方要求 macOS 13+——裸机大概率走 ffmpeg 回退丢特效,须实测)。

### 7.2 渠道整合三条路径(C2:任一即达标,三条全通)

| 路径 | 形态 | 工程量 |
|---|---|---|
| a. 原生 MCP client | NodeTool 配 stdio/HTTP MCP server → 27 工具以 `mcp_mediagen_<tool>` 进 agent toolbelt | **0** |
| b. MIT 代码流入二开 | 我们的渠道层(MIT)直接嵌入/对接其 TS 节点体系(同语言!) | 按需 |
| c. **factory+manifest 原生节点包** | 仿 higgsfield-factory.ts(**仅 89 行**)+1 份 manifest 声明 9 渠道 schema,运行时批量生成节点类;minimax-nodes 手写范式=每节点 60-120 行(BaseNode+@prop 装饰器+process()) | **≈1 base+1 factory+1 manifest,近零代码**(我们已有 9 家渠道完整 wire 契约沉淀,manifest 就是契约转写) |

### 7.3 重排后的路线矩阵(C1 管线完整度最高权重)

| 维度 | **路线 A:NodeTool 二开(新推荐首选)** | 路线 B:自研壳+双引擎 | 路线 C:ComfyUI 生态 |
|---|---|---|---|
| C1 端到端管线 | **6/6 ✔+导演台闭环**(§7.1) | 需自建全部六环节(人月级) | 生成强,**拼接/音频/时间线弱**——不是完整管控台 |
| C2 渠道整合 | **三路径全通**(§7.2,c 路径近零代码) | MCP HTTP 化+适配层 | mcpo 桥或自建节点 |
| C3 许可(二开) | AGPL=自用/开源二开**无碍**;仅闭源商用分发受限(届时可:渠道层保持独立 MIT 服务进程隔离,壳闭源风险的只是我们写的部分) | 全自持 | 同左(GPL 进程隔离安全) |
| C4 架构/生态 | TS monorepo(与我们同栈)/React Flow v12/SQLite+Drizzle/极活跃(09-23)/electron+web 双形态 | 全新建设 | 13.5 万★生态,Python 栈 |
| 附加 | **已内置 ComfyUI 集成节点**(integration-nodes/comfy.ts)——本地 GPU 引擎顺手就接 | 需自接 ComfyUI | — |
| 主要风险 | 🔴 RenderTimeline WebGPU 在 Monterey 可用性(回退丢特效);AGPL 未来商用边界 | 工作量最大 | 范式错位(执行图≠管控台) |

**裁决翻转说明**:初稿按「产品化优先」把 AGPL 当红线判 NodeTool 仅自用;按用户修正标准(二开 OK、能力优先),**NodeTool 升为唯一同时满足 C1 全✔+C2 三通+C4 同栈的候选**——且其 ComfyUI 集成节点把「本地 GPU 执行后端」也顺手收编。ComfyUI 从「底座候选」归位为「NodeTool 之下的本地推理引擎」;自研壳降为「未来闭源商用时的迁移预案」(我们的渠道层 MIT 独立,迁移门始终开着)。

### 7.4 多 Agent 隔离并发:架构级合成(§6 白盒 × §3 执行器白盒,结论不变)

四层调度栈,各层职责由白盒证据唯一确定:

```
L3 账号级配额(HF ZeroGPU/pixverse 积分/单页 CDP) ← 唯一全局可见者=画布/编排层
      ↓ 只能在这里做配额预算闸、gemini 串行化(S303 对应排队)
L2 画布执行器(NodeTool Actor DAG:每 job 一个 Runner+SQLite 状态+可取消)
      = 全局调度者:吞 N Agent 并发请求,产出确定性顺序(天然互斥,无需分布式锁)
L1 MCP server 实例(stdio 每进程一个 / Streamable HTTP statelessMode 多路复用)
      = 渠道聚合+确认门+fallback;单实例下 L1 内存态(熔断/限速)自然全局
L0 产物层(per-cwd outDir+唯一 name;NodeTool 本地同机=路径直通)
```

- **白盒相互印证**:NodeTool Actor DAG、ComfyUI 单 worker 串行队列、MCP stdio 单管道——在「画布执行器统一驱动」架构下都从缺陷变成特性(天然互斥)
- **确认门落点**:confirm-token 两段式=画布节点对(官方 Comfy MCP 同构先例);10min TTL 要求确认节点不被长队列饿死(超时→自动重取 token)
- **剩余风险**(非架构可解):账号级配额挤兑——建壳层全局 credit 预算闸(唯一必须自建的并发安全件)

### 7.5 实施路线(修正版)

**第一步(本周):NodeTool 本地实证**——clone/pnpm 起本地(自用无 AGPL 顾虑)→①stdio 挂 media-gen-mcp 验证 27 工具进 toolbelt;②跑通一条端到端链(生图→i2v→Concat→TTS→混音→RenderTimeline 导出);③🔴 实测 Monterey 下 RenderTimeline 是否 WebGPU 或回退(决定特效/转角质量预期);④看 storyboard-to-trailer 配方。
**第二步:渠道层三选一落地**——先 a 路径(0 成本)用着;同步把 9 渠道 manifest 化(c 路径)做成原生节点包(体验最佳,confirm-token 可做成节点对);b 路径按需。
**第三步(可选):ComfyUI 接入**——NodeTool 的 comfy 集成节点 + 官方 Comfy MCP,本地 Wan2.2/LTX-2 推理补进同一画布;与 [[comfyui-workstation-project]] 硬件规划衔接。

## 8. 风险与红线

1. 🔴 **AGPL(NodeTool)/GPL(ComfyUI 双仓)边界**:进程隔离+API 调用=安全(白盒确认);fork/嵌入/魔改再分发=传染。任何「把 NodeTool 代码搬进自研壳」的想法直接封死
2. **API 节点断供前科**:ComfyUI Sora2 节点 2026-09 官方随 OpenAI 停服移除——云 API 节点生命周期风险;缓解=media-gen-mcp 的渠道抽象(渠道死→白名单摘除,工具面不变)
3. **Monterey 平台风险**:Diffusion Studio WebCodecs 路线在本机(与 Remotion compositor dyld 同域)可用性未验证——若采用其合成组件须先探针
4. **HF ZeroGPU 配额挤兑**(§6 L3):画布多任务并发 hfspaces 会互相 data:null——需壳层配额预算
5. **维护负担**:路线 B=三件套(壳+MCP+ComfyUI)长期维护;media-gen-mcp 已证明可持续(渠道工厂飞轮),壳是新负担
6. **伪开源坑重申**:hypit/waoowaoo(NOASSERTION)、Diffusion Studio v2(邀请制+非商用)、Refly(商用限制)——选型时 license 必逐条核验

## 9. 结论与建议

1. **可行性总裁定:GO**——按用户修正标准(能力优先、二开 OK、对接方式不限),**NodeTool 是唯一全项达标的开源底座**:端到端管线 6/6 ✔+导演台闭环+渠道整合三路径全通+同栈活跃。选型层面不存在「需要自建整个管控台」的必要性——自建只剩两个场景:①未来闭源商用(AGPL 边界,届时渠道层独立迁移);②NodeTool 停维。
2. **渠道整合落地序**:先用 a 路径(原生 MCP client,0 成本,当天可用)→ 再做 c 路径(9 渠道 factory+manifest 原生节点包,confirm-token 做成节点对,体验最佳)→ b 路径(MIT 代码流入)按需穿插。
3. **对本项目的最小行动集**:①(可选)server 加 StreamableHTTPServerTransport(2-4 天)——NodeTool 的 MCP client 吃 HTTP 更稳,也解多路并发;②渠道 wire 契约导出为 manifest(为 c 路径做准备,我们契约沉淀的直接变现);③产物 URL 化仅远程画布需要,本地 NodeTool 同机路径直通可缓。
4. **多 Agent 隔离并发终论**(不变):三层状态模型证明进程级竞态全部在文档化可接受窗口内;唯一必须自建的并发安全件=画布层全局 credit/配额预算闸(HF ZeroGPU+pixverse 积分挤兑与 gemini S303 串行都归它管)——这建在 NodeTool 的 job 调度处,不建在我们 MCP 里。
5. **第一步行动(本周可做)**:clone NodeTool 本地起 → stdio 挂 media-gen-mcp → 跑通「生图→i2v→Concat→TTS→混音→RenderTimeline 导出」一条链 → 🔴 实测 Monterey 下 WebGPU 合成是否回退(决定特效质量预期)→ 看 storyboard-to-trailer 配方。此步零决策成本,产出直接决定二开深度。

---

*附:本报告调研 agent 证据清单与来源 URL 见各章;白盒证据行号对应当日 main(31103fb / v0.23.0)。*

---

## 10. 附录:三问精确核验(2026-09-24 追加,基线 main@88a9c27;zread 快照为旧布局已弃用,经 jsDelivr CDN 逐文件核验)

### 10.1 Q1:MCP 工具挂入后的可用形态(能力边界)

**✅ Chat/Agent 对话层——可用,LLM 填参形态**:
- 挂入后工具以 `mcp_<server>_<tool>` 进 agent toolbelt(`packages/agents/src/tools/external-mcp-tools.ts`;stdio 用 StdioClientTransport;HTTP 先 StreamableHTTP 回退 SSE);唯一消费点 `packages/websocket/src/session/chat-turn.ts:1708`(并入 rawToolbelt 交 agent loop)——**对话回合内由 LLM 决定调用+填参**
- inputSchema 原样透传给 LLM(`this.jsonSchema = remote.inputSchema`);**无参数钉死机制**(不能锁 provider;唯一硬开关=server 级 enabled);设置页只显示工具名列表
- 结果回流:text/base64 images/resource_link → **临时资产句柄(asset:// URI)**,不直接变图节点 ImageRef 输出

**❌ 画布确定性层——MCP 路径做不到**:
- 画布 Agent 节点(`packages/llm-nodes/src/nodes/agents.ts:1036`)选不到外部 MCP 工具(hydration 注册表只含内置+媒体工具,mcp_* 不在;注入机制 setInjectedTools 存在但主链路未用)
- 全仓 mcp 命中全集确认:**不存在「MCP Tool 图节点」**——「画布上摆节点、表单里定向选渠道选参数」须走二开两条正路:
  1. **自建 node pack**:factory+manifest 范式(higgsfield-factory 仅 89 行;minimax-nodes 手写范式=每节点 60-120 行:BaseNode+@prop 装饰器+process())
  2. **custom provider 路线(零代码)**:Settings→Models & Providers→**OpenAI-compatible endpoints→Add endpoint**(name+baseUrl+apiKey,文档明示带 /v1 版本路径)→ 端点模型**直接进模型菜单**;图像模型走 /images/generations 进 Image 节点选择器(实现 `runtime/src/providers/custom-openai-provider.ts`);亦支持 env `CUSTOM_*_BASE_URL/API_KEY`

### 10.2 Q2:推理模型接入面

- **智谱 GLM 零二开直连**:OpenAI 兼容端点填 `https://open.bigmodel.cn/api/paas/v4` + apiKey 即现于模型菜单(`docs/developer/providers/openai-compatible.md` 直证)
- 原生 provider 家族(`packages/runtime/src/providers/index.ts`):Anthropic(**Claude 原生**,ANTHROPIC_API_KEY,live 拉模型)/Gemini/OpenAI/Ollama/LMStudio/llama.cpp HTTP/NodeLlamaCpp(进程内 CPU GGUF)/vLLM/CustomOpenAI/HF/Replicate
- **CC 反向驱动=一等公民**:自带 /mcp 服务端(默认 127.0.0.1:7777;`nodetool mcp serve` stdio 模式);**`nodetool mcp install` 一键注册进 Claude Code**(写 ~/.claude.json);面=**CodeAct**(沙箱 execute_code 含 run_workflow/create_workflow/get_asset/generate_image 全 belt)+view_image;生产默认关(NODETOOL_ENABLE_MCP=1),OAuth 或 Bearer ntk_ 令牌

### 10.3 Q3:运行足迹与弱机适配(含对本机 Monterey 的修正)

- **单 Node 进程**(端口 7777,同进程伺服 API+web 静态;STATIC_FOLDER);SQLite(DB_PATH,可选 Postgres);Node **≥22**(.nvmrc=22.22.1);**npm workspaces+turbo,无 pnpm**;桌面=Electron Studio
- 🔴 **修正此前「ffmpeg 回退」表述**:时间线导出=浏览器内 **WebGPU 优先、Canvas2D 确定性回退**(`web/src/components/timeline/preview/gpu/createCompositor.ts` 明文;WebCodecs/mediabunny 编码)——Monterey 无 WebGPU 走 Canvas2D:**能出片且确定性不丢,仅 CPU 合成较慢**;ffmpeg 只是 Docker CLI 依赖非回退链
- MLX 在 TS main 已无(Intel Mac 不适用);本地 LLM 替代=Ollama/LM Studio/llama.cpp(NodeLlamaCpp CPU GGUF "works anywhere")
- **纯云端模式=生产默认**(docker-compose 注释:"curated cloud catalog, no local model runtimes");Docker 形态齐全(node:22-slim 三阶段/17777→7777/health/**lavapipe 软渲染让服务端 WebGPU 图像节点无 GPU 可跑**)
- 本机(MBP11,4/16GB/Monterey)判定:当画布+调度中枢够用(单 Node 进程+浏览器 ~1-2GB);GPU 合成慢但可用;**Windows 机器(见 §11 实施方案)承接全流程跑+渲染更优**——Windows Chrome WebGPU(DX12)可用,RenderTimeline 走真 GPU 合成

### 10.4 对用户三问的直答摘要

1. **「Tools 中可用吗?怎么个可用法?」**——chat 对话层可用(LLM 填参,不可钉死渠道);画布确定性选渠道选参数**MCP 路径给不了**,须 node pack(体验完整)或 custom provider(零代码,智谱/siliconflow 类 OpenAI 形状渠道可直挂;聚合全渠道可写极薄适配层)
2. **「本机性能够吗?」**——够(指挥台角色:单 Node 进程+云 API 生成;Canvas2D 回退可用);GPU 重活建议放 Windows 机
3. **「能接 Claude Code/智谱推理吗?要二开吗?」**——**都零二开**:智谱走 OpenAI 兼容端点直填;Claude 原生 ANTHROPIC_API_KEY;CC 反向驱动 `nodetool mcp install` 一键注册(CodeAct 面,CC 当大脑编排 NodeTool 工作流)

---

## 11. 附录:开发落位裁决与实施路线(2026-09-24 用户确认方向后定稿)

**用户决策**:Windows 机器跑全流程;开远程私有仓库;按「自建 node pack + custom provider」双路接入渠道(=二开)。

**落位裁决:新项目(NodeTool 私有 fork),不在 media-gen-mcp 内做**——两者是不同代码库(NodeTool=TS monorepo 画布产品;media-gen-mcp=独立 MCP 渠道引擎);许可上 MIT 渠道层可单向流入 AGPL fork,但保持 media-gen-mcp 独立=迁移门常开+CC 生态继续受益。实施文档:**doc/NodeTool二开实施方案-2026-09-24.md**(新会话执行入口)。
