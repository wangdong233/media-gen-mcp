# 渠道工厂飞轮(Channel Factory Flywheel)v1

> 2026-09-23 启动 | 用户指令:持续接入全部有价值的免费/强能力渠道,渠道名简单明了,说明极致清晰,每轮深搜索,能力探到极致(边界/容量/限制全测成已知),飞轮每轮自评估,直到新发现为零才停。

## 队列 v4(2026-09-23 用户存量调研交叉产出;v3 已全部处置)

| # | 渠道名 | 平台 | 关键事实(⚠️存量快照 09-10~16,均需复核) | 形态 | 备注 |
|---|---|---|---|---|---|
| 1 | ~~`minimax`~~ → 付费册首位 | MiniMax(海螺) | 无免费层终裁(PAYGO 零免费;Token Plan 排除 H3) | REST(推荐) | 轮17 SKIP-记册:**付费档差集最大候选**(音画同出 H3 独占+image-01 $0.0035/张+TTS 三域单 key);MCP 半弃疗/CLI 绑订阅 H3 不在池→实施走 REST;wire 契约已备(充值后直接升 GO) |
| 2 | ~~`sandai`~~ → NO+观察位 | Sand.ai | 官方双向隔离原文锤死(API 不消费 Magi/Gaga credits 且反之) | REST(双站) | 轮18 NO:免费通路结构性不存在;付费观察位(转正=公开 $/credit+Magi-2 上 API);Magi-2(114B)是研究发布不在 API,权重 Apache2.0 开源=本地 ComfyUI 工作站资产 |
| 3 | ~~`tensorart`~~ → 记册-GO(付费册) | 吐司 TAMS | UI 免费 50/天锁站(官方明文不可互换);**首登 1000 免费 credits 可走 API**(✅官方计费页) | REST(TAMS) | 轮18 记册-GO:≈625 张 FLUX 种子额;SDXL $0.0024/张全队列最低;数万社区模型云跑+WAN2.1 视频+ComfyUI 模板;契约全公开+先算价+失败不扣费;充值人工通道短板 |

**观察位补充(终轮 S1)**:VideoGen(videogen.io)——定价页称每计划含 API+官方 MCP,但免费层额度无公开数字且自相矛盾($4/$12/$19 三说);日后公开确切额度可复议。

**mindvideo 专项裁决(轮 22,2026-09-24,用户发现站)**:SKIP-记册——免费层真实但极小(⚠️单源实测:**1 条 6s 视频/天 + 2 张 GPT Image 图/天**,签到 2 credits/日;水印+产出公开+仅个人用途);无官方 API(api-app.mindvideo.ai 匿名可观察 JSON envelope 但生成锁 Turnstile 登录+会话);**ToS 红**(逐字禁 bots/spiders/scrapers/逆向);匿名运营 SEO 农场型**多上游聚合站**(域名 2025-02 隐私保护,同时转售 Kling/Veo/PixVerse/即梦/Sora2 等,上游全部可直连,价格比直连贵 1.9-2.9 倍,Trustpilot 2.4/5);自牌 HappyHorse 1.0 宣称商用但与自家 FAQ 矛盾。revisit=官方开放 API。**方法论价值:该站类型(SEO 页农场聚合站)确实在旧网之外,重扫正当性成立**。

**裁决债务(交叉报告指出)**:①火山方舟与 doubao SKIP 的覆盖关系需补记(方舟=Seedance/Seedream 官方 B 端 API,若 doubao 排除理由仅是模型层则方舟是独立通道)②Tapnow 促销 09-23 截止/Flova 缩水 2800→2400 已过期——年付注记带时效。**Miora 专项:NO(通道不存在,OpenAPI 仅只读;3D Director 残值为手动工具用途)**。

## 队列 v3(2026-09-23 S1 再发现扫描产出;v2 已全部处置)✅ 已全部处置(4/4,见轮报 12-15)

**S1 补充输入(用户指引,2026-09-23)**:队列 v3 处置完后,下一轮 S1 增量扫描须交叉用户存量调研 `Project/其它/AIGC网站/`(8 报告+research/ 底稿)——重点增量:**补漏普查报告-09-13**(飞轮未见的补漏渠道)、**Miora 深度分析-09-16**(AIGC 剧集工作流既选资产画布,或有生成 API)、**年付对比/年付推荐白盒终版**(付费候选册缺的 Krea $63/年·Tapnow $90·LibTV ¥569·Flova ¥1400 等年付档位)、即梦/PixVerse 集成手册(既有渠道深挖);中转审查报告(duoyuanx 不推荐结论已在册,查有无新增中转站)。处置方式:先读补漏普查+Miora 两份(免费渠道增量),付费册补注年付档位,再决定是否需要新一轮深挖。

| # | 渠道名 | 平台 | 免费额度(核验) | 形态 | 备注 |
|---|---|---|---|---|---|
| 1 | `cloudflare` | Cloudflare Workers AI | **10,000 Neurons/天全账号续杯**(✅官方定价文档,6 天前更新) | 官方 REST API(`@cf/` 命名空间) | ✅轮12 已实施(image-only;BLOCKED-on-login 待用户建 Token+Account ID) |
| 2 | `imagineart` | ImagineArt(Vyro AI) | **官方 MCP 同池:免费 100 credits/日可编程消费**(✅官方原文"billed through your existing credits") | 官方 MCP+CLI(npm) | ✅轮13 GO;≈16-20 张 Z-Image-Turbo/日或 3 条 Wan2.2 视频;非 SeaArt 同集团(证伪);S4 走 CLI spawn(PixVerse 先例),REST API 勿走(独立计费待证+模型旧) |
| 3 | ~~`prism`~~ → NO(排除) | Prism(YC P25) | 免费砍至 10 credits/月(❌Launch 时 100 的 10 倍缩水);API 已无声退役 | ~~Workspace+API~~ | 轮14 NO:2 人团队 pivot 卖 LLM token(prisminference.com);上游=fal 转售,直连 fal 恒优;零模型独占 |
| 4 | ~~`pika`~~ → SKIP-记册 | Pika | 免费档已归零(0 credits/月 packs only,❌「80/月」三源全过时);**免费 credits 官文明文不能进 Agent Wallet**(MCP 纯付费) | 官方 MCP+付费 REST(API Club) | 轮15 SKIP:模型面与既有池 ~100% 重复且更贵;再入点=API Club 首月 $10 赠金(付费册)+Pikaffects 特效面 |

**官方 MCP 生态测绘(新维度,2026-09-23)**:官方 MCP 存在=fal(2026-03)/Pika(2026-07)/Ideogram/Runway/kling(Kling Skills)/SeaArt/krea(09-21);无官方 MCP=Luma(仅三方包装)。Ideogram/Runway API 纯付费且能力被既有渠道覆盖→排除;fal 无免费档(付费册补注「官方 MCP 可用」)。

## 队列 v2(2026-09-23 S1 重新发现重排;v1 发现路额度连续三轮证伪后重扫)✅ 已全部处置(7/7,见轮报 4-10)

**付费规则(用户 2026-09-23 补充)**:少量付费可拿高性价比(图/视频均可)的渠道也纳入考虑——人民币计价记册;性价比足够高时记录或征求用户意见后再定。

### 实施队列(免费已核实优先 > 待核实;API 形态 > UI;直连 > 代理)

| # | 渠道名 | 平台 | 免费额度(核验) | 形态 | 备注 |
|---|---|---|---|---|---|
| 1 | `siliconflow` | 硅基流动 | **Kolors 永久免费模型 + 注册赠 ¥14**(✅官方原文) | HTTP API | 国内直连零代理;✅轮4 已实施(真机门 BLOCKED-on-key) |
| 2 | ~~`krea`~~ → 付费候选册 | Krea | 100 CU/日仅 app 消费侧(✅pricing 原文);**对本工具=按量付费**(轮5 反转) | 官方 MCP/API/SDK | 免费层视频三源互证不可用;UI 自动化 ToS 明禁+Turnstile |
| 3 | ~~`leonardo`~~ → 付费候选册 | Leonardo.Ai | 150 tokens/日锁死 UI 层(✅原文;公开产出+基础质量+**免费档 IP 归平台** ToS 8.7);API/MCP 与免费额度官方原文完全隔离 | 官方 API+MCP | 轮6 反转同 krea;聚合目录最宽;单价藏 app 内计算器 |
| 4 | `pixai` | PixAI | **10,000 积分/日绑定账号**(✅官方原文;claim 制+邮箱验证)≈12 张/日 | 账号级 API | ✅轮7 GO(轮 7 S4 实施中;BLOCKED-on-login 待用户注册+验证邮箱+试建 key) |
| 5 | ~~`vidu`~~ → 付费候选册 | Vidu 生数 | UI 每日积分数值官方不公示(⚠️量级 20/日);**免费产物无商用授权+UI 自动化 ToS 明禁** | UI+官方 API(C类) | 轮8:SKIP UI;API ¥0.375/s 记付费册 |
| 6 | ~~`seaart`~~ → 升级观察册 | SeaArt | 130-150 画力/日(官方自相矛盾:docs 150/官网 130,下行通道) | 官方 CLI+MCP(稀缺) | 轮9 SKIP-记册;真增量=聚合模型入口+云 LoRA 训练,与 PixVerse 池七成重叠,pixai 全面胜;revisit=MCP 计费池验证+免费层可调聚合模型则升级 |
| 7 | ~~`kling`~~ → 付费候选册 | 可灵 | UI 66 灵感值/日锁端内(⚠️66 口径延续但 3.0 单条 5s 1080p≈70 灵感值,**买不起一条**);API 积分制无免费层 | UI+官方 API(C类) | 轮10 SKIP-记册;增量=最新模型面(v3/4K/15s 多镜头/Omni),非免费量;PixVerse 池已覆盖主力 |

### 付费候选册(人民币计价,逐项记性价比,攒批征求用户)

| 候选 | 付费门槛 | 性价比初判 |
|---|---|---|
| `minimax`(轮17 入册,**册首**) | 国际 PAYGO:H3 768P $0.08/s≈¥0.58/s、2K $0.13/s≈¥0.94/s;国内 768P ¥0.5/s、2K ¥0.8/s;**image-01 $0.0035≈¥0.025/张**;TTS speech-2.8 ¥0.6-1.0/万字符;⚠️隐藏坑:参考视频输入也按秒计费、图片第 6 张起 $0.04 | **付费档差集之最**:音画同出 H3(原生立体声,候选册唯一)+全参考 Ref2VA(≤9 图+3 视频+3 音频)+image-01 低价+TTS/克隆全域,**单 key 三域**;4-15s 连续时长+2K 直出;输出 IP 归用户;无订阅可白嫖 H3(Token Plan 明确排除);MCP 半弃疗+CLI 绑订阅→**实施走 REST**(V2 submit/query+V1 image/t2a 双错误形态契约已备,充值后直接升 GO);纯裸价比 vidu 错峰贵 33%(vidu 无声) |
| `krea-api`/`krea`(轮5 反转入册) | 官方 API/MCP 按量预充值($0 起,无月费):K2 Turbo **$0.015≈¥0.10/张**、K2 Medium $0.03、NB2 $0.08、Seedance 2.0 $0.0849/s≈¥0.58/s、Kling 3.0 $0.176/s、Veo 3.1 $0.84/条 | **官方 MCP 09-21 新发布**(`claude mcp add --transport http krea https://api.krea.ai/mcp`,OAuth 免 key);84 模型聚合;API 商用权明文;ToS 零 API 禁令;接入形态全渠道最优。**计费池归属(MCP 走 API balance 还是订阅 CU)接入后跑一张 K2 Turbo 5 分钟定案**。待用户裁决:①media-gen-mcp 内嵌 provider ②用户直接加官方 MCP 独立用 ③SKIP |
| `leonardo-api`(轮6 入册) | API PAYG 充值 $5≈¥36 起(**余额永不过期**,无月费);新账号是否送 $5 三方多源/官方未确认(实测定案);单价无公开总表(藏 app 内计算器),三方口径 Lucid 档≈$0.009-0.03/张≈¥0.065-0.22 | 聚合目录全市场最宽之一(Flux 全家/GPT Image 2.5/Kling 3.0/Veo 3.1/Seedance/NB2 一把 key)+官方 MCP(mcp.leonardo.ai,仅 generate-image 3 模型);官方 SDK/异步+webhook/10 key;**排位低于 fal(同类形态且 fal 单价公开)** |
| `vidu-api`(轮8 入册) | API 按秒计价:**q3-turbo 720p ¥0.375/s(5s=¥1.875),错峰半价 ¥0.1875/s(国产同档最低档)**;q3-pro 1080p ¥0.75/s(16s=¥12);资源包 ¥500=16,000 积分起;需实名;并发≤5;API 积分与 UI 订阅两池完全分离 | **独有增量=Q3 参考生视频(Artificial Analysis 全球第二,角色一致性)+16s 音画同出(对标 Veo 3.1)+错峰半价+腾讯云/阿里云托管通道**;官方 API 文档完整(text2video/img2video/参考生/首尾帧/对口型/延长,Token 认证,回调+签名);API 产物可商用;建议与可灵/火山 API 价格-能力对比后裁决,先验证 Trial Credits 数值 |
| `kling-api`(轮10 入册) | API 积分预付**无免费层**:视频 1 积分=¥1(Kling 3.0 ¥0.6/s 720P 无声~¥3/s 4K);图片 1 积分=¥0.025(Image 3.0 ¥0.2/张,4K ¥0.4/张);**试用包 ¥70/100 积分**(30 天,并发 5,限购 5)≈166s 720P 视频;正式包 ¥5000 起 | **增量=最新模型面**:kling-v3(4K/15s 多镜头/首尾帧/主体控制)/v3-omni(多模态)/video-o1(人声控制)/image-o1(风格训练/人脸参考)+电商模板 API(一键换装/口播带货);**官方 Claude Code Skill(clawhub.ai/klingai-dev/klingai,账号一键绑定/AK-SK,可作 provider 参考实现)**;09-15 旧模型下线后 2.5 Turbo/2.6 存活;「¥554/年」是 UI 黄金年卡与 API 无关;**条件触发升 GO:AIGC 工作流需要 v3/Omni/O1 独占能力时直接接 API** |
| `liblib-api` | ¥10 起充(100 积分=¥1,积分永久有效) | LoRA 生态 API 可调(全网唯一)+Seedream5.0/Qwen-Image 在池;内容审核国内强度 |
| `siliconflow` 付费线 | 赠金外按量(Qwen-Image ¥0.3/张,Z-Image-Turbo ¥0.1/张,Wan2.2 ¥2/个) | 低价地板;✅轮4 已随免费线一并实施(软计费:每次调用带价格警示) |
| `poe-api` | $4.99/月=10k 点/日(≈¥36/月) | 单订阅聚合 Veo/Sora-2/Kling API 正路;Veo $0.135/s 偏贵 |
| `dashscope` | 百炼按量(wanx2.6 图/Wan3.0 视频 ¥0.3-1.2/s) | 新客免费 50 图+50s;待 AccessKey |
| `higgs`(付费) | 免费疑似归零,付费课金 | 运镜 i2v 独家,低频精品 |
| `recraft`(轮11 补注) | API Units 与订阅 credits 分离预付(免费档锁 Studio);矢量 $0.08/个 | **原生 SVG/Lottie 矢量生成全市场独占**(其余渠道全是位图);A 类但独占能力记付费册 |
| `fal`(轮11 补注) | 无免费档,纯 pay-per-use | **官方 MCP 可用**(2026-03,hosted,1000+ 模型)——此前调研已覆盖付费榜,补注形态增量 |
| `pika-api-club`(轮15 入册) | $10/月会费+PAYG,**首月送 $10**;自称比 fal 便宜至多 80%(含 5% 平台费) | ~15 供应商聚合 REST(dev.pika.art:Alibaba/BFL/ByteDance/Google/OpenAI/Meta/MiniMax/xAI/Ideogram/Recraft/ElevenLabs/Topaz);若做「付费低价聚合」线是现成再入点;旧 pika.me/dev API 已废弃 |

### 移出/维持 SKIP
- **移出**:higgs(免费归零单源)、nano(无免费循环额度,pay-as-you-go 付费备选注记)
- **排除实锤**:Sora(官方 2026-04-26 停服/API 09-24 死)、Grok(2026-03-19 撤免费)
- **维持 SKIP**:poe/wanxiang/doubao(无新证据);liblib 免费层 SKIP(20 积分/日≈40 张弱)→ 转 `liblib-api` 付费候选

## 每轮结构(7 段)

```
S1 发现:四通道扫描(榜单/中文媒体/英文社区/融资)→ 增量候选去重入队
S2 深挖:能力探到极致 —— lasso 实探页面 + agent 三路(能力配额/条款风控/技术先例)→ 「能力边界清单」(已知/未知全景)
S3 裁决:GO / SKIP-记册 / NO(三态,2026-09-23 用户强化:发现前期调查有误或性价比不高者,
        **不直接对接**——跳过并记入「延迟判定册」[错在哪/当前性价比/ revisit 条件],
        攒一批后综合判定(批量对比增量价值),再决定是否进入 S4)
S4 实施:provider(简单名)+ 白盒 stub 套件 + channelInfo 结构化说明 + 注册表/工具描述接入
        (轮4 起完成标准追加:能力声明与实现逐项对齐——supportsImageToImage/capabilities 说
         支持的每个输入,provider 内必须有对应接线,禁静默丢弃)
S5 测试:机械门(全量回归)+ 真机门(XX_IT 双门;有登录态→把 S2 未知项逐一测成已知,写入 channelInfo;
        无登录态→BLOCKED-on-login 登记,用户动作后补测)
S6 审查:fresh agent(03 清单)+ 修复循环
S7 轮末:①再发现扫描(增量) ②飞轮自评估(流程缺陷/文档/README/说明优化,当场改)
        ③轮报落盘(本文件 §轮报)
```

## 命名与说明标准(用户重点要求)

- **渠道名**:小写短名 ≤8 字符,与平台惯称一致(上表);注册名即 `provider=` 值。
- **channelInfo(结构化说明,三处一致)**:每 provider 实现 `channelInfo()` →
  `{ freeQuota, capabilities{t2i,i2i,t2v,i2v,keyframes}, limits{rate,daily,size,duration}, watermark, prerequisites{login,proxy}, risks, status{live|blocked-on-login|disabled} }`
  透出到 list_models detail + 工具 provider 描述 + README 渠道总表——**调用方一眼看清每渠道的额度/边界/前置/风险**。
- **能力边界清单 → 测试用例**:S2 的每个"未知"(如:万相视频单日上限?图生图支持否?)在 S5 转成真机 IT 用例,测成已知后回写 channelInfo(未知项清零是完成标准)。
- **provider 开发纪律(轮4 新增)**:抛给共享 withRetry 的错误必须带 `.status`(否则 isTransient 默认 0 误判为瞬时错被重试 3 次);错误码族 S1xx 环境/S2xx 网络/S3xx 参数/S4xx 结果全渠道统一。

## 延迟判定册(S3 SKIP 记录)

| 渠道 | 前期信息纠错 | 当前性价比判断 | revisit 条件 |
|---|---|---|---|
| `poe`(Poe UI 免费层) | 🔴"每日 3000 积分"**过时**——2026-03-30 官方砍 90% 至 **300 点/日**(24h 重置不滚存);300 点 ≈ **4 张 FLUX.1 schnell/日**,NB2-Lite/GPT-Image 单张 >300 点生不起,视频(≥数千点/条)免费层完全无缘;ToS 明文禁自动化;UI 逆向社区已死(无先例) | 低:免费层价值坍塌;"UI 自动化成本最低"判断作废 | ①若愿订阅 $4.99/mo(10k 点/日):**改走官方 API**(api.poe.com/v1 OpenAI 兼容,/v1/videos 聚合 Veo3.1/Sora-2/Kling/Seedance——市面少有的单订阅聚合视频正路;Veo $0.135/s 不便宜)→ 列付费候选 `poe-api` 待用户裁决 |
| `wanxiang`(通义万相 UI 驱动) | 🔴"文生图不限次"**不实**——灵感值制且两次下调(日送 50→10→新人包模式,免费 ≈50 张/天量级);**免费档带水印+标清+禁商用**;视频额度不明走慢速排队;UI 驱动零 GitHub 先例(自研工程量) | 低:免费层=水印小额图,商用/AIGC 成片价值低;无先例自担工程 | ①用户愿付费(付费版去水印+4K+商用) ②改走**百炼 dashscope API**(wanx2.6/Wan3.0 正路,新客免费 图 50 张+视频 50s,¥0.3-1.2/s)——列候选 `dashscope`(待 AccessKey) |

## 自驱动机制(2026-09-23 轮2 起)

会话级心跳 cron(每 23 分钟,job 05ff5a7c,session-only 不落盘)驱动续跑;心跳 prompt 内含本飞轮规则与终止动作(删 cron)。撞登录墙→BLOCKED-on-login 记录后跳队尾继续,飞轮不停。

## 终止条件(用户定义)

全部队列 S4-S6 完 + S1 再扫描连续一轮无新价值渠道 + 全部 BLOCKED 项(登录态/key)已交用户处理 → 飞轮停止。否则继续。

## 轮报

### 轮 1(2026-09-23,wanxiang + 飞轮基建)
- **S2 完成**:lasso 实探(✅ 登录态在[阿里账号 197***51],工作台 /wan/explore,能力面:万相3.0/720P/5s 默认,chips=全能参考 15 素材/首尾帧/视频编辑/图片生成/Agent)+ agent 深挖(额度/条款/先例)。
- **S3 裁决:wanxiang SKIP-记册**(发现路核心信息"不限次"被证伪;免费=水印小额;零先例)。衍生候选 `dashscope`(百炼 API 正路,待 key)入队尾。
- **S4 公共层落地**:channelInfo 渠道说明卡基建(types ChannelInfo 接口/registry 透出/五存量渠道全实现/禁用渠道附卡/守护测试)——gemini 套件 37/37。
- **S7 自评估**:①🔴**发现路信息时效风险**(万相案例:额度口径三个月一变)→ 定规:S2 必须实探+agent 核实,旧口径禁直接入 channelInfo;②SKIP 三态裁决生效(用户规则);③lasso 1.30.2 恢复验证通过(doctor stale_runtime pass + browse 正门全链 ✓,阻塞-3 解除);④发现路的"文生图不限次"来源于二手榜单——四通道发现的核验义务加重。
- **下轮**:队列 #2 `poe`。

### 轮 2(2026-09-23,poe → SKIP)
- **S2 完成**:lasso 实探(✅ 代理直达/登录墙 Google·Apple·电话/模型面确认:GPT-Image-2.5 双变体+NB2-Lite+Gemini-Omni-1.1+GPT-6/Claude/Grok/Kimi 全系聚合)+ agent 深挖 8 维(积分体系核实到官方原文级)。
- **S3 裁决:SKIP-记册**——免费层从 3000→300 点/日(-90%,未公告),仅 4 张低档图/日,主流图像 bot 单张生不起,视频无缘;ToS 禁自动化。**用户侧授权动作取消**(原等 Google OAuth,已无必要)。
- 衍生付费候选 `poe-api`(官方 OpenAI 兼容 API,/v1/videos 聚合视频)记册待裁决。
- **S7 自评估**:①发现路"免费层额度"类信息**时效衰减极快**(wanxiang/poe 连续两轮证伪)→ S2 深挖的积分体系必须找到**官方原文/多源当月交叉**才算核实;②登录墙探测顺序优化:先深挖配额再动授权(本轮若先授权就浪费用户动作)——已定规:S2 agent 先行,实探授权动作后置。
- **下轮**:队列 #3 `liblib`。
  - 轮 2.5(用户裁决,2026-09-23):**S1 重新发现启动**——连续两轮额度证伪判定发现路数据源可信度不足,重扫四通道(核验标准提高:额度数字须官方原文或 2026-08 后多源交叉)+ 根据结果**重排全部队列**。liblib 深挖 agent 并行在跑(其结果并入重排输入)。

### 轮 3(2026-09-23,liblib → 免费层 SKIP + 队列大重排)
- **S2 完成**:agent 深挖(官网实时渲染双源交叉)——🔴"日 300 张"证伪:实为**登录 20 积分/日 ≈ 40 张**(发现路第三次额度坍塌);但**官方 API 成熟**(openapi.liblibai.cloud,Go/Java/Python 先例,LoRA 生态 API 可调=全网唯一)。
- **S3 裁决:liblib 免费层 SKIP;转付费候选 `liblib-api`(¥10 起,LoRA 可控性独有)**。
- **S1 重新发现(用户裁决触发)**:四通道重扫+核验纪律升级 → **队列 v2 大重排**:siliconflow(Kolors 永久免费+¥14 赠金,API/直连)升 #1;新晋 krea/leonardo/pixai/vidu;**排除实锤 Sora(停服)/Grok(撤免费)/higgs(免费归零)/nano**;砍额趋势链实证(2026 全行业收紧,默认不信免费数字)。
- **付费规则入册**(用户 2026-09-23 补充):少量付费高性价比可考虑,人民币记册/征求用户。
- **S7 自评估**:①S1 重扫的核验纪律(官方原文/当月交叉)有效——本轮 ✅ 四家全部官方级;②发现路数据源(第三方目录站)整体不可信已成规律,后续 S1 直接从官方页起步;③liblib 的官网实时渲染取数(关登录弹窗取真实 DOM)是新技能,记档。
- **下轮**:队列 v2 #1 `siliconflow`(S2 深挖+注册赠金使用面;API key 门槛=用户注册,骨架实施不阻塞)。

### 轮 4(2026-09-23,siliconflow → GO 已实施,S5 真机门 BLOCKED-on-key)
- **S2 完成**:siliconflow-probe agent 深挖(API 契约完整到可写 provider)——三处信封偏差(`images[].url` 非 OpenAI `data[]`/图片 URL 1h TTL/batch_size 已移除)、视频 submit-poll 双 TTL(结果 10min+URL 1h)、免费=Kolors 唯一(实名)、代金券 ¥14 先扣、静态价地板(Z-Image-Turbo ¥0.10/张,Wan2.2 ¥2/条)、429 TPM/IPM(2/分,400/日)限流体系。
- **S3 裁决:GO**(免费模型+API 形态+国内直连零代理三重契合;付费线走代金券,每次调用带价格警示无硬确认门)。
- **S4 完成**:`src/providers/siliconflow.ts` 完整 provider(S1xx-S4xx 错误族/7 图模型+2 视频模型/静态价目/costCatalog ¥计价/channelInfo 三态卡/`fetchImpl`+`downloadImpl`+`pollIntervalMs`+`pollDeadlineMs` 四测试缝/requiresOptIn=true);registry 注册(primaryKey=注册赠金);index.ts 三工具描述+路由注接入;README 渠道总表+路由节更新。
- **S5 机械门**:`test/siliconflow.test.ts` 白盒 15 测全绿(生图信封/1h TTL 立即转 data:URI/n 循环 seed+i/i2i Edit 族三态/视频 submit-poll-Succeed-Failed-timeout/S100-S400 全路径/熔断);全量回归 721 测 0 fail。**发现并修复 1 真 bug**:S101/S300 抛错未带 `.status` → `isTransient` 默认 `?? 0` 误判为网络瞬时错 → withRetry 对 4xx 无谓重试(测试 1s 退避暴露)——全部带 sfStatus 后 4xx 立即抛。
- **S5 真机门:BLOCKED-on-key**(config `providers.siliconflow.apiKey` 未配)。用户动作:①注册 siliconflow.cn ②实名 ③cloud.siliconflow.cn/account/ak 建 key ④填 config。待真机把 channelInfo 未知项(代金券实际余额/IPM 实测/Wan2.2 实际耗时)测成已知。
- **S7 自评估**:①**i2i 能力谎报防线生效**——supportsImageToImage()=true 会放行 images 进入 provider,初版实现静默丢弃,白盒自检抓到后补齐 Edit 族接线+非 Edit 模型 S302 明确拒绝(能力声明与实现逐项对齐已列入 S4 完成标准);②共享 withRetry 的 status 判定坑已写进 provider 开发纪律(命名与说明标准节);③价格静态表+每次调用警示的「软计费」模式对本渠道够用(地板价+代金券),不复制 pixverse 硬确认门(避免交互摩擦)。
- **S6 审查(fresh agent,只读)**:首判 **FAIL→修复循环→PASS**。抓出 1 BLOCKER:**A1 n² 双重计费**(provider 内 n 循环 × 工具层 runPool n 扇出,n=8 → 64 次调用;且共享层已把 n 塞进 ImageRequest,provider 必须像 agnes 一样忽略 n)——删 provider 循环+seed verbatim。同批修 MAJOR×3:B1 静默丢弃参数未告警(补 zhipu 式 warn-and-ignore 块:图 aspect/quality/extra、视频 negativePrompt/numFrames/frameRate/resolution/images/audioMediaIds/videoMediaId)、B2 ratio 4:3/3:4 静默当 16:9(补告警)、D1 契约测试盲区(补「provider 恒单张」断言防回归);MINOR×6:A4 下载零重试(withRetry 包裹,丢链=丢已计费产物)、A5 getVideo 下载失败改结构化 failed(可复调)、E3 batch_size 日期措辞(09-30 是未来日)、E4 429「退避重试中」假文案(共享重试不覆盖 429)、E5 costCatalog 视频条目 per-image→per-clip(types 联合类型补值)、B4 size 逐模型固定档写进 channelInfo limits。测试 15→19 全绿;全量 725 测 0 fail;README 双语同步门过。
- **S6 飞轮级教训入库**:①**provider 白盒测试必须含「与工具层契约正交」的断言**(provider 层 15 测全绿仍漏 n²——只测了 provider 自身行为,没测它与 handler 扇出的组合语义;D1 契约断言补上);②README 双语同步门是隐性交付门(en 漏配 siliconflow 行被拦)——渠道交付清单须含 README.en.md;③fresh agent 审查首轮 FAIL 是常态而非事故——本轮抓的 BLOCKER 全是实现者盲区(扇出语义只有跨层看才能发现)。
- **下轮**:队列 #2 `krea`(100 CU/日,需代理 UI)。

### 轮 5(2026-09-23,krea → S2 大反转:官方四通路,转付费候选册待用户裁决)
- **S2 完成**:krea-probe agent 深挖(官方 4 页原文直抓:pricing/terms/api-pricing/mcp)。
- **🔴 反转:发现路「UI 形态需代理」被推翻**——Krea 接入面全渠道最优:**官方 API**(krea.ai/app/api/pricing,`@krea-ai/sdk`,按量预充值 $0 起无月费)+ **官方 MCP**(09-21 新发布,`claude mcp add --transport http krea https://api.krea.ai/mcp`,OAuth 免 key)+ CLI + Agent Skills 四通路;GitHub 零逆向库(官方正门在,生态无逆向需求);UI 登录 Cloudflare Turnstile 且 ToS 明文禁自动化(manual or automated means)→ **UI 通路判死**。
- **额度真相**:100 CU/日 ✅pricing 原文,但**仅 app 消费侧**;官方原文「API billing is separate from Krea consumer plans」——API/MCP 与免费 CU 完全隔离,纯美元按量(K2 Turbo $0.015/张、NB2 $0.08、Seedance $0.0849/s、Veo $0.84/条)。免费层实际购买力:50 张 K2 Turbo/日或 11 张 Medium,但 **NB2 级一张 44-78 CU**;**视频免费层三源互证不可用**(官方对比表 Limited+kive.ai 明说+旗舰视频 all paid plans);3D 50 CU/个每日约 2 个;并发 Image 1。
- **S3 裁决:转付费候选册 `krea-api`**(非 SKIP:渠道价值高,但免费通路对本工具不存在;按用户付费规则记册征求裁决)。三选项交用户:①media-gen-mcp 内嵌 provider(MCP streamable HTTP 裸实现或 SDK)②用户直接一行命令加官方 MCP 独立用(零开发,但脱离本工具统一入口/成本台账)③SKIP。**决策依赖实测**:MCP 计费池归属(API balance vs 订阅 CU)接入后跑一张 K2 Turbo($0.015)5 分钟定案——若走订阅 CU 且 Free 层可用,则 50 张/日免费是意外之喜。
- **S7 自评估**:①**发现路「形态」字段与「额度」字段同权重需核验**——本轮额度数字对但形态错,导致整个接入策略重排;后续 S1 候选必须双字段核实;②官方 MCP 的存在改变渠道判定矩阵:**「有官方 MCP 的平台优先评估直接消费,而非内嵌 provider」**(内嵌的增量价值=统一入口/台账/channelInfo,成本=维护一层 MCP client;逐渠道权衡);③定价页对比表是额度核实的最硬信源(本轮 4 页全原文级)。
- **下轮**:队列 #3 `leonardo`(150 fast tokens/日,UI 需代理)。

### 轮 6(2026-09-23,leonardo → 同 krea 反转:免费锁 UI,转付费候选册)
- **S2 完成**:leonardo-probe agent 深挖(lasso 代理通道过 Cloudflare 实抓 pricing/ToS;docs.leonardo.ai 的 llms.txt+逐页 .md 是官方核验主通道;帮助中心经 Wayback CDX 反查)。
- **🔴 反转同 krea**:发现路「UI 形态」半错——官方 **API+MCP 双形态实锤**(cloud.leonardo.ai/api/rest,Python/TS SDK,异步+webhook,每账号 10 key;mcp.leonardo.ai/v1/mcp 存活实测),但**双形态都吃 API 余额,与 UI 免费 150 tokens/日完全隔离**(官方原文双源:"API access is separate from free or web app subscriptions"/"API Credits vs tokens")。免费通路对本工具不存在。
- **免费层真相(三条重锤)**:①产出全 Public(禁私密)+仅 Basic 质量档;②token 不滚存(每日 24h 重置);③**ToS 8.7:免费订阅用户产出 IP 在创作时即归 Leonardo**(付费档才有产出 IP 8.3)——免费档产出商用法律基础残缺。视频:unlimited 慢速视频仅 Ultimate($60);eesel「Free 含无限慢速视频」被官方现行页证伪。Veo 3 一条≈2,500 tokens(⚠️单源)。
- **API 付费线**:PAYG $5≈¥36 起充值**余额永不过期**;无公开单价总表(藏 app 内计算器——与 fal 对比的劣势);聚合目录全市场最宽之一(Flux 全家含 FLUX.2 Pro/GPT Image 2.5/Kling 3.0/Veo 3.1/Seedance 2.5/NB2 全系/Seedream 5.0/音频/3D Rodin);新账号送 $5 三方多源 vs 官方未确认(矛盾未决,实测定案);ToS 无 anti-automation 条款。
- **S3 裁决:SKIP 免费线(锁死 UI),转付费候选册 `leonardo-api`(排位低于 fal:fal 单价公开且更低价)**。
- **S7 自评估**:①**「免费额度锁 UI 层」已成平台通用模式**(krea/leonardo 连续两轮:API/MCP 与消费侧额度隔离)→ S1 评估新渠道时先问「API 与 UI 额度是否隔离」可提前判形态;②**免费档 IP 条款是隐藏雷区**(leonardo 8.7 产出归平台)——后续渠道 S2 增查「产出 IP 归属」维度;③docs 站 llms.txt/逐页 .md 是官方核验的高速通道(readme.io 系平台通用),记入调研技能;④Cloudflare 分层实测(营销页 403/docs 开放/mcp 无质询)——核验顺序 docs→mcp→营销页最省。
- **下轮**:队列 #4 `pixai`(10,000 积分/日,动漫垂直,UI 需代理)。

### 轮 7(2026-09-23,pixai → **GO**:唯一「免费跟账号走+API 可编程」渠道)
- **S2 完成**:pixai-probe agent 深挖(blog/docs/platform 三站官方原文+直连实测+社区库源码核查)。
- **🔴 模式判定:PixAI 是第三种模式,非 krea/leonardo 型**——免费 10,000 积分/日绑定**账号**(非 UI 表面):Web 后端 GraphQL(`api.pixai.art/graphql`,直连实测 200,**需代理被实测推翻**)即 UI 自己调的 API,社区库(TS/Python)暴露 `claimDailyQuota()`/`getQuota()`,**程序化路径与 UI 同池吃免费积分(构造性必然)**;另有官方 REST API(platform.pixai.art,Bearer key,邮件审批 5 日或会员即时)其计费文档零提及(❓一发实测定案)。
- **额度/能力**:10,000 积分/日(✅官方原文,claim 制+**邮箱验证才发放**反脚本)≈ Standard ×4 批 ×3 = **12 张/日 / 月 360-480 张**(Tsubaki.2 Standard 3,200 积分/批×4张);发图/任务/LoRA 返利可加赚;模型=Tsubaki.3(09-16 新,DiT,对话式编辑+漫画排版)/Tsubaki.2/.1/Haruka V2/Hoshino V2 + **1.8M 社区 LoRA**;视频 UI 有 t2v+i2v(Basic 档免费额度可覆盖)但**官方 API 明确无视频**;Reference Pro/Edit Pro API 不支持(开发中)。
- **IP/商用条款:全部候选中最优**——平台不主张版权(✅官方原文)+无会员墙+**API 产出用户持版权且私有**(永不进公开历史)。
- **风险登记**:GraphQL 非官方路径随时可能变(官方客户端已 archived 印证演化);单账号自律(官方明文反多账号);ToS 程序化灰色未明禁。
- **付费兜底**:Starter ¥72/月含 30 万积分≈**¥0.19/张**(比即梦/可灵便宜一个量级)。
- **S3 裁决:GO(带两个实施验证步)**。验证①官方 REST API 是否扣账号积分(扣=双通道全免费;独立计费=REST 记付费册只走 GraphQL 免费);验证②GraphQL claim+生成冒烟。**S4 双通道设计:GraphQL(免费主)+ REST API(验证后定位)**。
- **S5 真机门:BLOCKED-on-login**——用户动作:①注册 pixai.art ②**邮箱验证**(不验证无每日积分)③profile settings 试生成 API key(或发 api@withpixai.art 邮件审批)④告知账号 token 给 config。
- **S7 自评估**:①三种平台模式分类学成型:**A 免费锁 UI(krea/leonardo)/B 免费跟账号+API 同池(pixai)/C 免费+官方 API 双轨(siliconflow)**——S1 可先按「API 与 UI 额度是否同池」三分再排优先级,B/C 类才值得免费接入;②「需代理」字段也开始证伪(pixai 直连 200)——网络可达性以实测为准,S1 标注仅作参考;③IP 条款维度两轮验证有效(leonardo 雷/pixai 优),固化为 S2 标准维度。
- **下轮**:队列 #5 `vidu`(体验积分+每日登录,Q3 16s 原生音频视频独有)。

### 轮 8(2026-09-23,vidu → SKIP UI;API 记付费候选册)
- **S2 完成**:vidu-probe agent 深挖(vidu.cn/platform.vidu.cn 双端定价页+ToS+API 文档全原文直抓,Cloudflare 极弱零挑战)。
- **模式判定:C 类(免费 UI+官方 API 双轨),但免费侧对自动化不可用**——双否:①UI ToS 明文「不得使用任何自动化脚本、网络爬虫、机器人软件…」(措辞比多数平台更直白,UI 直连路线封死);②**免费产物无商用授权**(「有限的、非排他性、可撤销许可,个人非商业目的;付费用户扩展至商业」);③每日积分官方不公示数值(量级 20/日,只能一两条 6-8s);④免费层水印+16s 档是付费权益(Q3 模型不锁但 16s/1080p 列付费区)。
- **API 付费线(文档完整)**:api.vidu.cn/ent/v2(text2video/img2video/参考生/首尾帧/智能多帧/对口型/延长),Token 认证,回调+签名,并发≤5,需实名;**按秒计价 q3-turbo 720p ¥0.375/s,错峰半价 ¥0.1875/s(国产同档最低档)**;资源包 ¥500 起;API 积分与 UI 两池完全分离(同名积分不同计价体系,铁证);第三方托管=腾讯云/阿里云百炼转售。
- **独有增量(vs 已有 pixverse 订阅池+可灵/火山在册)**:Q3 参考生视频(全球第二,角色一致性场景)+16s 音画同出(对标 Veo 3.1,国产独有)+错峰半价机制。
- **S3 裁决:SKIP UI;`vidu-api` 记付费候选册**(与可灵/火山 API 同册价格-能力对比;先验证 Trial Credits 数值+¥500 入门包实测错峰价再定)。
- **S7 自评估**:①**「免费+API 双轨」的 C 类还须再问一句「免费侧产物商用授权」**——vidu 免费层连个人产出商用都不给,A 类判定再加一票否决维度;②**ToS 自动化措辞强度分级**(vidu 明文点名自动化脚本=红;poe 类 ToS 禁自动化=红;krea「scrape or spiders (manual or automated means)」=红;leonardo 无显式条款=黄;pixai 未明禁=灰)——记入 S2 评估模板;③云厂商托管通道(腾讯云/阿里云)是国产模型的替代接入正路,后续渠道调研增查「是否有云厂转售」。
- **下轮**:队列 #6 `seaart`(130-150 画力/日,⚠️数值打架,动漫,与 pixai 同垂类)。

### 轮 9(2026-09-23,pixai S4 实施 + seaart SKIP-记册)
- **pixai S4 完成**:`src/providers/pixai.ts` 完整 provider——双通道(apiKey=REST v2 严格 schema / token|email+password=GraphQL 免费通道);凭证三选一+recaptcha 两步自动登录+token 响应头提取+磁盘持久化(~/.media-gen-mcp/pixai-token.json)+手动 DevTools 降级指引;每日 claim 幂等闸+余额回读告警(凭证错快速失败非吞掉);模型简单名映射版本 ID(tsubaki2/haruka2/hoshino2);i2i 仅 GraphQL+公网 URL(acceptsImageInputRef 门);REST v2 枚举映射(aspectRatio 11 档/size 1k/promptHelper disable 确定性);**无视频模态按项目模式不实现 VideoProvider**(asVideoProvider 守卫天然拒);P1xx-P4xx 错误族全带 httpStatus。registry+index.ts 三工具描述+路由注+README 双语接入。
- **pixai S5 机械门**:`test/pixai.test.ts` 白盒 15 测全绿(GraphQL 全链路含 claim 幂等/batch 多产物全取/THUMBNAIL 跳过/REST null url 过滤/登录 token 复用/D1 恒单张/契约纪律/错误全路径);全量 740 测 0 fail exit=0。**白盒再抓 2 真 bug**:①`JSON.stringify(undefined)` 返回 undefined 非"" → P400 诊断路径自身 TypeError(两处硬化);②claim 吞凭证错继续走 create 产生误导错(precondition 错改快速失败)。**真机门 BLOCKED-on-login**(待用户注册+邮箱验证+token 三选一)。
- **seaart S2+S3:SKIP-记册(升级观察)**——反转:官方 CLI(sac-cli,npm MIT)+官方 MCP(seaart.ai/mcp,generate_image/image2video/text2video/reference2video/get_task_status)+OpenClaw skill 三官方通道(动漫垂类稀缺);已转型聚合器(Veo/Sora/Seedance 2.5/MiniMax H3/NB/GPT Image 2)。但 vs pixai 全维度负(额度 130-150 画力<10,000 积分;IP 条款 D 级宽授权<pixai 最优;NSFW 严格禁 vs pixai 允许;动漫口碑负)+聚合面与 PixVerse 订阅池七成重叠;ToS 红级自动化条款(官方 CLI/MCP 构成事实豁免通道);**MCP 计费池(stamina 同池 vs 独立网关)未证实=最大未决变量**。revisit:①MCP 走免费 stamina 同池 ②聚合模型免费层可调 → 升级 GO 定位「聚合模型补充通道」。
- **S7 自评估**:①wire 契约挖掘 agent 从 archived 官方客户端提取 GraphQL 原文的路径**可复制成标准 S4 前置**(比拍脑袋写契约快且准);②「claim 幂等闸」这类账号侧 mutation 的设计原则=幂等+告警+precondition 快速失败,记入 provider 开发纪律;③seaart 的「官方 CLI 双轨疑点」(登录制 vs sa- key 制)提示:**官方通道也可能多轨且计费池不同**——S2 接入形态维度须列全所有官方通道并逐轨核计费。
- **下轮**:队列 #7 `kling`(66 灵感值/日,⚠️2024 口径;模型 pixverse 已有,增量=免费量)。

### 轮 10(2026-09-23,kling → SKIP-记册;**队列 v2 全部处置完毕**)
- **S2 完成**:kling-probe agent 深挖(官方 dev 平台/能力地图/更新公告/付费协议全实抓)。
- **模式判定:C 类双轨变体(两池完全分离不可互兑)**——UI 66 灵感值/日锁端内(免费灵感值仅端内使用),且 **3.0 时代贬值到买不起一条**(5s 1080p≈70 灵感值>66;2.1 时代残留口径 20-35 已过时);API=积分预付**无免费层**(视频 1 积分=¥1,试用包 ¥70/100 积分≈166s 720P);UI 自动化被 ToS 机器人条款封死(8.1「机器人软件、蜘蛛软件、爬虫软件…非官方渠道」)。免费层对本工具价值≈零。
- **09-15 下线红线核实精确化**:下线的是「部分早期模型、特效模版及虚拟试衣」(1.0-2.1 视频离场);**2.5 Turbo/2.6/v3/v3-omni/video-o1/image-o1 在线**(v3 原生 4K/15s 多镜头;image-o1 风格训练/人脸参考)。
- **重要发现:官方 Kling Skills**(2026-04-01 上线,clawhub.ai/klingai-dev/klingai)——官方 Claude Code/Cursor/Codex/Copilot/Openclaw 接入件,账号一键绑定或 AK/SK,自动探测中国/全球端点;**可作未来 kling-api provider 的参考实现**。
- **S3 裁决:SKIP-记册(条件触发升 GO)**——PixVerse 订阅池已覆盖 kling 视频主力,「增量=免费量」证伪(**真增量=最新模型面**:v3 4K/15s/Omni 多模态/电商模板 API);需要独占能力时直接接 API(¥70 试用包可验证)。「¥554/年」勘误=UI 黄金年卡,与 API 无关。
- **S7 自评估**:①发现路「增量=免费量」预判再次被证伪(第五次额度/增量类信息坍塌)——**增量判定必须对照用户既有资产**(PixVerse 池/在册 API 调研),发现路只报渠道自身不报差集;②「官方 Skill/MCP 生态」成为新发现维度(kling/seaart 都有官方 agent 接入件)——聚合进 S1 扫描关键词;③两池同名概念(灵感值 vs 积分)是平台常见混淆源,S2 必须分别取官方计价原文。
- **队列 v2 状态:✅ 7/7 全部处置**(2 GO 已实施:siliconflow/pixai;5 转册:krea/leonardo/vidu/seaart/kling)。进入 S1 再发现扫描轮(终止判定:再扫描连续一轮无新价值渠道→飞轮停止)。
- **下轮**:S1 增量再扫描(四通道;过滤标准=模式分类学 B/C 类或高性价比付费候选;已处置渠道排除)。

### 轮 11(2026-09-23,S1 再发现扫描 → **非零新价值,飞轮继续**;队列 v3 产生)
- **S1 完成**:发现扫描 agent 四通道+官方 MCP 生态新维度(五路)。
- **裁决:本轮非零 → 不终止**。4 项入队(队列 v3):**cloudflare**(P1,强 C 类一手确证:10,000 neurons/天全账号续杯+官方 REST+flux-2-dev/phoenix 独家模型面,siliconflow 同构同价值级)/imagineart(P2,B 类待证:每日 100 credits 走 API 未证+疑似 SeaArt 同集团)/prism(P3,YC P25 聚合 API,存活待证)/pika(P4,官方 MCP 但免费 credits 注入 Agent Wallet 未证)。
- **付费册补注 2 项**:recraft(原生 SVG/Lottie 矢量生成全市场独占 $0.08/个——唯一 A 类例外入册理由)/fal(官方 MCP 可用补注)。
- **排除 11 项**(附理由):Together(free flux 促销 2024 起 3 个月已终结)/Stability(25 一次性)/Novita($0.5 一次性)/腾讯混元图像(能力重叠)/Puter.js(浏览器 JS SDK 用户付费模型,非 REST)/heurist(社区项目无承诺)/Luma(无官方 MCP+API 纯付费)/Ideogram+Runway(API 纯付费,能力被既有覆盖)。
- **官方 MCP 生态测绘入库**:fal/pika/ideogram/runway/kling/seaart/krea 有官方接入件;luma 无——「有无官方 MCP/Skill」固化进 S1 扫描维度。
- **S7 自评估**:①终止判定流程首轮运转正确(「连续一轮无新价值」的计数器从本轮起算,下轮 S1 若再零发现→满足连续两轮?不——用户条件是「连续一轮」,本轮非零归零计数,若下一整轮(处置完 v3 后的 S1)零发现即终止);②Cloudflare 证明了「基础设施厂商送 AI 额度」是被遗漏的渠道家族(Workers AI/Puter/GitHub Models 类)——后续扫描增查云厂商托管模型服务;③发现 agent 的排除表自带理由是好实践,固化进 S1 输出模板。
- **下轮**:队列 v3 #1 `cloudflare`(S2 深挖:neuron 单图成本/flux-2 系能力/10k neurons 日张数/区域可用性/REST 契约)。

### 轮 12(2026-09-23,cloudflare → GO 确认并实施;image-only 收窄)
- **S2 完成**:cloudflare-probe agent 深挖(官方文档一手逐字+端点实探;调研与 wire 契约一次产出)。
- **GO 确认(收窄:image-only)**——@cf/ 目录 65 模型**零视频任务**,provider 按项目模式不实现 VideoProvider。C 类成立:10,000 neurons/天全账号续杯(Free/Paid 同额,00:00 UTC 重置);**超额两态:Workers Free 硬停(429/3036,零扣费)/Workers Paid 自动计费 $0.011/千**(付费计划有真实扣费风险→premium 档逐张警示)。
- **档位分层(免费 10k/日)**:flux-schnell 57.6/张≈173 张/日(文生图主力)/flux-2-klein 104/张≈95 张(多参考编辑主力,≤4 张 <512²)/SDXL 双 **$0 Beta 零费**/dev 3,750(黑洞仅 2 张/日)/9b 1,364/lucid ≈6,000/phoenix 2,370——后四者 premium 档(≥1,000 neurons 线)每次调用警示。
- **S4 完成**:`src/providers/cloudflare.ts`——8 模型规格表(简单名→@cf/ 全名/两族 wire:JSON 与 multipart)/premium 档警示/$0 Beta 告警/尺寸 clamp/schnell 无尺寸参数如实告警/i2i 三态门(flux-2 multipart≤4+<512² 告警/SDXL image_b64 剥前缀/其余 C302)/v4 envelope 错误码提取/**429-3036 冷却至 00:00 UTC+快速失败 C201(不误熔断 60s)**/裸二进制防御分支(信响应 content-type)。registry+index.ts 描述+路由注+README 双语。
- **S5 机械门**:`test/cloudflare.test.ts` 白盒 12 测全绿(FormData 字段断言/clamp/额度尽冷却链/二进制分支/D1 恒单张);全量 752 测 0 fail exit=0。**真机门 BLOCKED-on-login**(用户建 API Token+Account ID)。
- **S7 自评估**:①「额度尽=冷却到重置时刻」而非通用 60s 熔断——**每日续杯型渠道的额度尽语义**值得推广到 pixai(积分日清)等其他日额度渠道;②凭据双必需(token+accountId)的配置错误要在 C100 一次说清;③README 双语门又拦一次数字格式差("千"vs"1k")——zh 侧写数字用阿拉伯+单位词根(en 对齐零成本)。
- **下轮**:队列 v3 #2 `imagineart`(B 类待证:每日 100 credits 走 API 未证+疑似 SeaArt 同集团)。

### 轮 13(2026-09-23,imagineart → **GO:首个「官方 MCP+免费日额同池」渠道**)
- **S2 完成**:imagineart-probe agent 深挖(官方 docs/MCP 页/npm/GitHub org 全一手)。
- **核心问题裁决:MCP 通路=B 类同池**——官方托管 MCP(mcp.imagine.art,streamable-http)明文「billed through your existing imagine.art credits」「free tier includes 100 credits/day」「no API key」(OAuth 登录即用,内置 balance 查询工具);**REST API 疑似独立计费**(Billing 页登录墙+reference 站无价+对照修辞)且模型目录陈旧——实施**只走 MCP/CLI 通路**。
- **SeaArt 同集团证伪**:ImagineArt=Vyro AI(土耳其注册,2018 创立 bootstrapped)≠ SeaArt(新加坡 STAR CLUSTER)——SeaArt 轮负面结论不可迁移。
- **额度换算(✅官方价目)**:100 credits/日 ≈ 16-20 张 Z-Image-Turbo/Flux-Dev(5cr)/日或 4 张 NB2/Qwen(24cr)或 **3 条 Wan2.2 视频(30cr/5s720p)**/1 条 Seedance 1.5 Pro(72cr);免费层限制:仅 standard 模型/720p 上限/产出公开/1 并发;**免费产出限非商用(terms 软措辞 may)**+疑似水印(⚠️单源)。
- **聚合舰队顶配**:图像 31 款(NB2/NB Pro/Seedream v4-v5 全系/Flux 2/MJ V7/Grok/ChatGPT Image 2)+视频 44 款(Kling 3.0/Seedance 2/2.5/Veo 3.1/Sora 2/Runway 4.5/Hailuo H3)——免费层可达的是 budget/standard 档。
- **接入形态**:npm `@imagineartofficial/mcp` v0.10.0(2026-09-08,MIT,trusted publishing)除 MCP 安装外**自带 CLI 直发命令**(image/video/music+doctor/status/login/logout)——可复用 PixVerse CLI spawn 先例;官方 Python/JS SDK 对应 REST(不采用)。
- **S3 裁决:GO**(第 4 个免费渠道;定位=素材池/草稿——商用成片需 $13/mo,视频主力仍在 PixVerse)。
- **S4 待实施**(需先挖 CLI wire 契约:命令语法/--json 输出/退出码/login 流)。
- **S7 自评估**:①「官方 MCP+同池」是新最优形态(kling/seaart 的 MCP 计费池都未证实,imagineart 官方明文同池)——S1 扫描对「有官方 MCP」的渠道增查计费池归属一票定性;②同集团疑云靠注册主体一票证伪(Vyro Turkey vs STAR CLUSTER)——S2 模板增「注册主体」查证项;③免费产出非商用条款与 pixai 形成对照(pixai 明文归用户),渠道商用分级:pixai/siliconflow/cloudflare=可商用,krea-api/leonardo-api=API 商用明文,imagineart/vidu UI=免费层非商用。
- **下轮**:imagineart S4(CLI wire 契约挖掘→provider);并行 prism S2。

### 轮 14(2026-09-23,prism → **NO:双重证伪直接排除**)
- **S2 完成**:prism-probe agent(站点/YC 公司页/HN 全帖/prisminference.com 桥接核验)。
- **发现轮两条件双双证伪**:①「免费 100 credits/月」→ 实测 **10 credits/月**(10 倍砍,仅够 1 条最弱档视频);②「官方 API」→ **全站零 API 字样+/docs 404+docs 子域 SSL 死+wayback 零快照=无声退役**(缺席证据三线同向)。
- **底牌**:founder HN 亲口「Models are accessed through Fal」——Prism=fal 加价转售,**要这些模型直连 fal 恒严格更优**;模型清单逐个被既有渠道覆盖(零独占)。
- **公司状态**:2 人团队二次 pivot(session replay→视频→LLM token 推理 prisminference.com,同实体三方互证);视频产品=无投入维持态,无回归触发器。
- **S3 裁决:NO**(不入记册复核位——连 revisit 条件都不存在)。
- **S7 自评估**:①**「pivot 探测」入 S2 模板**(YC/融资新闻里的新公司 6 个月后再看一眼:公司页描述变了没有/原产品还在不在维护)——本轮「站点活着但公司已跑」的分层判别法可复用;②**转售商识别问句**:「上游是谁」——上游可直连时,任何聚合层都必须靠免费额度或独占模型证明自己;③NO 与 SKIP-记册的边界:无 revisit 触发器=NO,有=SKIP(本轮首用 NO 三态)。
- **下轮**:队列 v3 #4 `pika`(最后一项;80 credits/月+官方 MCP,核心疑点=免费 credits 能否注入 Agent Wallet)。

### 轮 15(2026-09-23,pika → SKIP-记册;**队列 v3 终态 4/4**)
- **S2 完成**:pika-probe agent(官方三域实抓:pika.art/experiment.pika.art/dev.pika.art)。
- **核心疑点裁决:免费 credits 官文明文不能进 Agent Wallet**——「Top-up credits are only for Pika Create on pika.art. **They can't be used with the API, MCP**, iOS app, or old.pika.art」;MCP 计费=独立 Agent Wallet(token 包 $7.99/800 起,每次调用扣费官方未公布)。**三账务面互不相通**(Create 消费订阅/API Club 美元 PAYG/Agent Wallet tokens)。
- **免费档已归零**:现定价 Free = **0 credits/month, packs only**(2026-08-17 后重构;发现轮「80/月」三源全过时——eesel 06-24/lumalabs 09 也过时,定价漂移速度再证)。80cr 即使在也只够 0.65 条 Seedance(122cr/条)。
- **能力面**:消费端已转型聚合(头条 Seedance 2.5/Wan 3.0/NB2/GPT Image 2.5);MCP=大杂烩编排器+16 营销 skills;模型面与既有池 ~100% 重复且更贵(纯付费 wallet)。**发现轮勘误**:「无公开 REST API」→ 错,dev.pika.art API Club 是真实付费聚合 REST($10/月会费+首月赠 $10,~15 供应商,自称 fal 八折以下)。
- **S3 裁决:SKIP-记册**(非 NO:再入点=API Club 付费聚合线+Pikaffects 特效面)。`pika-api-club` 入付费候选册。
- **S7 自评估**:①**多账务面平台**(pika 三面)S2 须逐面核计费互通——「一个平台」可能藏三个渠道;②免费档归零是 2026 收紧趋势又一例(poe 3000→300/pika 80→0);③agent skills 分发体系(Pika-Labs/Pika-Plugins)对 media-gen-mcp 自身 skill 设计有借鉴价值,记观察。
- **队列 v3 终态:4/4**(cloudflare ✅实施/imagineart GO→S4/prism NO/pika SKIP-记册)。进入收尾段:imagineart S4 实施 + S1 终轮再扫描(含用户存量调研交叉)。
- **下轮**:imagineart S4(CLI 契约已就绪:npx spawn/--json 双错误路/status 探活含 credits/tools 动态枚举模型)。

### 轮 16(2026-09-23,imagineart S4 落地 + 用户存量调研交叉 → 队列 v4)
- **imagineart S4 完成**:`src/providers/imagineart.ts`——Transport 抽象(生产 npx spawn 版本锁 0.10.0,测试 stub 零 spawn 零积分);**status 探活前置拦下未登录**(🔴 CLI 未登录会自动开浏览器——provider 先 status --json,不签在即 I100+login --no-browser 指引);CLI 机器契约双错误路实现(queue 期 stdout 空+exit 1+stderr 额度文本→I101 配额判别/wait 期 results[].error→I400/exit 130 取消→I201/非 JSON→I202);credits 分层警示(budget 档 5cr 提示省钱/主流档 24cr 提示档位);seed/size/extra 告警忽略(CLI 无对应参);视频阻塞 CLI→**伪 handle**(gemini 先例,内存 promise,getVideo 单次结算);i2i http(s) 门。registry+index.ts 描述+README 双语。
- **imagineart S5 机械门**:白盒 12 测全绿(status 缓存/args 组装/双错误路四态/伪 handle 结算/D1 恒单张);全量 **764 测 0 fail exit=0**;README 双语同步过。真机门 BLOCKED-on-login(待用户 `login --no-browser` 一次性授权)。
- **用户存量调研交叉完成**(agent 读了 AIGC网站/ 8 报告+research/ 50 底稿抽验 5):**严格免费+可编程新增=零**(与用户预设一致,存量边际价值=付费视角);但产出 **队列 v4 三项**(minimax 最大付费遗漏/sandai 独占自回归+音画/tensorart 国际站免费待实测)+付费册年付档位注记批+裁决债务两条(火山方舟覆盖关系/促销时效)+Miora 定谳 NO(通道不存在)。
- **S7 自评估**:①交叉验证了飞轮四通道扫描的完备性——存量 50 底稿与飞轮处置清单的重叠度极高,差集集中在付费聚合与垂类(MiniMax/HeyGen/Hedra/Viggle),免费可编程层飞轮已穷尽当前信息面;②PixVerse 消费档注记:「14 模型 Relax 无限生图」09-14 已被用户终版证伪(实为 3000 积分/月)——**media-gen-mcp 的 pixverse provider 文档引用须用 09-14 终版口径**(列入飞轮收尾任务);③伪 handle(进程内 Map)跨重启失效——imagineart/gemini 共有约束,channelInfo limits 已如实标注。
- **下轮**:队列 v4 #1 `minimax`(S2 深挖:三通路契约/价格复核/1080p 缺档影响)+并行 sandai。

### 轮 17(2026-09-23,minimax → SKIP-记册:付费册首位,差集最大候选)
- **S2 完成**:minimax-probe agent(官方 llms.txt/PAYGO 定价/V2 API/MCP+CLI 双 repo/六源定价交叉直连核验)。
- **三通路核实**:①官方 MCP 半弃疗态(2026 仅季更,Music 工具 08-20 退役,README 已改推 CLI);②mmx-cli 2.2k★ 绑 **Token Plan 订阅,而 Token Plan 明确排除 H3**——CLI 池对 H3 视频不可用;③**REST 为推荐通路**(platform.minimax.io llms.txt 静态文档对 provider 最友好;⚠️国内站是 minimaxi.com 非 minimax.cn,key 与 host 区域错配即 Invalid key)。
- **免费层终裁:无**(国际 PAYGO 零免费;国内仅历史活动;快照复核成立)→ 按飞轮纪律须用户先开通,记册。
- **差集成立(付费册之最)**:音画同出 H3(原生 32kHz 立体声,候选册唯一)/全参考 Ref2VA(≤9 图+3 视频+3 音频)/4-15s 连续时长/2K 直出(1080p 真相=H3 无此档,768P→2K 跳档,2K 下采样实操无损失)/image-01 $0.0035/张/TTS+克隆全域——**单 key 三域**(video+image+TTS 同 Bearer)独一份。输出 IP 归用户(✅官方协议)。
- **wire 契约已备**(充值后直接升 GO 实施):V2 视频 submit/query(task_id→24h 过期 url;usage 含 input_seconds 参考视频计费)+V1 图像/TTS 同步;**双错误形态并存**(V2 OpenAI 风格 402/422/429;V1 base_resp 1002/1004/1008/1026)——实现须双解析。
- **S7 自评估**:①「官方 MCP ≠ 活跃通路」——星标数与维护度是两回事(1578★ 但 2026 仅季更+工具退役),S2 接入形态维度须看 commit 活跃度不只看存在性;②**订阅池与 API 池的排除矩阵**(Token Plan 排除 H3=CLI 通路对王牌模型不可用)——「有 CLI」也须核 CLI 能跑什么;③隐藏计费项(参考视频按秒/图片第 6 张起/音色设计 $3)入册——付费渠道 S2 必须列「隐藏计费项」小节。
- **下轮**:队列 v4 #2 `sandai`(Free 档 API 覆盖待验证——B 类判定最后一问)。

### 轮 18(2026-09-23,sandai NO + tensorart 记册-GO;**队列 v4 收口 3/3,进入终轮 S1**)
- **S2 完成**:sandai+tensorart 双查 agent(官方 docs/计费页/HF 权重页/ToS 全直连核验)。
- **sandai:NO(免费)+付费观察位**——官方原文双向隔离锤死(「API calls will not consume the credits of Magi accounts, and vice versa」);API 平台连 $/credit 都不公开(登录墙);**Magi-2(114B)是研究发布不在任何 API**(现行 API 是 Magi-1 chunks 形态),权重 Apache2.0 开源 307GB=**本地 ComfyUI 工作站资产而非渠道资产**(与用户既有 comfyui-workstation-project 互补,记入观察);GAGA-1 是对口型数字人视频非图模型(存量快照勘误);VidMuse 是聚合 MV 工作站无 API;自回归 chunk 级续写控制是真差集但价格面不可比。观察位转正条件:公开定价+Magi-2 上 API(接入成本半天,chunks 契约已验)。
- **tensorart:记册-GO(付费册)**——UI 免费 50/天锁站(官方 FAQ 明文「cannot be used interchangeably」)但**首登 1000 免费 credits 可真实走 API**(✅官方计费页 First Login 行;≈625 张 FLUX/1250 张 SDXL 种子额);$0.003/credit,**SDXL $0.0024/张=全队列已知最低图价**;数万社区模型云端跑(checkpoint/LoRA/ControlNet 兼容,与本地 ComfyUI 同生态互补免 VRAM)+WAN2.1 视频系数 0.72+ComfyUI 模板云跑;契约全公开(Bearer 简单模式/RSA 签名生产模式/stages 流水线/**calc-credits-job 先算价再下单**/失败不扣费/产出不经审核);短板=充值人工通道+无模型列表 API。种子额耗尽前可与用户确认是否充值——付费册位。
- **S7 自评估**:①「首登种子额」是新形态(非持续免费非纯付费)——**足以支撑一次性全链真机验证但不足渠道级免费**,归付费册带「首登赠金」标注;②开源权重=工作站资产的路由判断(Magi-2 → ComfyUI 本地)是渠道工厂与用户硬件线的第一次交汇,记方法论;③双查 agent(一次跑两渠道)模式成功,收尾期效率工具。
- **队列 v4 终态:3/3**(minimax 付费册首位/sandai NO+观察/tensorart 付费册)。
- **下轮**:终轮 S1 再发现扫描(判定「连续一轮无新价值」是否成立→飞轮终止判定)。

### 轮 19(2026-09-23,终轮 S1:零新价值 → 终止条件满足;S6 补审启动)
- **终轮 S1 完成**:六通道穷尽扫描(四通道+官方 MCP 生态增量+云厂商托管家族)——**零新 B 类/零新 C 类/零新基础设施家族/零新 ≥$5 契约公开种子**;4 边缘案例全部核实到具体数字后排除(VideoGen 免费层数字未公开自相矛盾/Runware $2 低于门槛/kie.ai 无契约数字/Eden AI 自述无固定免费);云厂商三家家核到数字(GitHub Models 纯 LLM 不在赛道/百度千帆 ¥20<\$5/腾讯 LKE 无生成池);Vidofy MCP 付费聚合不入。**「连续一轮无新价值」成立**。
- **终止三条件对账**:①队列全处置 ✅(v2 7/7+v3 4/4+v4 3/3;GO 4 家)②S1 连续一轮零新价值 ✅(本轮)③BLOCKED 项已交用户 ✅(siliconflow key/pixai token/cloudflare token+accountId/imagineart login,四家动作清单均已多次呈现)。**遗留:S6 fresh 审查覆盖**——siliconflow 已审(轮 4 FAIL→修复→PASS),pixai/cloudflare/imagineart 三家 S5 机械门全绿但未过 S6;**收轮前补一轮合并 fresh 审查**(siliconflow 审查教训已前瞻性应用于三家:D1 契约测试/warn-and-ignore/status 纪律,系统性风险已预防,补审为完整性)。
- **边缘留档**:VideoGen(若日后公开免费层确切额度可复议,观察位记一笔)。
- **下轮(末轮)**:S6 合并补审结果处置 → 飞轮总结轮报 → 删除心跳 cron → 付费册攒批交用户裁决。

### 轮 20(2026-09-23,**终章:S6 补审 FAIL→全数修复→PASS;飞轮停止**)
- **S6 合并补审**(fresh agent):首判 **FAIL(2 BLOCKER+6 MAJOR+9 MINOR)**——证明补审必要:三家 image 路径预防到位,但 BLOCKER 全在 image 路径之外。
- **修复清单(全部完成,全量 768 测 0 fail exit=0)**:
  - **A-1 BLOCKER** cloudflare:flux-2 参考图从 URL 字符串改**二进制 Blob 文件部件**(官方 changelog wire 实证;fetchBinaryImpl 注入缝;测试断言 Blob+filename);
  - **A-2 BLOCKER** imagineart:视频伪 handle 补 `p.catch` 哨兵——未观察 rejection 原会击穿整个 server(Node≥15 默认 throw);回归测试「createVideo 后不 poll 不崩」;
  - A-3 index.ts:pinned 直抛前补 notifyUnavailable(额度冷却态不再只在 fallback 分支生效);
  - B-1 imagineart:视频丢弃参数补告警(negativePrompt/frameRate/mode/images)+ **numFrames→--duration 换算**(240@24fps→10s,功能性映射);
  - B-2 imagineart:i2v data:URI 解码写临时文件传 CLI(原为不可达死分支);channelInfo limits 披露;
  - B-3 pixai:P301 补 httpStatus 400(轮 4 教训复发被抓,重试计数测试钉死);
  - B-4 cloudflare:acceptsImageInputRef 从恒 true 改 URI 白名单;
  - MINOR 六项:claim in-flight memo(并发幂等)/claim 统一浏览器头/token 落盘 0600/klein 死分支删/非 premium 逐张 neuron 警示/测试禁读宿主 token(disableStoredTokenLoad 缝)。
- **结构性教训(终章沉淀)**:①**白盒测试会把实现现状锁成期望值**(cloudflare 测试曾断言错误的 URL 字符串形态)——S5 测试必须对着 wire 契约写,不是对着实现写;②「教训前瞻应用」只覆盖了 image 路径,**每个新模态(视频/参考图/异步 handle)都是教训的新战场**;③伪 handle 类异步(gemini 惰性执行 vs imagineart 热执行)两种模式语义不同,复用先例须核对执行时机。
- **飞轮终态对账(20 轮)**:✅队列 v1/v2/v3/v4 全部处置(19 项裁决);✅4 个免费渠道实施(siliconflow/pixai/cloudflare/imagineart,65 白盒测试,S6 双轮审查全 PASS);✅S1 终轮零新价值;✅BLOCKED 四家动作清单已交用户;✅付费册 14 项攒批待裁决;✅文档/README 双语/工具描述/飞轮 SSOT 全程同步(20 commit 全 push)。**终止条件全部满足,飞轮停止,心跳 cron 删除。**

### 轮 21(2026-09-23 深夜,真机门全通——用户配齐四家凭证后的完整实测)
- **配置落地**:config.json 写入 siliconflow apiKey / pixai token(JWT 校验 7 天有效期,9-30 到期)/ cloudflare apiToken+accountId;imagineart 凭据在 ~/.imagine-mcp/(用户已 login)。
- **真机结果矩阵**(产物存 /tmp/channel-it/):
  | 渠道 | 案例 | 结果 |
  |---|---|---|
  | siliconflow | Kolors t2i(免费) | ✅ 5.1s/1730KB |
  | siliconflow | Z-Image-Turbo t2i(¥0.10 代金券) | ✅ 4.6s/849KB |
  | siliconflow | Wan2.2-T2V 视频(¥2 代金券,16:9) | ✅ 提交 363ms→完成 229s/1374KB |
  | pixai | tsubaki2 t2i | ✅ 12.7s,🔴 实扣 2,100 积分 |
  | pixai | haruka2 t2i | ✅ 29.0s,🔴 实扣 4,100 积分 |
  | cloudflare | flux-schnell t2i(58 neurons) | ✅ 4.3s(修 seed 拒收后) |
  | cloudflare | sdxl-lightning($0 Beta) | ✅ 4.9s |
  | cloudflare | flux-2-klein 多参考编辑(A-1 Blob 修复实证) | ✅ 8.0s |
  | imagineart | 登录/status/credits 可见 | ✅ 100 credits 在账 |
  | imagineart | 图像 z-image-turbo / nano-banana-pro | 🔴 双双被 plan 墙拒(1114) |
  | imagineart | 视频 wan-2-2 | 🔴 明文「requires an active subscription」 |
- **真机抓出 3 个白盒漏网真 bug(全修)**:①cloudflare flux-schnell 现行 schema **拒收 seed**(探针契约漂移;补 supportsSeed per-model 声明+告警忽略);②真实 fetch headers 是 Headers 对象,`["content-type"]` 取不到 → 二进制防御分支从未生效(schnell 实际返回裸 JPEG 而非文档声称的 base64-in-JSON);③(前置)A-1 Blob 修复经多参考编辑真机实证有效。
- **🔴 两大渠道真相修正(文档全量改口径)**:
  - **pixai 量能坍塌**:官方「×4 批次价÷4」口径误导——API 单张不享批次折扣,实测 tsubaki2=2,100/张(≈**4 张/日**)、haruka2=4,100(≈2 张/日);channelInfo/costCatalog/index.ts/README 双语全部改为实测口径,定位改「低量动漫+IP 最干净」;
  - **imagineart 免费层生成被锁**:官方 MCP 文档承诺「100 credits/日同池可编程」,实测图像/视频均被 plan 墙拒(报错文案自相矛盾:先说免费只能用 nano-banana-pro,用它又报 1114 不在 plan)——credits 可见不可花,免费价值≈0,解锁需 $13/mo;文档全部改「PAID-GATED」口径,选型 tag 改「未订阅勿选」。
- **全量回归 768 测 0 fail exit=0;README 双语同步过。**
- **教训**:①**「官方文档承诺」与「免费层实际可用性」必须真机分离验证**——imagineart 的同池文档与 pixai 的批次折扣价都是文档层成立、实测层坍塌;②白盒测试永远抓不到「上游 schema 漂移」(seed)与「运行时对象形态差异」(Headers)——真机门不可省;③真机三渠道产物已存档 /tmp/channel-it/ 供人工目检。

## 飞轮重启(v2,2026-09-24 用户裁决)

> 用户发现 mindvideo.ai 不在扫描覆盖内 → **旧 S1「已穷尽」结论作废**,网重织重启。主攻方向:**免费生成视频**。配置动作:pixai/imagineart 已入 disabledProviders(轮21 真机实证:前者量能坍塌、后者免费层被锁)。

### 轮 22(2026-09-24,重启:认错重扫 + mindvideo 专项)
- **S1 教训入册(用户点名)**:旧六通道网眼太粗——漏工具导航站长尾/SEO 长尾词/白牌家族监测;「连续一轮零新价值即终止」的判据在网不密时过早成立。**修正:终止判据升级为「重织后的网连续两轮阴性 + 每张网须报告扫描页数/词数」**。
- **双 agent 并行**:mindvideo.ai 专项 S2(免费图/视频真实性+上游白牌判定+可编程性)+ S1 重启深扫(七张网:中文导航站长尾/SEO 长尾/英文目录翻页/新站换皮监测/reddit 深翻/白牌家族/开源免费推理通路;主攻免费视频)。
- **待办**:两 agent 结果回来 → 队列 v5 排序 → S4 实施(沿用 7 段结构)。
