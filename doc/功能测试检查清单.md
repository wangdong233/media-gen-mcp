# media-gen-mcp 功能测试检查清单（v0.8.1）

> **用户视角验收清单**：照着每条执行，勾选确认功能正常。覆盖 12 工具 + 0.6–0.8.x 全部能力 + 横切约束 + 引擎生命周期。
>
> **怎么用**：重启 Claude Code 会话（连最新版）→ 对 Claude 说每条左侧的话（或自选参数）→ 看右侧预期 → 勾 `[x]`。产出默认落 `output/`，建议带 `name` 命名。
>
> **标注**：🆕 = 0.6+ 新增/修复能力 · ⚡ = 本地秒出 · 🌐 = 需联网/Key

---

## ① generate_image · AI 文生图/图生图 🌐

### 基础
- [ ] **1.1** 文生图默认 — "生成一只赛博朋克橘猫" → 出 1 张图，写实清晰
- [ ] **1.2** 🆕 name 自定义 — 带 `name:"橘猫"` → 文件名直接 `橘猫.png`（中文保留，非 mv）

### 批量
- [ ] **1.3** 🆕 n=3 批量 — `n:3` → 返回 **3 张**（`-1/-2/-3` 后缀；旧版只 1 张）

### provider / 路由 / 约束
- [ ] **1.4** 🆕 跨 provider 自动路由 — `provider:agnes, model:cogview-4` → 自动转 zhipu + warning（不再 503）
- [ ] **1.5** 🆕 zhipu size 吸附 — `provider:zhipu, size:1080x1920` → 自动吸附合法（不再 400）
- [ ] **1.6** 🆕 水印控制 — `provider:zhipu` → 响应带 `watermarked` 标志 + warning
- [ ] **1.7** 图生图 — 先出一张线稿，再用 `images:[url]` 转水彩 → 保留构图换风格
- [ ] **1.8** 🆕 images 拒绝（zhipu）— `provider:zhipu + images` → 友好报错"不支持图生图"

---

## ② create_video · AI 视频 🌐

- [ ] **2.1** 文生视频 — "星云推进 3s 720p" → mp4 可播
- [ ] **2.2** 图生视频 — 先出海上日出图，再让它动起来
- [ ] **2.3** 关键帧 — 白天/夜晚城市两帧 → 日夜交替过渡
- [ ] **2.4** 🆕 异步转 handle — 5s 视频省略 wait → 返回 handle + 预估（不阻塞）
- [ ] **2.5** 🆕 numFrames×resolution 钳制 — `1080p + 441帧` → 自动降 241 + warning
- [ ] **2.6** 🆕 frameRate 跨 provider — 路由到 cogvideox + frameRate 24 → 吸附 30 + warning
- [ ] **2.7** 🆕 mode 白名单 — `mode:"invalid"` → 友好报错"须为 text-to-video/image-to-video/keyframes"

---

## ③ get_video · 异步轮询 🌐

- [ ] **3.1** 轮询下载 — 查 2.4 的 handle → completed 后下载 mp4
- [ ] **3.2** 🆕 outDir 一致 — `get_video outDir` 与 create_video 一致（不散落两处）
- [ ] **3.3** 🆕 retry 提示 — in_progress/queued → 返回 `retry_after_seconds` + hint
- [ ] **3.4** 🆕 失败保留 handle — 视频失败 → 返回 videoId/taskId（可 get_video 复查，不丢句柄）

---

## ④ list_models · 模型清单 ⚡

- [ ] **4.1** 🆕 模型 + 双约束 — 返回 `imageModels`/`videoModels` 拆分 + `videoConstraints`(含 allowedFrameRates) + `imageConstraints` + `estimate_example`

---

## ⑤ generate_diagram · 结构图 ⚡

- [ ] **5.1** d2 架构图 — 三层架构（前端→网关→DB+缓存）→ 自动布局清晰
- [ ] **5.2** d2 时序图 — `shape: sequence_diagram` → 登录流程生命线
- [ ] **5.3** 🆕 graphviz 流程图 png — 审批流程（菱形判断）→ 尺寸受控（不窄长爆炸）
- [ ] **5.4** 🆕 d2 theme 名 — `theme:"neutral"` → 生效（名/数字均可，非法则报错）
- [ ] **5.5** 🆕 DOT 误走 d2 提示 — DOT 代码 + engine 默认 d2 → 报错提示"设 engine:graphviz"

---

## ⑥ generate_chart · 数据图表 ⚡

- [ ] **6.1** 柱状图 — Q1–Q4 销售额 → 4 柱高度对应
- [ ] **6.2** 折线多系列 — 产品 A/B + 图例 → 两线颜色区分
- [ ] **6.3** 饼图 — `mark:arc + theta` → 扇区比例正确
- [ ] **6.4** 🆕 默认尺寸 — spec 不给 width/height → ~640×400 量级（不极小 ~100×240）

---

## ⑦ generate_formula · 公式 ⚡

- [ ] **7.1** 简单 — `E=mc^2` → 字形正确
- [ ] **7.2** 复杂 — 正态分布积分（分数/积分/根号）png → 清晰无缺字
- [ ] **7.3** 🆕 fontSize 生效 — fontSize 18 vs 50 → 输出尺寸明显不同
- [ ] **7.4** 🆕 background 参数 — `background:"transparent"` → 透明底（非强制白）

---

## ⑧ generate_card · 卡片 ⚡

- [ ] **8.1** og 卡 — 标题+副标题+渐变辉光 → 1200×630 布局层次
- [ ] **8.2** quote 卡 — 引言 + 署名
- [ ] **8.3** hero 中文 — "一键出图" → 无 tofu/乱码
- [ ] **8.4** panel 卡 — 玻璃面板
- [ ] **8.5** 🆕 blob 自适应 — hero + 浅色 bg → 深色光斑**可见**（不与 bg 同色融入）
- [ ] **8.6** 🆕 muted/panel 浅 bg 自适应 — 浅 bg + 深色字 → 正文/面板**可读**（WCAG）
- [ ] **8.7** 🆕 模板丢字段 warning — `minimal + body` → warning"body 在 minimal 不渲染"
- [ ] **8.8** 🆕 accent≈bg warning — accent 与 bg 同色 → warning"对比度过低"
- [ ] **8.9** 🆕 fontPath 友好错 — fontPath 指向不存在文件 → 可读错误"文件不存在"

---

## ⑨ generate_qrcode · 二维码 ⚡

- [ ] **9.1** URL 默认 — 指向 GitHub → 可扫
- [ ] **9.2** 彩色 + png — 深蓝前景 + 高容错 → 可扫
- [ ] **9.3** 🆕 width 参数 — `width:400` → 400px（打印可用）
- [ ] **9.4** 🆕 颜色校验 — `dark:"black"` → 友好报错"须 hex 或 CSS 色名"

---

## ⑩ generate_icon · 图标 🌐（Iconify 联网）

- [ ] **10.1** `mdi:rocket` → 火箭矢量清晰
- [ ] **10.2** `lucide:palette` 红色 256px png
- [ ] **10.3** 🆕 background 参数 — 自定义背景色
- [ ] **10.4** 🆕 404 候选搜索 — `icon:"mdi:rockt"`（拼错）→ "Did you mean: mdi:rocket..."

---

## ⑪ render_svg · SVG→PNG ⚡（含滤镜需 Chrome）

- [ ] **11.1** resvg 几何 — 纯色圆/矩形 → 几何准确
- [ ] **11.2** chrome 滤镜辉光 — feGaussianBlur → 辉光保真（resvg 会糊）
- [ ] **11.3** 🆕 CSS filter 检测 — SVG 含 `filter:blur()` → auto 走 chrome

---

## ⑫ render_video · 确定性视频 ⚡（需 Chrome + ffmpeg）

- [ ] **12.1** CSS 动画 mp4 — 圆球横移 2s → 球真移动
- [ ] **12.2** 文字飞入 — 大标题 ease 落位
- [ ] **12.3** 确定性 — 同输入两次 → **逐字节一致**
- [ ] **12.4** 🆕 gif 格式 — palette 优化（体积/质量优于旧版原生 gif）
- [ ] **12.5** 🆕 MAX_FRAMES warning — `fps:60 + duration:120` → 截断 warning

---

## 横切：约束前置 / 错误可操作性

- [ ] **X.1** 🆕 ratio 白名单 — `ratio:"wide"` → 报错"须为 16:9/9:16/1:1/4:3/3:4"
- [ ] **X.2** 🆕 images/keyframes URI 校验 — 本地路径 → 报错"须 http(s):/data: URI"
- [ ] **X.3** 🆕 numFrames/duration 互斥 — 同传 → 报错"二选一"
- [ ] **X.4** 🆕 503 自动重试 — agnes 偶发 503 → 自动重试恢复（日志可见，不抛给用户）

---

## 引擎生命周期（独立调用稳定性）

- [ ] **L.1** 🆕 d2 独立退出 — 独立脚本 render d2 后进程**自然退出**（不 hang；旧版 worker pin）
- [ ] **L.2** 🆕 chrome 不 hang — render_svg 走 chrome 后 ~30s 释放（旧版 5min hang）

---

## 验收统计

| 组 | 检查点数 |
|---|---|
| generate_image | 8 |
| create_video | 7 |
| get_video | 4 |
| list_models | 1 |
| generate_diagram | 5 |
| generate_chart | 4 |
| generate_formula | 4 |
| generate_card | 9 |
| generate_qrcode | 4 |
| generate_icon | 4 |
| render_svg | 3 |
| render_video | 5 |
| 横切约束 | 4 |
| 引擎生命周期 | 2 |
| **合计** | **64** |

> 全部勾选通过 = v0.8.1 功能验收完成。🆕 项为 0.6–0.8.x 新增/修复能力（用户感知的差异点），重点验。
